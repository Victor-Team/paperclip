## 裁定 + 派单（开发部负责人）

[@开发部·后端实现席](agent://e5688583-b5ee-4366-8445-d219f4a23c3a) 你要的两件事都办了。先说裁定，再说派单，最后一条是你没处理、我给你补上的问题。

---

## 一、裁定：期望 1 的空响应半边 —— **采纳你的反驳**，但改成范围裁定并带前提

你的三条一手证据我逐条核过了，**成立**（不是采信你的自述）：

| 你的主张 | 我的核验 |
|---|---|
| `run-liveness.ts:345,367` 把「成功但无有效输出」判 `empty_response` | ✅ 两处都在，`return output("empty_response", …)` |
| `run-liveness-continuations.ts:11` 把它列为可续跑 | ✅ `ACTIONABLE_LIVENESS_STATES = new Set([...])` 含 `empty_response` |
| 默认 2 次有界续跑 | ✅ `DEFAULT_MAX_LIVENESS_CONTINUATION_ATTEMPTS = 2` |

**裁定：期望 1 收窄为 ERROR 半边。空响应半边不改判 `failed`。** 理由采纳你写的两条：① 会整条绕开一个现成的一等通道；② 会在 `errorCode` 命名空间新造一个与 liveness 命名空间同名不同义的 `empty_response`，这种同名不同义会长期误导读代码的人。

🔴 **但这条裁定带两个前提，你要知道它不是无条件的：**

**前提一 —— 空响应半边并入期望 2 的范围。** 我查了 `successful-run-handoff.ts:230`，确认你说的死角是真的：

```ts
: keyValueRow("Recovery owner", "Board decision required")
```

而 `empty_response` 续跑耗尽后走的**正是** `successful_run_handoff` —— 和 429 那条撞在同一个死角上。你自己在交付里也写了：TOK-179 的 `85a8962a`（SUCCESS + 空）确实走到了 `successful_run_handoff_required`，「那条路是通的」。**它通到的地方是死的。** 所以我把「`empty_response` 续跑耗尽」这条入口**写进了期望 2 那张单的验收点**，不许只覆盖 429。换句话说：**这条裁定以期望 2 修好为前提**，期望 2 不修好，裁定站不住。

**前提二 —— 那条通道在默认配置下真的开着，要由复核席独立核。** 你说它「默认 2 次有界续跑」，这是自述。按 R1，我把它写成 TOK-186 的判据 5：**若默认关着（需要 flag 才开），裁定翻转。** 那不是我推翻你，是前提不成立。

---

## 二、派单：4 张，串成一条链

我没按你建议的「期望 2 与期望 3 并行」派。**理由写清楚：** 你的判断是「两张单碰的文件互不重叠，可并行」，文件层面你说得对；但两张单共用同一个工作树，而 `heartbeat.ts` import `recovery/service.ts` —— 期望 3 改 `heartbeat.ts` 的中间态会让期望 2 跑 `recovery/` 全量时 `tsc` 直接红。文件不重叠 ≠ 可并行，**共享工作树 + 编译依赖也算撞车**。所以串行。

| 单 | 席位 | 干什么 | blockedBy |
|---|---|---|---|
| **TOK-186** | 代码复核席 | 复核你这 6 个文件（7 条类形判据） | — |
| **TOK-187** | 整机放行席 | 默认生产入口断言 + 接线断言 + 回滚候选 | TOK-186 |
| **TOK-188** | 你 | 期望 2（含 `empty_response` 入口扩面） | TOK-186, TOK-187 |
| **TOK-189** | 你 | 期望 3（派发期模型护栏） | TOK-188 |

TOK-186 已被复核席接走，状态 `in_progress`。

**复核与放行串行，不并行**：放行那 44 条内嵌 Postgres 测试很重，复核若出阻断项，放行白跑一遍还得重跑。两张单的步骤 0 都写了「上游结论是阻断项就不开跑」。

**子单建在「平台改造·ANC」项目（`2e3af214-…`）而不是开发部 project** —— 开发部 project 的工作区指向部门档案目录，干不了代码活；这个项目的工作区就是 `/home/yuanjian/dev/ANC`。

---

## 三、你漏掉的一件事：**这个候选没有版本标识**

你报了「可交付」，但工作树**一行都没提交**。按我的验收点（「每次交付都能指到候选版本标识与证据目录」），现在指不到。而且工作树里混着**不属于本事项的改动**：

```
 M cli/src/checks/managed-install-check.ts
 M cli/src/__tests__/managed-install-check.test.ts
 M ui/src/i18n/index.ts  +  47 个 ui/src/i18n/locales/*.json
 M ui/src/pages/Search.tsx
```

这些来历不明 —— 属于董事会或别的窗口，**不碰、不 stash、不 revert**（纪律第三条：未知改动不越界）。我在四张单里都把这句写死了，并把你的 6 个文件清单写成精确白名单。

**我先记了指纹兜底**，让复核对象可复现：

```
git -C /home/yuanjian/dev/ANC diff -- server/src/services/heartbeat.ts server/src/services/recovery/service.ts \
  server/src/__tests__/heartbeat-retry-scheduling.test.ts server/src/services/recovery/provider-failure-classification.test.ts | sha256sum
# f2c8318eefe97aac675b14a5d8c7fa6954427b2e3853662272468330e7f5835e
sha256sum server/src/services/adapter-final-response.ts      # 05a70ffcbde2a90c3d71477f984ae5b31e04f3d0e3f5d9f3b3fd0daf8bf671ff
sha256sum server/src/services/adapter-final-response.test.ts # 9b139faae6a0afd16cd318eb110c46f8be562f6f58b0ebafdd7e8a20e9b03ac5
```

🔴 **你的动作**：TOK-186 / TOK-187 都通过之后，在 TOK-188 的第一步**把这 6 个文件（精确 `git add`，不许 `git add -A`）提交到一个分支（不许直接提 `master`），SHA 回报在 TOK-188 评论**。那个 SHA 就是候选版本标识，也是 TOK-189 的解阻条件 —— 在一个未提交的 `heartbeat.ts` 上再叠一层改动，会把两个事项的候选搅成一团。我自己不提交。

---

## 四、复核席要额外盯的一处（你的取证没覆盖完）

你查了 `process/execute.ts:89,100` 的 `resultJson` 是 `{stdout, stderr}`（我复核了，对的），但**没枚举其余一等适配器**。新守卫会在 `resultJson`（顶层或 `parsed`）出现 `status: ERROR/FAILED` 或 `is_error: true` 时改判 `failed` —— 假阳性面没穷尽。

按「判据须类形而非实例形」，我写进 TOK-186：**枚举所有写入 `resultJson` 的一等适配器，逐个列出这两个键是否存在及其语义；任何语义不是「本轮 agent 回合失败」的命中即为阻断项，并要求报出 N/M 两个数字。** 另外两条也进了 checklist：

- `readStructuredFinalResponse` 的 `for (const candidate of [nested, resultJson])` 取**第一个**命中就 return，于是 `parsed.status === "SUCCESS"` 而顶层 `is_error === true` 时返回 SUCCESS、**不判失败**。这是取舍还是漏判，请复核席给结论。
- `nativeFinalization` 分支排在新守卫**之前**，原生运行器不受守卫影响 —— 确认这是有意的作用域划分。

---

## 五、期望 3 的配置层未决项：我接下，不归你

你说得对：`gemini-3.5-flash` 是 `agents.adapterConfig.model` 的**过期钉选**，不是代码里的硬编码（我认你那个「未找到（非穷尽）」的三态措辞，写得准确）。代码护栏是开发部交付物，已派 TOK-189，并按你的建议写了 **列表为空必须 fail-open** 的验收点（要求写明理由 + 空列表分支有独立测试 + 复核席单独审这一条）。

🔴 **但 TOK-179 那个活着的 agent 上的过期钉选本身，不归开发部代码席改** —— 那是配置动作。护栏合进去也**不会**自动修好那个 agent 已经存在的钉选。这是「要别的部门配合」，属于我该回头找 CEO 的四种情形之一，**我在本轮汇报里报给 CEO 路由**。TOK-189 里也要求你交付时再点一次这个未决项，别让它随代码交付一起消失。

---

**本单处置**：`in_review` 不变，加 `blockedByIssueIds = [TOK-186, TOK-187]`。复核与放行完成会自动唤醒本单，届时我核三件事（复核无阻断项 / 放行有默认入口断言结果 / 有回滚候选）再决定交付还是退回。**你现在不用动**，等 TOK-188 被解阻唤醒。

**自身失误登记：本次未发现。** 本轮我只做路由、裁定、派单与取证核验，未改任何代码文件。（上一步的部门级教训——「负责人不派单、让 CEO 当瓶颈」——本轮 4 张单全由我自己建，未回退给 CEO 拆。）
