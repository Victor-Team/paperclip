## 负责人核验：TOK-186 复核已过，B3 升格派单

### 一、复核结论：无阻断项（我抽查过，不是照抄）

TOK-186 已 `done`，报告落盘 `/home/yuanjian/dev/ANC/交付/TOK-185/代码复核.md`（20KB）。总结论 **无阻断项、无应改项**，3 条建议（B1–B3）。

**我抽查的是覆盖面**，因为我派单时把判据 1 的作用面写窄过一次（写成 `server/src/adapters/`，后来补正）。核查结果：报告的 N=11 **确实覆盖了补正后的范围** —— `packages/adapters/gemini-local/src/server/execute.ts:726-729`（#5，守卫的主要作用对象）、`claude-local`（#6）、`packages/adapter-utils/src/acpx-engine`（#9），以及两个**本机已安装的外部插件**（#10 antigravity_local、#11 omp_local）都被一手源码核验过，不是按包名推断。M=4 逐个判语义。抽查通过，不追问。

顺带一条有价值的副产品：报告查出 `claude_local` 的 `is_error===true` 必然伴随非空 `errorMessage`（`execute.ts:1125`），acpx-engine 是 `exitCode` + `errorMessage` 双重门（`:4971,4973`）——**所以新守卫的实际新增作用面只有 `gemini_local` 和 `antigravity_local` 两处**，比我原先估的窄。假阳性面比预期小。

### 二、裁定前提二成立，期望 1 的裁定**不翻转**

我上一条评论把裁定挂在一个前提上：`empty_response` 通道必须在默认配置下真的激活，且**要由别人核**。复核席核了（判据 5），追到调用链：

```
heartbeat.ts:24793  if (!conversationSettled) { await handleRunLivenessContinuation(livenessRun); … }
heartbeat.ts:12977  livenessState !== "plan_only" && livenessState !== "empty_response" 才提前返回
```

主流程无条件执行，只依赖运行时状态，**未发现任何 flag 包裹**。前提成立，裁定维持：**期望 1 收窄为 ERROR 半边，空响应半边不改判 `failed`，并入 TOK-188 的验收点。**

### 三、B1–B3 处置：B3 我升格为「应改」并派了 TOK-192

| # | 处置 | 去向 |
|---|---|---|
| B1（acpx 词汇表是不成文约定） | 并入 B3 那张单一起做（纯注释，成本近零） | **TOK-192** |
| B2（外部插件可能跨层拆分 status） | **不做** —— 复核席原话「无需代码改动，风险留痕即可」，报告本身就是留痕 | 结案 |
| B3（配额正则扩宽） | 🔴 **升格为应改** | **TOK-192** `b2486c4e-c2f5-49a6-9593-96f7b70a29a7` |

**为什么升格 —— 不是推翻复核席。** 报告原话「也可由负责人自行决定是否本轮一并处理」，这是他交回来的处置权。他的定级在他职责内成立（不影响本次改动的正确性）；我补的是另一个视角：**它引入了新的错误行为**。

我自己跑了新旧正则对照（不取自述），分清本次引入与既有：

| 样例 | 旧 | 新 | 归属 |
|---|---|---|---|
| `429 RESOURCE_EXHAUSTED: … exceeded the weekly usage quota` | 否 | 中 | ✅ 真正例 |
| `Gemini API quota reached` | 否 | 中 | ✅ 真正例 |
| `exceeded the storage quota for this repository` | 否 | 中 | 🔴 本次新引入 |
| `Storage quota reached for bucket` | 否 | 中 | 🔴 本次新引入 |
| `RESOURCE_EXHAUSTED: connection pool exhausted` | 否 | 中 | 🔴 本次新引入 |
| `RESOURCE_EXHAUSTED: grpc: received message larger than max` | 否 | 中 | 🔴 本次新引入 |
| `Disk quota exceeded` | 中 | 中 | ⚪ 既有，**不算本轮账、不修** |

代价是实的（`recovery/service.ts:655`→`686-691`）：误判配额且无 `retryNotBefore`、无时钟重置文案 → `retryAt = now + PROVIDER_QUOTA_RECOVERY_DEFAULT_BACKOFF_MS` = **60 分钟**；而瞬时错误基线 `CONTINUATION_RECOVERY_TRANSIENT_BASE_BACKOFF_MS` = **60 秒**。一次连接池耗尽会被停一小时，**60 倍**。gRPC 的 `RESOURCE_EXHAUSTED` 是通用状态码，非配额语境用得很多，这条会真被踩到。

判据就是那条刹车规则：**不做，系统会不会错？会。** TOK-185 修的是「任务树停死」，拿一个 60 倍退避换另一种停滞，不算修好。

### 四、TOK-192 插在提交之后，不插在提交之前

我一开始想把它插在 TOK-187 和候选提交之间，**这是错的，已纠**。两个理由：

1. TOK-187 此刻正在这棵树上跑放行。放行完再改 `recovery/service.ts` 再提交，提交出来的 SHA 就不是放行验过的那棵树 —— 我自己定的「能指到候选版本标识与证据目录」被我自己打断，44 条放行还得白跑一遍。
2. 「提交前必须改」事实上就是把 B3 当阻断项，**那才是越过复核席的结论**。

所以：**TOK-192 blockedBy TOK-188**（候选 SHA 落地之后），并把 TOK-192 加进 **TOK-189** 的 blockedBy —— 理由和 188/189 串行一样：`heartbeat.ts` import `recovery/service.ts`，192 改到一半 189 跑 `tsc` 会红。

当前链（读回确认）：

```
TOK-186 done  →  TOK-187 in_progress  →  TOK-188  →  TOK-192  →  TOK-189
```

### 五、给董事会那份说明

[@开发部·后端实现席](agent://e5688583-b5ee-4366-8445-d219f4a23c3a) 你写的那份中文说明我认可，**不重复、不改写**。「只留了一条评论，没留一张单」这句概括得比我准。另外你撤回并行建议时给出的反对自己的实证（那条 import 恰恰是你这次新增的）、以及承认候选无版本标识——两条都记在你名下，不在我这边重复记账。

你回的第 5 点（`readStructuredFinalResponse` 取第一个命中、`nativeFinalization` 排序）复核席已判：**前者是可接受的取舍、不是漏判**（判据 2，已用一手源码核验当前所有生产者都不跨层拆分）；**后者是有意的作用域**（判据 3）。两处都不用改，TOK-188 不必再动它们。

---

## 本单处置

`in_review` 不变，`blockedBy = [TOK-186(done), TOK-187(in_progress)]`。TOK-186 已自动清掉，TOK-187 还活着，这是真实路径，不是挂着等人。

**我还不能验收** —— 我要核的三件事只齐了第一件：

| 要核 | 状态 |
|---|---|
| ① 复核无阻断项 | ✅ 已核（含覆盖面抽查） |
| ② 整机放行有默认入口断言结果 | ⏳ TOK-187 跑着 |
| ③ 有回滚候选 | ⏳ 同上（TOK-187 判据 5） |

TOK-187 交付时会按单里要求提及我，届时我核②③并决定交付还是退回。

**自身失误登记：本次发现一条。** 我第一反应是把 TOK-192 插在候选提交**之前**，那会让放行验过的树与提交的 SHA 脱钩、并把复核席标为「建议」的项事实上当成阻断项。已在落笔前纠正（见第四节）。教训可提炼为：**「改动要不要合进本轮」和「插在流水线哪一步」是两个独立问题** —— 前者看严重度，后者看证据链完整性，混在一起答就会用一次正确的升格毁掉一次有效的放行。这条我按 `engineering-lessons` 准入四条判：有代价（会白跑 44 条放行 + 破坏候选标识）、可复用、可执行、非显然 —— **符合，登记进库由本轮负责**，不推给下游。
