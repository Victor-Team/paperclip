# TOK-185 整机放行记录

- 验收席：开发部·整机放行席
- 验收单：TOK-187
- 候选实现单：TOK-185
- 上游代码复核：TOK-186
- 验收日期：2026-09-16
- 边界：只验收交接包指定的 6 个文件；未改代码、测试或任何非本单文件。

## 0. 开跑前置

命令：

```sh
sed -n '1,240p' /home/yuanjian/dev/ANC/交付/TOK-185/代码复核.md
```

实际结果：文件第五节的总结论是 **“无阻断项”**，允许启动整机验收。

## 1. 候选指纹

命令：

```sh
git diff -- server/src/services/heartbeat.ts server/src/services/recovery/service.ts server/src/__tests__/heartbeat-retry-scheduling.test.ts server/src/services/recovery/provider-failure-classification.test.ts | sha256sum
sha256sum server/src/services/adapter-final-response.ts server/src/services/adapter-final-response.test.ts
```

开跑前与全部验收结束后各执行一次，两次结果一致：

```text
f2c8318eefe97aac675b14a5d8c7fa6954427b2e3853662272468330e7f5835e  -
05a70ffcbde2a90c3d71477f984ae5b31e04f3d0e3f5d9f3b3fd0daf8bf671ff  server/src/services/adapter-final-response.ts
9b139faae6a0afd16cd318eb110c46f8be562f6f58b0ebafdd7e8a20e9b03ac5  server/src/services/adapter-final-response.test.ts
```

断言：**通过**。三项均与交接包期望值精确相等，验收期间没有候选漂移。

## 2. 判据 1：默认生产入口 `heartbeat.invoke`

### 2.1 实际走过的接缝与落库断言

新增接缝用例直接调用：

```ts
const run = await heartbeat.invoke(agentId, "on_demand", {}, "manual");
const finishedRun = await waitForRunToFinish(heartbeat, run!.id);
expect(finishedRun?.status).toBe("failed");
expect(finishedRun?.exitCode).toBe(0);
expect(finishedRun?.errorCode).toBe("adapter_failed");
expect(finishedRun?.error).toBe(STRUCTURED_ERROR_QUOTA_MESSAGE);
expect(
  (finishedRun?.resultJson as Record<string, unknown> | null)?.errorFamily,
).toBe("provider_quota");
```

其输入中 `STRUCTURED_ERROR_QUOTA_MESSAGE` 的实际字符串是：

```text
API error (attempt 7): RESOURCE_EXHAUSTED (code 429): Individual quota reached.
```

执行命令（默认 Vitest 配置，无候选功能开关）：

```sh
pnpm exec vitest run server/src/__tests__/heartbeat-retry-scheduling.test.ts
```

完整终端输出：

```text
[WARN] The "pnpm" field in package.json is no longer read by pnpm. The following keys were ignored: "pnpm.patchedDependencies", "pnpm.overrides". See https://pnpm.io/settings for the new home of each setting.
(!) Your Vite config uses features that are unsupported by `configLoader: 'native'`, which is planned to become the default in a future major version of Vite:
  - `__dirname` (vitest.config.ts:7:25). Use `import.meta.dirname` instead
Set `VITE_CONFIG_NATIVE_IGNORE_WARNING=true` to suppress this warning.

 RUN  v4.1.11 /home/yuanjian/dev/ANC

[08:43:13] INFO: Loading external adapter package {"packageName":"/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local","packageDir":"/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local","entryPoint":"./dist/index.js","modulePath":"/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local/dist/index.js","hasUiParser":false}
[08:43:13] INFO: Loaded UI parser from adapter package {"packageName":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter","uiParserFile":"./dist/ui-parser.js","size":13146}
[08:43:13] INFO: Loading external adapter package {"packageName":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter","packageDir":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter","entryPoint":"./dist/index.js","modulePath":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter/dist/index.js","hasUiParser":true}
[08:43:13] INFO: Loaded external adapters from plugin store {"count":2,"adapters":["antigravity_local","omp_local"]}
[08:43:22] WARN: run failure report has no task id, skipping Sentry report {"runId":"38c77c7d-c8a3-4840-bb7c-efac1923a9e9"}
[08:43:23] WARN: run failure report has no task id, skipping Sentry report {"runId":"3a00ea68-d92f-4ac9-aa00-2a181dc76087"}

 Test Files  1 passed (1)
      Tests  44 passed (44)
   Start at  08:43:10
   Duration  21.71s (transform 5.32s, setup 38ms, import 8.47s, tests 13.11s, environment 0ms)
```

说明：在受限沙箱内首次用同一命令运行时，内嵌 PostgreSQL 的本地端口探测不可用，因此 Vitest 收集 44 条后全部跳过；该结果不用作候选结论。获准之后在沙箱外重跑的命令完全相同，未增加 flag、未设置环境开关、未使用临时脚本；最终实际执行 44 条且跳过数为 0。

落库断言结果：

| 字段 | 实际通过的精确断言 | 结果 |
|---|---|---|
| `heartbeat_runs.status` | `failed` | 通过 |
| `heartbeat_runs.exit_code` | `0` | 通过，退出码未被守卫改写 |
| `heartbeat_runs.error_code` | `adapter_failed` | 通过 |
| `heartbeat_runs.error` | `API error (attempt 7): RESOURCE_EXHAUSTED (code 429): Individual quota reached.` | 通过，保留 429 原文 |
| `result_json.errorFamily` | `provider_quota` | 通过 |

**判据 1 结果：通过。**

## 3. 判据 2：失败路径与非误杀

先运行结构化终响应的失败/非误杀用例：

```sh
pnpm exec vitest run server/src/services/adapter-final-response.test.ts
```

完整输出：

```text
[WARN] The "pnpm" field in package.json is no longer read by pnpm. The following keys were ignored: "pnpm.patchedDependencies", "pnpm.overrides". See https://pnpm.io/settings for the new home of each setting.
(!) Your Vite config uses features that are unsupported by `configLoader: 'native'`, which is planned to become the default in a future major version of Vite:
  - `__dirname` (vitest.config.ts:7:25). Use `import.meta.dirname` instead
Set `VITE_CONFIG_NATIVE_IGNORE_WARNING=true` to suppress this warning.

 RUN  v4.1.11 /home/yuanjian/dev/ANC

 Test Files  1 passed (1)
      Tests  9 passed (9)
   Start at  08:42:15
   Duration  171ms (transform 24ms, setup 48ms, import 16ms, tests 3ms, environment 0ms)
```

### 3.1 结构化 `SUCCESS` + exit 0

- 输入：`resultJson: { status: "SUCCESS", response: "done" }`。
- 实际断言：`readAdapterStructuredFinalFailure(...)` 返回 `null`，对应用例在上述 9/9 中通过。
- 接线结果：`heartbeat.ts` 的默认结局分支仅在 `exitCode === 0`、无 `errorMessage`、无 `signal`、无 process cancellation 失败且 `structuredFinalFailure === null` 时取 `outcome = "succeeded"`。此输入不被守卫误杀，落库路径仍为 `succeeded`。

结果：**通过**。

### 3.2 无结构化终响应 + exit 0

- 纯函数输入：`resultJson: { stdout: "…" }`；实际断言返回 `null`，对应用例通过。
- 生产形状核对：`server/src/adapters/process/execute.ts:89-104` 在 exit 0 时返回 `resultJson: { stdout, stderr }`，没有 `status` 或 `is_error`。
- 实际落库回归：现有 `heartbeat-runtime-skills.test.ts` 的测试适配器返回 exit 0 且不提供结构化终响应，然后通过 `heartbeat.invoke` 读回并断言 `status === "succeeded"`。

执行命令：

```sh
pnpm exec vitest run server/src/__tests__/heartbeat-runtime-skills.test.ts
```

完整输出：

```text
[WARN] The "pnpm" field in package.json is no longer read by pnpm. The following keys were ignored: "pnpm.patchedDependencies", "pnpm.overrides". See https://pnpm.io/settings for the new home of each setting.
(!) Your Vite config uses features that are unsupported by `configLoader: 'native'`, which is planned to become the default in a future major version of Vite:
  - `__dirname` (vitest.config.ts:7:25). Use `import.meta.dirname` instead
Set `VITE_CONFIG_NATIVE_IGNORE_WARNING=true` to suppress this warning.

 RUN  v4.1.11 /home/yuanjian/dev/ANC

[08:44:21] INFO: Loading external adapter package {"packageName":"/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local","packageDir":"/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local","entryPoint":"./dist/index.js","modulePath":"/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local/dist/index.js","hasUiParser":false}
[08:44:21] INFO: Loaded UI parser from adapter package {"packageName":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter","uiParserFile":"./dist/ui-parser.js","size":13146}
[08:44:21] INFO: Loading external adapter package {"packageName":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter","packageDir":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter","entryPoint":"./dist/index.js","modulePath":"/home/yuanjian/.paperclip/external-adapters/node_modules/@zhafron/paperclip-omp-adapter/dist/index.js","hasUiParser":true}
[08:44:21] INFO: Loaded external adapters from plugin store {"count":2,"adapters":["antigravity_local","omp_local"]}

 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  08:44:18
   Duration  18.31s (transform 5.02s, setup 41ms, import 8.07s, tests 10.09s, environment 0ms)
```

结果：**通过**。

**判据 2 结果：通过。**

## 4. 判据 3：默认接线（R3 / R4）

核对命令：

```sh
rg -n "readAdapterStructuredFinalFailure|structuredFinalFailure|feature|FEATURE|process\\.env|env\\." \
  server/src/services/heartbeat.ts server/src/services/adapter-final-response.ts
```

与本能力直接相关的实际命中为：

```text
server/src/services/adapter-final-response.ts:66:export function readAdapterStructuredFinalFailure(input: {
server/src/services/heartbeat.ts:324:import { readAdapterStructuredFinalFailure } from "./adapter-final-response.js";
server/src/services/heartbeat.ts:24294:        const structuredFinalFailure = readAdapterStructuredFinalFailure({
server/src/services/heartbeat.ts:24318:          !structuredFinalFailure
server/src/services/heartbeat.ts:24351:                    structuredFinalFailure?.message ??
server/src/services/heartbeat.ts:24372:          isProviderQuotaFailureMessage(structuredFinalFailure?.message)
```

接线断言：

1. 纯函数在 `heartbeat.ts` 中被直接静态 import。
2. 在默认运行结局计算中无条件调用，不依赖 feature flag、env 开关或试验性配置。
3. 其结果是 `succeeded` 分支的必要布尔条件，并同时驱动失败错误文本与 `provider_quota` 分类。
4. 整机用例确认它在默认 `heartbeat.invoke` 路径真正生效，不是只合入了死代码。

反事实断言：若故意关闭该能力（使 `structuredFinalFailure` 不参与 succeeded 条件），新接缝输入的其它成功条件全为真，运行会落为 `succeeded`，与用例的 `failed` 硬断言直接冲突；这种候选会被本 Gate 判定不放行。本席按禁令未临时改写共享工作树做变异。

**判据 3 结果：通过，已激活；非“未接线”。**

## 5. 判据 4：回归算术恒等与跳过统计

基线来自 `HEAD` 版本，未使用 stash：

```sh
git show HEAD:server/src/__tests__/heartbeat-retry-scheduling.test.ts | rg -o '\bit\s*\(' | wc -l
rg -o '\bit\s*\(' server/src/__tests__/heartbeat-retry-scheduling.test.ts | wc -l
git show HEAD:server/src/__tests__/heartbeat-retry-scheduling.test.ts | rg -o '\bit\.each\s*\(' | wc -l
rg -o '\bit\.each\s*\(' server/src/__tests__/heartbeat-retry-scheduling.test.ts | wc -l
```

实际输出：

```text
25
26
6
6
```

展开计算：

- `HEAD` 普通 `it(...)` 为 25 条。
- 6 个 `it.each(...)` 在当前与 `HEAD` 中完全相同，参数数量分别为 2、3、4、2、4、3，合计展开 18 条。
- 基线：25 + 18 = **43**。
- 新增非参数化接缝：**1**。
- 合并后：26 + 18 = **44**。
- 算术恒等：**43 + 1 = 44**。
- 完整整机运行实际结果：`44 passed (44)`，**skipped = 0**。

**判据 4 结果：通过。**

## 6. 判据 5：回滚候选

迁移核对命令：

```sh
git diff --name-status -- packages/db/src/schema packages/db/src/migrations packages/db/dist/migrations server/src/db server/src/migrations
```

实际输出：

```text
(无输出)
```

断言：本候选确实**没有 schema 或 migration 改动**，不是“没跑迁移”。

显式回滚命令（只针对候选 6 文件；本次只记录，未执行）：

```sh
git restore -- \
  server/src/services/heartbeat.ts \
  server/src/services/recovery/service.ts \
  server/src/__tests__/heartbeat-retry-scheduling.test.ts \
  server/src/services/recovery/provider-failure-classification.test.ts

rm -- \
  server/src/services/adapter-final-response.ts \
  server/src/services/adapter-final-response.test.ts
```

该命令不会触碰交接包明确排除的 CLI/UI 并发改动。

**判据 5 结果：通过。**

## 7. 验收汇总

| 验收点 | 断言结果 | 跳过数 | 结论 |
|---|---|---:|---|
| 候选指纹 | 3 项精确匹配，前后一致 | 0 | 通过 |
| 默认生产入口 | `heartbeat.invoke` 实跑；5 个落库字段精确断言通过 | 0 | 通过 |
| 失败/非误杀路径 | SUCCESS 不误杀；无结构化终响应的 `heartbeat.invoke` 仍 succeeded | 0 | 通过 |
| 接线 | 默认无条件激活，无 flag/env 门 | 0 | 通过 |
| 算术恒等 | 43 + 1 = 44；实跑 44/44 | 0 | 通过 |
| 回滚 | 无 schema/migration；6 文件可显式还原 | 0 | 通过 |

## 8. 明确总结论

**可放行。**

5 条判据全部通过，最终有效整机运行的跳过数为 0，未发现阻断项。本结论仅覆盖默认生产入口与本单指定的 6 个候选文件，不代替安全或运维 Gate。
