# TOK-213 共享组件返工报告

## 代码席 B1–B5 对照

| 项 | 修复 |
|---|---|
| B1 | `PageTabBar` 窄屏 `<option>` 从 ReactNode 文本或调用处显式 `mobileLabel` 取语义，不再退回原始 `value`；`Search.tsx` 为每个范围添加随结果变化的名称与计数，筛选排除的两项显示破折号。 |
| B2 | `CommandDialog` 默认标题、说明改用 `t()`，语言切换即时更新；显式 `title`、`description` 优先。 |
| B3 | `CommandPalette` 七处展开／搜索分组标题改为翻译键。 |
| B4 | `ProjectDetail`、`PluginSettings`、`Approvals`、`AgentDetail.production`、`ProjectWorkspaceDetail` 固定页签改为翻译键。插件动态标签仍由插件提供。 |
| B5 | `AgentChatPicker` 无匹配结果改用带 `{{search}}` 插值的翻译键。 |

新增 25 个英文事实键和简中对应译文；其余 38 份 locale 只由 `scripts/generate-i18n-locales.mjs` 重算。默认语言仍是英文。未改变服务端、共享类型、数据库或依赖。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 locale × 14,657 键一致。
- `pnpm check:token-gates`：四项 CLEAN。
- `pnpm --filter @paperclipai/ui exec vitest run src/components/PageTabBar.test.tsx src/components/ui/command.test.tsx src/components/AgentChatPicker.test.tsx src/components/CommandPalette.test.tsx src/pages/ProjectDetail.test.tsx src/pages/ProjectWorkspaceDetail.test.tsx src/pages/PluginSettings.test.tsx`：7 文件、36 项通过。
- `PATH=/home/yuanjian/.cargo/bin:$PATH pnpm -r typecheck`：35 个工作区项目通过。第一次未带既有 Rust PATH 停在 `cargo: not found`，修正后完整通过。
- `git diff --check`：通过。
- 默认隔离入口真实 `/ANC/search`：浏览器证据见 `交付/TOK-213/浏览器证据/共享组件返工-40e45927/证据索引.md`，中英双断点共八张截图，断言 4/4 通过；不证明其他动态页面状态已整机放行。

原运行基线 `2cce83439d079db84e3f2dc8964d0908d7fd4b65`、上轮 secrets 修复及其证据沿用，本轮不重复改动。回滚点为前候选 `40e45927f87c2f672958ce9a1c81207bb75a13e6`。未 push、未动 master、未装机。
