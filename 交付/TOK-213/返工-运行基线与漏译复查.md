# TOK-213 运行基线合入与漏译返工记录

## 候选与边界

- 上轮候选／回滚点：`8474464a3c766857d3e009e8ab48f09279bf25dd`。
- 本轮运行基线：`2cce83439d079db84e3f2dc8964d0908d7fd4b65`，候选中的合并提交 `0500fd22911c2f59962f82bd36b200af6d4e0c44` 以它为第二父提交。
- 本轮界面补译提交：`52747de38eb47a15cc41630130d2875c6400ae20`；最终交付 HEAD 以分支 work product 回读为准。
- 只改隔离分支 `feat/anc-zh-cn`，未 push、未动 master、未装机或改正式实例。

## 两处合并冲突

1. `pnpm-lock.yaml`：保留候选侧 `opencode-ai@1.18.32` 条目。`packages/paperclip-runner/package.json` 要求 1.18.32；运行基线侧冲突块是 1.18.29。锁文件解析项与清单保持一致。
2. `ui/src/components/agent-config-primitives.tsx`：保留候选侧通过 `t()` 实时解析的 `help` 代理。运行基线侧是静态英文对象，会使中文提示退回英文；其更新的 Fast mode 说明已在既有 `agentconfigprimitives.general.fastMode` 的英中键中覆盖。

合并后相对上轮候选，变动包含运行基线的 `packages/adapter-utils`、`packages/shared`、`server/src` 修复及历史交付记录；前端源代码另改 `ui/src/pages/Secrets.tsx` 和英中语言包。其余 38 份 locale 只由 `node scripts/generate-i18n-locales.mjs` 生成，未手工编辑。

## 七条点名漏译

| 键（均位于 `general`） | 新中文值 |
| --- | --- |
| `missingusersecretsbanner.youareresponsiblefor` | 由您负责 |
| `missingusersecretsbanner.novalueyetrunsthatrequire` | 尚未设置值。需要 |
| `missingusersecretsbanner.willfailuntilyousetyourvalue` | 的运行会失败，直到您设置自己的值。 |
| `myusersecretstab.thesearecredentialsonlyyouprovideeach` | 这些凭据仅由您提供。每个值都只属于您，仅在您是运行的负责用户时使用，且不会再次显示给任何人，包括管理员。 |
| `setmyusersecretdialog.thisvalueisyoursonlyitis` | 此值仅归您所有。当您负责的运行需要 |
| `usersecretdefinitionstab.eachmembersuppliesforthemselves` | 由每位成员自行提供 |
| `usersecretdefinitionstab.yousettheshapehereeveryuser` | 。您在此定义凭据结构；每位用户在“我的机密”中输入自己的值。覆盖情况仅显示已设置值的成员人数，不显示值本身。 |

这七条是旧的拆句键。当前四个 secrets 组件已改用完整句键；例如横幅使用 `oneusersecretmissing`／`multipleusersecretsmissing`，弹窗使用 `valueusedforrun`，页面介绍使用 `usersecretsintro`。本轮同时润色了实际调用的三条完整句键（横幅单数、复数及弹窗说明），确保插入数量或机密键后中文通顺。不能把旧拆句键的翻译当作浏览器已显示的证据。

## 同值清单复扫与新增补译

`zh-CN.json` 对 `en.json` 逐键比较：原同值 354 条，本轮改了上述七条及 `foldercontrols.general.the`（译“该”）、`systemnoticeuxlab.general.from`（译“来自 ”），剩余同值 345 条。剩余项逐项核过，包含模型／推理档位名、品牌与产品名（含 Smoke Lab）、协议与提供商字段名、URL／HTTP 方法、环境变量与命令、代码标识符、枚举字面量、示例数据、插值占位符和标点；另有 `user secret`、`has`、`have` 等旧拆句键，已确认当前组件不调用，不能算作当前界面已译或尚未译的显示证据。没有再发现**当前调用中的完整界面散文**与英文同值。

浏览器自验另外发现 `Secrets.tsx` 的视图按钮直接输出枚举值 `folders`／`flat`，静态同值扫描无法检出。本轮补了 `secrets.general.foldersview`／`flatview`，英文仍为 Folders／Flat，中文为“文件夹／平铺”；40 份 locale 键集由脚本重算。

## 验证与浏览器证据

- `node scripts/check-i18n-locales.mjs`：40 locale × 14,632 键完全对齐。
- `pnpm check:token-gates`：四项 CLEAN。
- 相关定向 Vitest：合并所涉 UI 四文件 108／108，secrets 两文件 11／11，Secrets 页面四文件 53／53。
- `pnpm --filter @paperclipai/ui typecheck`、`pnpm --filter @paperclipai/adapter-utils typecheck`、`pnpm --filter @paperclipai/server exec tsc --noEmit` 均通过；完整 `PATH=/home/yuanjian/.cargo/bin:$PATH pnpm -r typecheck` 通过。隔离 worktree 初次报旧共享类型，是其 `node_modules` 链接指向主工作树；仅在隔离 worktree 内重建这两处依赖链接后重跑通过，未改源码或主工作树。
- `git diff --check` 与候选工作树状态：干净。运行基线带来的四份历史日志文件有原有末尾空格，保留其证据原文；合并后的未提交差异检查为零。
- [浏览器证据索引](浏览器证据/返工-运行基线-机密页/证据索引.md)：真实隔离路由上桌面／窄屏的机密默认态和我的机密空态四张截图，均无页面错误或横向溢出。浏览器只验证本轮改动涉及的这些状态；其余全站 Gate 由原单整机放行席独立判断。

## 未解决的边界外发现

浏览器复查显示窄屏页签 `proposals` 和全局命令面板默认标题／说明仍为英文。对应文件是 `ui/src/components/PageTabBar.tsx` 与 `ui/src/components/ui/command.tsx`，超出本席获准的组件文件范围；本席未改、未声称全站无英文。须由开发部经理在 TOK-213 原单安排文件所有权和复核，不另建返工单。隔离数据库无机密定义，未取得缺失横幅和设置值弹窗的真实浏览器状态，不能宣称这些状态已经整机放行。
