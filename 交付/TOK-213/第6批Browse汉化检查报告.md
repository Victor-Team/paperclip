# TOK-213 第 6 批 Browse 汉化检查报告

- 提交：`82cc3480a`。
- 范围：连接器搜索、加载／空态、移除确认、连接状态、权限菜单及自定义 MCP 入口。
- 补充：移除确认中的动态计数、服务名与提供商插值改为运行期 i18n；动态值本身保持原值。
- 验证：Browse Vitest 8/8 通过；40 locale 共 3,201 键一致；Token Gate 四项 CLEAN；`git diff --check` 通过。

本切片仍是 37 文件 `pages/apps` 第 6 批的一部分，不构成目录或整机放行。
