# TOK-213 第 6 批：网关客户端对话框与列表检查报告

日期：2026-09-24

## 本切片范围

- `ui/src/pages/apps/gateways/ConnectClientDialog.tsx`
- `ui/src/pages/apps/gateways/GatewaysList.tsx`
- `ui/src/i18n/locales/{en,zh-CN}.json`，并由脚本同步其余 38 个 locale 的键集。

## 接线结果

- 客户端代码片段对话框的令牌签发、复制反馈、无障碍标签、帮助文本、端点与授权说明、空态和完成操作均通过 `t()` 读取。
- 网关列表的面包屑、状态 toast、无组织提示、搜索、表格与移动卡片标签、计数、空态和说明均通过 `t()` 读取。
- 网关名称、客户端代码片段标签、服务端错误文本、端点 URL、令牌内容、`Authorization: Bearer` 协议字段及访问配置摘要保持原值；没有将外部数据或协议字面量翻译。
- `ConnectClientDialog` 的 AST 复扫为 0 候选；`GatewaysList` 的 AST 复扫为 0 候选。

## 验证

| 检查 | 结果 |
| --- | --- |
| `node scripts/check-i18n-locales.mjs` | 通过；40 个 locale 各 3,886 键，键集与 `en.json` 一致。 |
| `pnpm --filter @paperclipai/ui exec vitest run src/pages/apps/gateways/ConnectClientDialog.test.tsx` | 通过；1/1。 |
| `pnpm check:token-gates` | 通过；四项 Gate 均为 CLEAN。 |
| `git diff --check` | 通过。 |
| `pnpm --filter @paperclipai/ui typecheck` | 未通过，但仅报既有 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`；本切片未新增诊断。 |

## 后续

网关目录仍有 `NewGatewayDialog.tsx`、活动、概览、高级和令牌面板的可接线文案；下一切片继续处理创建网关对话框。
