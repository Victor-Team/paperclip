# TOK-213 第 6 批：共享连接身份标签检查报告

## 本切片

- 范围：`ui/src/pages/apps/connection-identity.ts`、`Connections.tsx`、`app-detail/IdentitiesSection.tsx` 与对应语言包。
- 身份类型、授权状态、帐户回退名和受众摘要改由渲染端的 `t()` 解析，不再由共享辅助函数内置英文可见文案。
- 组织连接的持久化命名仍按凭据策略判断；`per_user_with_fallback` 延续原有的组织连接后缀语义，未受显示语言影响。
- 动态帐户/租户名、成员人数和状态值继续作为受控插值或原始运行时数据传递。

## 验证

- `connection-identity.test.ts` 与 `Connections.test.tsx`：17/17 通过。
- 40 个 locale 键集与 `en.json` 一致，共 4,147 键。
- `connection-identity.ts`、`Connections.tsx`、`IdentitiesSection.tsx` 的 AST 复扫均为 0 条候选。
- Token Gate 四项均为 CLEAN；`git diff --check` 通过。
- `pnpm --filter @paperclipai/ui typecheck` 仍仅报既有 `InstanceExperimentalSettings.test.tsx:71` 测试夹具缺少 `enableMcpAggregators`；本切片未新增类型诊断。

## 结论

共享身份标签已接入 i18n，中文翻译已写入 `zh-CN.json`，并由脚本同步到其余 38 个占位语言包。该切片可继续纳入 TOK-213 的第 6 批目录级收口。
