# TOK-213 放行记录

> 验收席位：开发部·整机放行席<br>
> 验收对象：`feat/anc-zh-cn` @ `7636364254bb219d7cf081fd93556c89d33f8cfd`<br>
> 基线：`cd672708f`<br>
> 日期：2026-09-18<br>
> 结论：**Search.tsx 试点放行；TOK-213 回开发部负责人继续第 2 批**

## 一、验收边界

- 本轮只验收 `Search.tsx + i18n 接线` 试点，不代替后续全站汉化验收。
- 本席没有修改代码或测试，没有套用候选分支未包含的 doctor 临时绕过。
- 第一次、第二次复验时，开发部·前端实现席留下的三张截图仅作为上游证据复核；本席没有把这些截图冒充为独立运行证据。第十节记录 TOK-163 正式修复后的独立动态复验。

## 二、失败路径（先验）

| Gate | 故意失败输入 / 步骤 | 实际输出 | 通过否 |
|---|---|---|---|
| 默认入口必须在未改候选上直接启动 | 在干净工作树执行 `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc --force`，不改候选代码 | doctor 返回 `11 passed, 1 failed`；失败项为 `Managed install manifest: Managed install artifacts exist but /home/yuanjian/.paperclip-anc/cli/install.json is missing`；继而输出 `Doctor found blocking issues. Not starting server.`，进程 exit 1 | **失败**；护栏确实拒绝了不符合 doctor 条件的候选，但也因此证明候选不能通过默认入口放行 |
| 语言键集必须一致 | 直接比较 `en.json` 与 `zh-CN.json` 的 scalar key path | 两者各 49 个键，`cmp` exit 0；`locale-validation` 另覆盖全部 locale | **通过** |

## 三、默认路径

| 验收点 | 命令 / 步骤 | 实际输出 | 通过否 |
|---|---|---|---|
| 候选身份与干净度 | `git rev-parse HEAD` / `git status --short --branch` | HEAD `0b9b0fb3a96c23b443271fac23431c4733143ca9`；验收开始时工作树干净 | 通过 |
| 默认入口启动隔离实例 | `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc --force` | doctor 阻断，服务未启动，exit 1 | **失败** |
| 中文路径 `/search?lng=zh-CN` | 要求默认入口起 3200 后独立访问 | 3200 未启动，无法从未改候选独立验证 | **未通过** |
| 英文阴性对照 `/search` | 不带 `?lng=` 访问 | 3200 未启动，无法独立验证 | **未通过** |
| i18n 测试 | `ui/node_modules/.bin/vitest run ui/src/i18n/locale-validation.test.ts` | `1 passed (1)`，`7 passed (7)`，无 skipped | 通过 |
| UI 类型检查 | `ui/node_modules/.bin/tsc --noEmit -p ui/tsconfig.json` | exit 0，无错误输出 | 通过 |
| UI token gate | `node scripts/check-token-gates.mjs` | 扫描 1027 文件；4 类 Gate 全为 `CLEAN`；`All gates clean.` | 通过 |
| diff 格式 | `git diff --check cd672708f..HEAD` | exit 0，无输出 | 通过 |
| 正式实例未受影响 | 只读 GET `http://127.0.0.1:3100/api/health` | HTTP 000，当时 3100 未在监听；本轮没有启停或修改正式实例 | 不适用；无法证明“运行中实例未受影响”，但本轮未对其执行任何变更 |

## 四、译文随机抽查（10 条）

抽样命令：`awk '/^\\| [0-9]+ \\|/{print}' 交付/第3步/汉化进度台账.md | shuf -n 10`。

| # | 英文 / 键 | 候选中文 | 核对结果 |
|---|---|---|---|
| 30 | `Updating…` / `search.general.updating` | 更新中… | 通过 |
| 11 | `Identifier lookup:` / `search.general.identifierLookup` | 标识符查找： | 通过 |
| 26 | `Create task from this query` / `search.general.createTaskFromThis` | 根据此查询创建任务 | 通过；`task` 与术语表“任务”一致 |
| 25 | `Search all scopes` / `search.general.searchAllScopes` | 搜索全部范围 | 通过 |
| 5 | `Try` / `search.general.try` | 尝试搜索 | 通过；返工 commit `763636425` 已按 `/home/yuanjian/dev/ANC/交付/第2步/词表-修订-批2.md` 第 76、944 行的已验收译文更正，台账也已更正取值来源 |
| 13 | `PAP-123` | 保留原文 | 通过；标识符格式示例，非自然语言 |
| 29 | `Wrap multi-word phrases in quotes.` / `search.general.wrapMultiWordPhrasesIn` | 将多词短语用引号括起来。 | 通过 |
| 22 | `Searching for “{{query}}”…` / `search.general.searchingFor` | 正在搜索“{{query}}”… | 通过；占位符同名保留 |
| 1 | `Search` / `search.general.search` | 搜索 | 通过 |
| 9 | `Tasks, comments, plan documents, artifacts, agents, projects — same surface, ranked by relevance.` | 任务、评论、方案文档、产物、智能体、项目 — 同一界面，按相关度排序。 | 通过；`task/agent/project/organization` 的相关用词与术语表一致 |

抽查结果：初验 9 通过、1 不通过；返工 commit `763636425` 后复验为 **10 通过、0 不通过**。

## 五、接线断言

| 断言 | 证据 | 结果 |
|---|---|---|
| 默认语言仍为英文 | `initialLocale()` 的候选不在 `supportedLocales` 时回到 `DEFAULT_LOCALE`；无保存值时也使用 `DEFAULT_LOCALE` | 静态接线通过；运行断言未能执行 |
| `?lng=zh-CN` 保留区域码 | `supportedLocales.includes(candidate)` 做原值精确匹配，没有小写化或截断区域码 | 通过 |
| 保存的语言值可被读取 | `window.localStorage.getItem("paperclip.locale")` | 读取接线存在；本轮按派单未要求可见切换器或写入接线 |
| 40 个 locale 键集一致 | `locale-validation.test.ts` 7 个用例全过；`en`/`zh-CN` 两组 scalar key 直接比各 49 个 | 通过 |
| 候选在默认生产入口激活了汉化能力 | doctor 在 server 启动前就拒绝候选；只有上游“临时改四行后再撤销”的截图证据 | **不通过** |

## 六、截图复核

| 证据 | 观察 | 判定 |
|---|---|---|
| `交付/第3步/截图/01-search-zh-CN-初始态.png` | Search.tsx 范围内的页签、标题、说明与提示为中文；未见截断、重叠、按钮撑破或异常换行。共用侧栏仍为英文，属本轮明示豁免范围 | 布局复核通过；非本席独立运行证据 |
| `交付/第3步/截图/02-search-zh-CN-零结果态.png` | 中文空态标题和按钮没有溢出；`Sort: Relevance` 与筛选器仍为英文，来自本轮豁免的子组件/lib | 布局复核通过；范围豁免需留待后续批次 |
| `交付/第3步/截图/03-search-en-阴性对照.png` | 页面主体是英文，布局正常 | 上游阴性对照形式合格；仍不替代本席默认入口独立运行 |

## 七、跳过统计

- i18n 目标测试：7 通过，0 失败，**0 跳过**。
- UI 类型检查：完整执行，0 跳过。
- token gate：4 类 Gate 完整执行，0 跳过。
- 浏览器中文、英文动态路径：因默认入口被 doctor 拒绝而**未执行**，这不记为跳过后的通过，而是直接构成不放行理由。

## 八、最终结论与退回对象

**不放行，退回开发部·前端实现席。**

必须在原单上修正后重新交验：

1. 让干净候选不需临时修改源码，即可通过本单指定的默认入口启动隔离实例；否则本席无法进行中文/英文动态复验。
2. 重交时保持工作树干净，提供新 commit SHA；整机放行席将重跑默认入口、`?lng=zh-CN` 和无 `?lng=` 的英文阴性对照。

## 九、返工复验（2026-09-18）

| 复验项 | 实际结果 | 结论 |
|---|---|---|
| 返工边界 | `git diff --name-only aa846c2f0..763636425` 只有 `ui/src/i18n/locales/zh-CN.json` 和 `交付/第3步/汉化进度台账.md`；`git diff --check` 通过 | 通过 |
| 译文与台账 | `jq -r '.search.general.try'` 输出“尝试搜索”；台账已注明批 2 第 76、944 行为来源 | 通过 |
| i18n 测试 | `1 passed (1)`，`7 passed (7)`，0 skipped | 通过 |
| UI 类型检查 | `ui/node_modules/.bin/tsc --noEmit -p ui/tsconfig.json` exit 0 | 通过 |
| 未覆盖 `HOME` 的原始默认入口 | 在 `763636425` 上重跑，仍是 doctor `11 passed, 1 failed`，`Managed install manifest` 失败，`Not starting server`，exit 1 | **失败** |
| `HOME=/tmp/paperclip-doctor-home-scratch` 建议 | 该覆盖专门改变 doctor 对 `shimPath` 的判定环境；虽然不改候选文件，但它不是候选的默认配置，且正在绕过本次失败 Gate | **不执行，不认作默认路径证据** |

复验后三选一结论：**不放行，退回开发部负责人。** 译文返工已通过；剩余唯一失败是未改候选在指定默认入口上无法启动，需由负责人按其评论 `4efb03ca` 第二节上报 TOK-163 排期或提供不需特殊环境覆盖的可验证候选。

## 十、TOK-163 解阻后的最终复验（2026-09-18）

### 10.1 验证候选与边界

- TOK-163 的 doctor 修复提交为 `9fcdf5c44`，记录提交为 `c3f760a7d`。本席用一次性 detached worktree 从 `c3f760a7d` 建立已修复基线，再机械应用 Search 试点代码提交 `e519f8053` 和译文修正提交 `763636425`。
- `763636425` 同时修改过本分支台账；该台账在已修复基线上不存在，因此 cherry-pick 只发生一处 modify/delete 冲突。本席在一次性 worktree 保留基线的“无台账”状态，最终集成候选为 `f522d6f245a0ad9083ea49efcd4eafe9d28bc9e6`，候选差异只包含 Search/i18n 代码与既有同步脚本。
- 为复用主仓已安装依赖，本席只在一次性 worktree 建立 `node_modules` 目录链接；没有安装依赖、修改源码、锁文件、现有分支或运行配置。验证结束后已停止服务并删除一次性 worktree。

### 10.2 失败路径（先验）

| Gate | 故意失败输入 | 实际输出 | 结论 |
|---|---|---|---|
| 语言码必须精确匹配 | 浏览器访问 `/ANC/search?lng=zh-cn`（错误的小写区域码） | 渲染后的 `<html lang="en">`；页面标题、搜索框、页签与说明均保持英文，例如 `Search`、`All`、`Type to search organization memory.` | **通过**；错误语言码没有被误接受为 `zh-CN` |
| locale 键集与内容约束 | `pnpm exec vitest run src/i18n/locale-validation.test.ts` | `1 passed`，`7 passed`，0 failed，0 skipped | **通过** |

### 10.3 默认入口与正常路径

| 验收点 | 命令 / 步骤 | 实际输出 | 结论 |
|---|---|---|---|
| 默认入口 doctor | `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc`；没有 `--force`，没有 `HOME=` 或其他环境覆盖 | doctor `12 passed`，`All checks passed!`；服务监听 `127.0.0.1:3200`，隔离数据库监听 `54339` | **通过** |
| 中文路径 | 浏览器访问 `/ANC/search?lng=zh-CN` | Search.tsx 范围内的标题、输入提示、页签、说明、空态与按钮显示中文 | **通过** |
| 中文查询态 | 在中文页面输入“测试” | 中文零结果态、操作按钮与辅助说明正常渲染 | **通过** |
| 英文阴性对照 | 浏览器访问 `/ANC/search` | 页面回到英文；标题、输入提示、页签与说明均为英文 | **通过** |
| 浏览器错误 | 已提交截图脚本监听 `console:error`、`pageerror` 与 HTTP `>=400` | 脚本只输出中文、英文最终 URL，没有输出控制台错误、页面异常或 `>=400` 响应 | **通过** |
| UI 类型检查 | `pnpm exec tsc --noEmit -p tsconfig.json` | exit 0，无错误输出 | **通过** |
| diff 格式与干净度 | `git diff --check c3f760a7d..HEAD`；健康接口报告候选 local changes | diff 检查 exit 0；`hasLocalChanges=false`，三类变更计数均为 0 | **通过** |

### 10.4 排版与截图

独立截图位于 `交付/TOK-213/最终复验/`：

| 文件 | SHA-256 | 人工检查 |
|---|---|---|
| `01-search-zh-initial.png` | `ccb4fbee667ae2d26610021bc7c4978b7cc969fa5d4d24998f9124beaa73209d` | 1440×900；未见文字截断、重叠、按钮撑破或异常换行 |
| `02-search-zh-query.png` | `6654753144399ad190cec6b639eb0056ae26ad8981e8bf6288b6f8f43830d0b1` | 1440×900；中文零结果态布局正常，操作区无溢出 |
| `03-search-en-control.png` | `0a8d32402ad8d1e35ffa22c3820e70c9a4310a640e1d9c92e32bc17762802845` | 1440×900；英文对照布局正常 |

共用侧栏及本轮明示豁免的子组件仍显示英文，不计为 Search.tsx 试点失败，留待后续全站批次处理。

### 10.5 正式实例隔离与清理

- 动态复验前后，正式实例 `http://127.0.0.1:3100/api/health` 均返回 HTTP 200，`processStartedAt` 始终为 `2026-09-18T15:04:34.127Z`，正式数据库 `54329` 持续监听。
- 复验期间，隔离实例使用 `3200/54339`；停止后 `3200` 返回 HTTP 000，`3200/54339` 均不再监听，而 `3100/54329` 仍正常。
- 一次性验证 worktree 已删除；既有 `feat/anc-zh-cn` 工作树未被代码改动。

### 10.6 跳过统计与最终结论

- doctor：12 通过，0 失败，**0 跳过**。
- i18n：7 通过，0 失败，**0 跳过**。
- UI 类型检查、diff 检查、失败语言码、中文初始页、中文查询态、英文对照、正式实例隔离：全部执行，**0 跳过**。

最终三选一结论：**Search.tsx 试点放行。** 本结论仅关闭第 1 批验收，不表示 TOK-213 全站汉化完成；本单应回开发部负责人继续派第 2 批（语言切换器 UI 与下一目录），状态保持 `in_progress`。
