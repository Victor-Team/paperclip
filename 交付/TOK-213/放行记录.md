# TOK-213 放行记录

> 验收席位：开发部·整机放行席<br>
> 验收对象：`feat/anc-zh-cn` @ `8ea176971`（第 2 批集成候选 `c05cf3ff2`）<br>
> 基线：`cd672708f`（集成基线 `c3f760a7d`）<br>
> 日期：2026-09-19<br>
> 结论：**第 2 批（语言切换器 UI + 持久化）放行；TOK-213 回开发部负责人继续第 3 批**

## 一、验收边界

- 本轮只验收 `Search.tsx + i18n 接线` 试点，不代替后续全站汉化验收。
- 本席没有修改代码或测试，没有套用候选分支未包含的 doctor 临时绕过。
- 第一次、第二次复验时，开发部·前端实现席留下的三张截图仅作为上游证据复核；本席没有把这些截图冒充为独立运行证据。第十节记录 TOK-163 正式修复后的独立动态复验。

## 二、失败路径（先验）

| Gate | 故意失败输入 / 步骤 | 实际输出 | 通过否 |
|---|---|---|---|
| 默认入口必须在未改候选上直接启动 | 在干净工作树执行 `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc --force`，不改候选代码 | doctor 返回 `11 passed, 1 failed`；失败项为 `Managed install manifest: Managed install artifacts exist but /home/yuanjian/.paperclip-anc/cli/install.json is missing`；继而输出 `Doctor found blocking issues. Not starting server.`，进程 exit 1 | **失败**；护栏确实拒绝了不符合 doctor 条件的候选，但也因此证明候选不能通过默认入口放行 |
| 语言键集必须一致 | 直接比较 `en.json` 与 `zh-CN.json` 的 scalar key path | 两者各 49 个键，`cmp` exit 0；`locale-validation` 另覆盖全部 locale | **通过** |

## 三、默认路径

| 验收点 | 命令 / 步骤 | 实际输出 | 通过否 |
|---|---|---|---|
| 候选身份与干净度 | `git rev-parse HEAD` / `git status --short --branch` | HEAD `0b9b0fb3a96c23b443271fac23431c4733143ca9`；验收开始时工作树干净 | 通过 |
| 默认入口启动隔离实例 | `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc --force` | doctor 阻断，服务未启动，exit 1 | **失败** |
| 中文路径 `/search?lng=zh-CN` | 要求默认入口起 3200 后独立访问 | 3200 未启动，无法从未改候选独立验证 | **未通过** |
| 英文阴性对照 `/search` | 不带 `?lng=` 访问 | 3200 未启动，无法独立验证 | **未通过** |
| i18n 测试 | `ui/node_modules/.bin/vitest run ui/src/i18n/locale-validation.test.ts` | `1 passed (1)`，`7 passed (7)`，无 skipped | 通过 |
| UI 类型检查 | `ui/node_modules/.bin/tsc --noEmit -p ui/tsconfig.json` | exit 0，无错误输出 | 通过 |
| UI token gate | `node scripts/check-token-gates.mjs` | 扫描 1027 文件；4 类 Gate 全为 `CLEAN`；`All gates clean.` | 通过 |
| diff 格式 | `git diff --check cd672708f..HEAD` | exit 0，无输出 | 通过 |
| 正式实例未受影响 | 只读 GET `http://127.0.0.1:3100/api/health` | HTTP 000，当时 3100 未在监听；本轮没有启停或修改正式实例 | 不适用；无法证明“运行中实例未受影响”，但本轮未对其执行任何变更 |

## 四、译文随机抽查（10 条）

抽样命令：`awk '/^\\| [0-9]+ \\|/{print}' 交付/第3步/汉化进度台账.md | shuf -n 10`。

| # | 英文 / 键 | 候选中文 | 核对结果 |
|---|---|---|---|
| 30 | `Updating…` / `search.general.updating` | 更新中… | 通过 |
| 11 | `Identifier lookup:` / `search.general.identifierLookup` | 标识符查找： | 通过 |
| 26 | `Create task from this query` / `search.general.createTaskFromThis` | 根据此查询创建任务 | 通过；`task` 与术语表“任务”一致 |
| 25 | `Search all scopes` / `search.general.searchAllScopes` | 搜索全部范围 | 通过 |
| 5 | `Try` / `search.general.try` | 尝试搜索 | 通过；返工 commit `763636425` 已按 `/home/yuanjian/dev/ANC/交付/第2步/词表-修订-批2.md` 第 76、944 行的已验收译文更正，台账也已更正取值来源 |
| 13 | `PAP-123` | 保留原文 | 通过；标识符格式示例，非自然语言 |
| 29 | `Wrap multi-word phrases in quotes.` / `search.general.wrapMultiWordPhrasesIn` | 将多词短语用引号括起来。 | 通过 |
| 22 | `Searching for “{{query}}”…` / `search.general.searchingFor` | 正在搜索“{{query}}”… | 通过；占位符同名保留 |
| 1 | `Search` / `search.general.search` | 搜索 | 通过 |
| 9 | `Tasks, comments, plan documents, artifacts, agents, projects — same surface, ranked by relevance.` | 任务、评论、方案文档、产物、智能体、项目 — 同一界面，按相关度排序。 | 通过；`task/agent/project/organization` 的相关用词与术语表一致 |

抽查结果：初验 9 通过、1 不通过；返工 commit `763636425` 后复验为 **10 通过、0 不通过**。

## 五、接线断言

| 断言 | 证据 | 结果 |
|---|---|---|
| 默认语言仍为英文 | `initialLocale()` 的候选不在 `supportedLocales` 时回到 `DEFAULT_LOCALE`；无保存值时也使用 `DEFAULT_LOCALE` | 静态接线通过；运行断言未能执行 |
| `?lng=zh-CN` 保留区域码 | `supportedLocales.includes(candidate)` 做原值精确匹配，没有小写化或截断区域码 | 通过 |
| 保存的语言值可被读取 | `window.localStorage.getItem("paperclip.locale")` | 读取接线存在；本轮按派单未要求可见切换器或写入接线 |
| 40 个 locale 键集一致 | `locale-validation.test.ts` 7 个用例全过；`en`/`zh-CN` 两组 scalar key 直接比各 49 个 | 通过 |
| 候选在默认生产入口激活了汉化能力 | doctor 在 server 启动前就拒绝候选；只有上游“临时改四行后再撤销”的截图证据 | **不通过** |

## 六、截图复核

| 证据 | 观察 | 判定 |
|---|---|---|
| `交付/第3步/截图/01-search-zh-CN-初始态.png` | Search.tsx 范围内的页签、标题、说明与提示为中文；未见截断、重叠、按钮撑破或异常换行。共用侧栏仍为英文，属本轮明示豁免范围 | 布局复核通过；非本席独立运行证据 |
| `交付/第3步/截图/02-search-zh-CN-零结果态.png` | 中文空态标题和按钮没有溢出；`Sort: Relevance` 与筛选器仍为英文，来自本轮豁免的子组件/lib | 布局复核通过；范围豁免需留待后续批次 |
| `交付/第3步/截图/03-search-en-阴性对照.png` | 页面主体是英文，布局正常 | 上游阴性对照形式合格；仍不替代本席默认入口独立运行 |

## 七、跳过统计

- i18n 目标测试：7 通过，0 失败，**0 跳过**。
- UI 类型检查：完整执行，0 跳过。
- token gate：4 类 Gate 完整执行，0 跳过。
- 浏览器中文、英文动态路径：因默认入口被 doctor 拒绝而**未执行**，这不记为跳过后的通过，而是直接构成不放行理由。

## 八、最终结论与退回对象

**不放行，退回开发部·前端实现席。**

必须在原单上修正后重新交验：

1. 让干净候选不需临时修改源码，即可通过本单指定的默认入口启动隔离实例；否则本席无法进行中文/英文动态复验。
2. 重交时保持工作树干净，提供新 commit SHA；整机放行席将重跑默认入口、`?lng=zh-CN` 和无 `?lng=` 的英文阴性对照。

## 九、返工复验（2026-09-18）

| 复验项 | 实际结果 | 结论 |
|---|---|---|
| 返工边界 | `git diff --name-only aa846c2f0..763636425` 只有 `ui/src/i18n/locales/zh-CN.json` 和 `交付/第3步/汉化进度台账.md`；`git diff --check` 通过 | 通过 |
| 译文与台账 | `jq -r '.search.general.try'` 输出“尝试搜索”；台账已注明批 2 第 76、944 行为来源 | 通过 |
| i18n 测试 | `1 passed (1)`，`7 passed (7)`，0 skipped | 通过 |
| UI 类型检查 | `ui/node_modules/.bin/tsc --noEmit -p ui/tsconfig.json` exit 0 | 通过 |
| 未覆盖 `HOME` 的原始默认入口 | 在 `763636425` 上重跑，仍是 doctor `11 passed, 1 failed`，`Managed install manifest` 失败，`Not starting server`，exit 1 | **失败** |
| `HOME=/tmp/paperclip-doctor-home-scratch` 建议 | 该覆盖专门改变 doctor 对 `shimPath` 的判定环境；虽然不改候选文件，但它不是候选的默认配置，且正在绕过本次失败 Gate | **不执行，不认作默认路径证据** |

复验后三选一结论：**不放行，退回开发部负责人。** 译文返工已通过；剩余唯一失败是未改候选在指定默认入口上无法启动，需由负责人按其评论 `4efb03ca` 第二节上报 TOK-163 排期或提供不需特殊环境覆盖的可验证候选。

## 十、TOK-163 解阻后的最终复验（2026-09-18）

### 10.1 验证候选与边界

- TOK-163 的 doctor 修复提交为 `9fcdf5c44`，记录提交为 `c3f760a7d`。本席用一次性 detached worktree 从 `c3f760a7d` 建立已修复基线，再机械应用 Search 试点代码提交 `e519f8053` 和译文修正提交 `763636425`。
- `763636425` 同时修改过本分支台账；该台账在已修复基线上不存在，因此 cherry-pick 只发生一处 modify/delete 冲突。本席在一次性 worktree 保留基线的“无台账”状态，最终集成候选为 `f522d6f245a0ad9083ea49efcd4eafe9d28bc9e6`，候选差异只包含 Search/i18n 代码与既有同步脚本。
- 为复用主仓已安装依赖，本席只在一次性 worktree 建立 `node_modules` 目录链接；没有安装依赖、修改源码、锁文件、现有分支或运行配置。验证结束后已停止服务并删除一次性 worktree。

### 10.2 失败路径（先验）

| Gate | 故意失败输入 | 实际输出 | 结论 |
|---|---|---|---|
| 语言码必须精确匹配 | 浏览器访问 `/ANC/search?lng=zh-cn`（错误的小写区域码） | 渲染后的 `<html lang="en">`；页面标题、搜索框、页签与说明均保持英文，例如 `Search`、`All`、`Type to search organization memory.` | **通过**；错误语言码没有被误接受为 `zh-CN` |
| locale 键集与内容约束 | `pnpm exec vitest run src/i18n/locale-validation.test.ts` | `1 passed`，`7 passed`，0 failed，0 skipped | **通过** |

### 10.3 默认入口与正常路径

| 验收点 | 命令 / 步骤 | 实际输出 | 结论 |
|---|---|---|---|
| 默认入口 doctor | `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc`；没有 `--force`，没有 `HOME=` 或其他环境覆盖 | doctor `12 passed`，`All checks passed!`；服务监听 `127.0.0.1:3200`，隔离数据库监听 `54339` | **通过** |
| 中文路径 | 浏览器访问 `/ANC/search?lng=zh-CN` | Search.tsx 范围内的标题、输入提示、页签、说明、空态与按钮显示中文 | **通过** |
| 中文查询态 | 在中文页面输入“测试” | 中文零结果态、操作按钮与辅助说明正常渲染 | **通过** |
| 英文阴性对照 | 浏览器访问 `/ANC/search` | 页面回到英文；标题、输入提示、页签与说明均为英文 | **通过** |
| 浏览器错误 | 已提交截图脚本监听 `console:error`、`pageerror` 与 HTTP `>=400` | 脚本只输出中文、英文最终 URL，没有输出控制台错误、页面异常或 `>=400` 响应 | **通过** |
| UI 类型检查 | `pnpm exec tsc --noEmit -p tsconfig.json` | exit 0，无错误输出 | **通过** |
| diff 格式与干净度 | `git diff --check c3f760a7d..HEAD`；健康接口报告候选 local changes | diff 检查 exit 0；`hasLocalChanges=false`，三类变更计数均为 0 | **通过** |

### 10.4 排版与截图

独立截图位于 `交付/TOK-213/最终复验/`：

| 文件 | SHA-256 | 人工检查 |
|---|---|---|
| `01-search-zh-initial.png` | `ccb4fbee667ae2d26610021bc7c4978b7cc969fa5d4d24998f9124beaa73209d` | 1440×900；未见文字截断、重叠、按钮撑破或异常换行 |
| `02-search-zh-query.png` | `6654753144399ad190cec6b639eb0056ae26ad8981e8bf6288b6f8f43830d0b1` | 1440×900；中文零结果态布局正常，操作区无溢出 |
| `03-search-en-control.png` | `0a8d32402ad8d1e35ffa22c3820e70c9a4310a640e1d9c92e32bc17762802845` | 1440×900；英文对照布局正常 |

共用侧栏及本轮明示豁免的子组件仍显示英文，不计为 Search.tsx 试点失败，留待后续全站批次处理。

### 10.5 正式实例隔离与清理

- 动态复验前后，正式实例 `http://127.0.0.1:3100/api/health` 均返回 HTTP 200，`processStartedAt` 始终为 `2026-09-18T15:04:34.127Z`，正式数据库 `54329` 持续监听。
- 复验期间，隔离实例使用 `3200/54339`；停止后 `3200` 返回 HTTP 000，`3200/54339` 均不再监听，而 `3100/54329` 仍正常。
- 一次性验证 worktree 已删除；既有 `feat/anc-zh-cn` 工作树未被代码改动。

### 10.6 跳过统计与最终结论

- doctor：12 通过，0 失败，**0 跳过**。
- i18n：7 通过，0 失败，**0 跳过**。
- UI 类型检查、diff 检查、失败语言码、中文初始页、中文查询态、英文对照、正式实例隔离：全部执行，**0 跳过**。

最终三选一结论：**Search.tsx 试点放行。** 本结论仅关闭第 1 批验收，不表示 TOK-213 全站汉化完成；本单应回开发部负责人继续派第 2 批（语言切换器 UI 与下一目录），状态保持 `in_progress`。

## 十一、第 2 批语言切换器 UI 与持久化复验（2026-09-19）

### 11.1 验证候选与集成边界

- 验证基线：`/home/yuanjian/dev/ANC` 上已包含 TOK-163 修复的提交 `c3f760a7d`。
- 集成 UI 提交：从候选分支 `feat/anc-zh-cn`（HEAD `8ea176971`）依序 cherry-pick 四个 UI 提交：
  1. `e519f8053`：Search 页面接入 i18n
  2. `763636425`：修正 `search.general.try`（DU 冲突中丢弃台账，仅保留 UI 译文修改）
  3. `fe6db4c70`：语言切换器（LocaleContext + LanguageToggle，存入 `paperclip.locale`）
  4. `9a2084e4d`：Gate 2 修复（`max-w-32` 替换 `max-w-[8rem]`）+ Storybook 补丁
  集成候选提交 SHA 为 `c05cf3ff2`，工作树完全干净。
- 依赖复用：在临时 worktree 建立 `node_modules` 软链接，未重新安装依赖或修改任何运行配置。验证结束后已完整删除临时 worktree 与分支。

### 11.2 失败路径（先验）

| Gate / 失败项 | 故意失败输入 / 测试方法 | 实际输出 | 判定 |
|---|---|---|---|
| Token Gate 2（任意方括号值） | 在旧代码 `fe6db4c70`（含 `max-w-[8rem]`）上执行 `node scripts/check-token-gates.mjs` | 准确拦截：`Gate 2 (arbitrary bracket vals): 1 violation(s)`，指出 `ui/src/components/LanguageToggle.tsx:66 max-w-[8rem]` | **通过**；护栏代码强制拒绝违规 |
| Token Gate 2 修复后断言 | 在新提交 `9a2084e4d`（改为 `max-w-32`）上执行 `node scripts/check-token-gates.mjs` | `Files scanned: 1028`；Gate 1/2/3/4 全部 `CLEAN`；`All gates clean.` | **通过** |
| 语言码精确匹配（大小写敏感） | 浏览器访问 `/ANC/search?lng=zh-cn`（错误的小写区域码） | `<html lang="en">`；页面保持英文（`Tasks`、`Search`），未被误识别为 `zh-CN` | **通过** |
| 未知/不支持的语言代码 | 浏览器访问 `/ANC/search?lng=invalid-code` | `<html lang="en">`；页面保持英文，平稳回退默认语言 | **通过** |
| 损坏/非法的 localStorage 偏好 | 写入 `localStorage["paperclip.locale"] = "unsupported-lang"` 后刷新 `/ANC/search` | `<html lang="en">`；页面保持英文，平稳回退默认语言 | **通过** |

### 11.3 默认生产入口与正常路径

| 验收点 | 命令 / 步骤 | 实际输出 | 判定 |
|---|---|---|---|
| 默认入口启动 | `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc`（零 `--force`、零特殊环境变量） | doctor `12 passed` / `All checks passed!`；3200 服务正常监听，隔离 DB 端口 54340 | **通过** |
| 判据 1：默认语言为英文 | 首次访问 `/ANC/search`（无存储偏好） | `<html lang="en">`，`localStorage` 为 `null`，URL 无 `lng=`，搜索页签为 `Tasks` | **通过** |
| 判据 2：切换器可见性与选项 | 点击打开账号菜单 | 切换器可见，选项严格为 `["English", "简体中文"]`，其余 38 个占位语言未列入 | **通过** |
| 判据 3：UI 切中文立即生效 | 菜单内选择“简体中文”（UI 操作，无刷新） | `<html lang="zh-CN">`，`localStorage["paperclip.locale"] = "zh-CN"`，页签立即变“任务”，URL 无 `lng=` | **通过** |
| 判据 4：刷新后中文持久化 | 页面执行 `reload`（URL 无 `lng=`） | `<html lang="zh-CN">`，`localStorage` 保持 `"zh-CN"`，页签保持“任务” | **通过** |
| 判据 5：同上下文新页面继承 | 浏览器同一上下文中新建标签页打开 `/ANC/search` | `<html lang="zh-CN">`，存储仍为 `"zh-CN"`，正常继承用户选择 | **通过** |
| 判据 6：切回英文并刷新 | 账号菜单中切回“English”并刷新 | `<html lang="en">`，`localStorage["paperclip.locale"] = "en"`，页签恢复为 `Tasks` | **通过** |
| 全流程 URL 无污染 | 检查上述全过程 URL | 全程未出现 `?lng=` 参数，完全由 UI 与 localStorage 驱动 | **通过** |
| 控制台与网络无异常 | 监听 Search 页面切换过程中的 `console:error`、`pageerror` 与 HTTP `>=400` | 输出 `problems: none`，0 错误响应 | **通过** |

### 11.4 变体与挂载点核验

- 设计指南页 `/ANC/design-guide`：三种形态（`icon`、`menu-action`、`compact-menu-action`）全部正常渲染，原生 `<select>` 选项与交互正常，未见重叠、溢出或截断。
- 登录页挂载点：`local_trusted` 模式下 `/auth` 自动重定向至 dashboard；按开发部负责人 03:13 裁定（评论 `ddfdb3c3`），接受 `Auth.test.tsx` 渲染覆盖与 ThemeToggle 并排同构证据。
- 生产双胞胎：`SidebarAccountMenu.production.tsx` 与开发版共用 `LanguageToggle` 组件；按负责人 03:13 裁定，接受现有同源测试与设计指南页视觉证据。
- Storybook 补丁：提交 `9a2084e4d` 已按负责人批准应用 `storybook-LocaleProvider.patch`。

### 11.5 自动化测试与代码质量

- 目标测试集：`vitest run ui/src/i18n ui/src/context ui/src/components/LanguageToggle ui/src/components/SidebarAccountMenu ui/src/pages/Auth`：13 个测试文件、156 条测试全部通过，0 失败，0 跳过。
- 语言包校验：`vitest run ui/src/i18n/locale-validation.test.ts`：7 条断言全过，0 跳过。
- 类型检查：`tsc --noEmit -p ui/tsconfig.json`：退出码 0，无任何类型错误。
- token-gates：`node scripts/check-token-gates.mjs`：扫描 1028 个文件，Gate 1/2/3/4 全部 CLEAN。
- diff 格式：`git diff --check c3f760a7d..HEAD`：退出码 0，格式规范。
- 译文抽查：10 条随机抽样核对（含新增 `common.language.*`）与术语表及批 2 定稿 100% 一致。

### 11.6 截图证据

独立截图落盘于 `交付/TOK-213/批次2复验/`：

| 文件 | 说明 |
|---|---|
| `fail-01-casing-zh-cn-fallback-en.png` | 失败路径：小写 `zh-cn` 回退英文 |
| `fail-02-unknown-code-fallback-en.png` | 失败路径：未知代码回退英文 |
| `fail-03-corrupted-storage-fallback-en.png` | 失败路径：异常存储回退英文 |
| `01-search-en-default.png` | 默认初始英文态 |
| `02-account-menu-en-open.png` | 英文账号菜单展开，切换器可见 |
| `03-search-zh-after-switch-no-reload.png` | UI 选择简体中文后即时变中文（未刷新） |
| `04-search-zh-after-reload.png` | 刷新后中文持久化态 |
| `04b-search-zh-new-page.png` | 新标签页自动继承中文态 |
| `05-account-menu-zh-open.png` | 中文账号菜单展开态 |
| `06-search-en-after-reload.png` | 切回英文并刷新后恢复态 |
| `07-variants-design-guide-en.png` | 设计指南页三变体英文态 |
| `08-variants-design-guide-zh.png` | 设计指南页三变体中文态 |
| `09-auth-page-resolved.png` | 登录路由跳转后态 |

### 11.7 正式实例隔离与清理

- 正式实例 `http://127.0.0.1:3100/api/health` 前后状态保持 `ok`，`processStartedAt` 始终为 `2026-09-19T01:26:08.237Z`，生产数据库端口 `54329` 未受任何影响。
- 隔离实例监听端口 `3200`、`13200`、`54340` 在停止后已全部释放。
- 临时 worktree `/tmp/tok213-b2-verify` 及分支 `tok213-b2-verify` 已完全清理，主工作树与 `feat/anc-zh-cn` 保持干净。

### 11.8 跳过统计与最终放行结论

- 跳过统计：所有测试、构建、Gate、失败路径与正常路径全量执行，**跳过数为 0**。
- 三选一结论：**第 2 批（语言切换器 UI + 持久化）放行。**
- 下一步处置：本结论仅放行第 2 批，不代表 TOK-213 全站汉化完成；工单保持 `in_progress`，改派给开发部负责人（`agent://e49c1f9a-94cb-44b8-a80b-93ad76a725f2`），由负责人继续派发第 3 批（下一目录页面汉化）。

## 十二、第 3 批 Dashboard.tsx 与全站多语言复验（2026-09-19）

### 12.1 验证候选与集成边界

- 验证基线：`/home/yuanjian/dev/ANC` 上已包含 TOK-163 修复的提交 `c3f760a7d`。
- 集成 UI 提交：从候选分支 `feat/anc-zh-cn`（HEAD `03d02d16c`）依序 cherry-pick 前序通过与本批 UI 提交：
  1. `e519f8053`：Search 页面接入 i18n
  2. `763636425`：修正 `search.general.try`（DU 冲突中丢弃台账，仅保留 UI 译文修改）
  3. `fe6db4c70`：语言切换器（LocaleContext + LanguageToggle）
  4. `9a2084e4d`：Token Gate 2 修复（`max-w-32`）+ Storybook 补丁
  5. `218678a94`：第 3 批 Dashboard.tsx 接入 t() 并补 dashboard.general 键（40 个语言包各 +36 键）
  6. `b59d9dd15`：检出 `ui/src/pages/Dashboard.i18n.test.ts` 进行集成测试断言
  集成候选最终提交 SHA 为 `c01605648`，工作树完全干净。
- 依赖复用：在临时 worktree 建立 `node_modules` 软链接，未重新安装依赖或修改任何运行配置。验证结束后已完整删除临时 worktree 与分支。

### 12.2 失败路径（先验）

| Gate / 失败项 | 故意失败输入 / 测试方法 | 实际输出 | 判定 |
|---|---|---|---|
| Token Gate 2（任意方括号值） | 在 `ui/src/pages/Dashboard.tsx` 故意引入 `w-[123px]` 执行 `node scripts/check-token-gates.mjs` | 准确拦截：`Gate 2 (arbitrary bracket vals): 10 violation(s)`，列出违规行号并 exit 1 | **通过**；护栏代码强制拒绝违规 |
| Token Gate 2 恢复后断言 | 撤销故意违规后执行 `node scripts/check-token-gates.mjs` | `Files scanned: 1029`；Gate 1/2/3/4 全部 `CLEAN`；`All gates clean.` | **通过** |
| 语言码精确匹配（大小写敏感） | 浏览器访问 `/ANC/dashboard?lng=zh-cn`（错误的小写区域码） | `<html lang="en">`；页面保持英文，未被误识别为 `zh-CN` | **通过** |
| 未知/不支持的语言代码 | 浏览器访问 `/ANC/dashboard?lng=invalid-code` | `<html lang="en">`；页面保持英文，平稳回退默认语言 | **通过** |
| 损坏/非法的 localStorage 偏好 | 写入 `localStorage["paperclip.locale"] = "unsupported-lang"` 后刷新 `/ANC/dashboard` | `<html lang="en">`；页面保持英文，平稳回退默认语言 | **通过** |

### 12.3 默认生产入口与正常路径

| 验收点 | 命令 / 步骤 | 实际输出 | 判定 |
|---|---|---|---|
| 默认入口启动 | `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc`（零 `--force`、零特殊环境变量） | doctor `12 passed` / `All checks passed!`；3200 服务正常监听，隔离 DB 端口 54339 | **通过** |
| 判据 1：默认语言为英文 | 首次访问 `/ANC/dashboard`（无存储偏好） | `<html lang="en">`，`localStorage` 为 `null`，标题与卡片为英文 | **通过** |
| 判据 2：切换器可见性与选项 | 点击打开账号菜单（`aria-label="Open account menu"`） | 切换器可见，选项严格为 `["English", "简体中文"]`，其余 38 个占位语言未列入 | **通过** |
| 判据 3：UI 切中文立即生效 | 菜单内选择“简体中文”（UI 操作，无刷新） | `<html lang="zh-CN">`，`localStorage["paperclip.locale"] = "zh-CN"`，仪表盘卡片即时呈现“已启用智能体”、“进行中的任务”、“本月支出”、“最近活动”等中文，URL 无 `lng=` 参数 | **通过** |
| 判据 4：刷新后中文持久化 | 页面执行 `reload`（URL 无 `lng=`） | `<html lang="zh-CN">`，`localStorage` 保持 `"zh-CN"`，仪表盘中文保持不变 | **通过** |
| 判据 5：同上下文新页面继承 | 浏览器同一上下文中新建标签页打开 `/ANC/dashboard` | `<html lang="zh-CN">`，存储仍为 `"zh-CN"`，正常继承用户选择 | **通过** |
| 判据 6：切回英文并刷新 | 账号菜单中切回“English”并刷新 | `<html lang="en">`，`localStorage["paperclip.locale"] = "en"`，仪表盘恢复为英文 | **通过** |
| 全流程 URL 无污染 | 检查上述全过程 URL | 全程未出现 `?lng=` 参数，完全由 UI 与 localStorage 驱动 | **通过** |
| 控制台与网络无异常 | 监听 Dashboard 页面切换过程中的 `console:error`、`pageerror` 与 HTTP `>=400` | 输出 `problems: none`，0 错误响应 | **通过** |

### 12.4 移动端断点排版核验（390×844）

- 移动端中文态（`/ANC/dashboard?lng=zh-CN`）：渲染 `<html lang="zh-CN">`，移动视口下各统计卡片、任务图表、最近活动单列流式布局正常，未见文字截断、按钮撑破或异常溢出。
- 移动端英文态（`/ANC/dashboard`）：渲染 `<html lang="en">`，布局与中文态保持一致。

### 12.5 自动化测试与代码质量

- 单元测试：`vitest run src/i18n src/context src/components/LanguageToggle src/pages/Dashboard.i18n.test.ts`：12 个测试文件、149 条测试全部通过，0 失败，0 跳过。
- 专项测试：`vitest run src/pages/Dashboard.i18n.test.ts`：3 条断言（所有键存在、插值替换正确、英文基准完备）全过，0 跳过。
- 语言包校验：`vitest run src/i18n/locale-validation.test.ts`：7 条断言全过，0 跳过。
- 类型检查：`tsc --noEmit -p tsconfig.json`：退出码 0，无任何类型错误。
- token-gates：`node scripts/check-token-gates.mjs`：扫描 1029 个文件，Gate 1/2/3/4 全部 CLEAN。
- 词表与术语一致性：已核实开发部负责人裁定点（`Review agents` 对应“查看智能体”，`errors` 对应“{{errors}} 个出错”），与整句翻译及术语规范一致。

### 12.6 截图证据

独立复验截图落盘于 `交付/TOK-213/批次3复验/`：

| 文件 | 说明 |
|---|---|
| `fail-01-casing-zh-cn-fallback-en.png` | 失败路径：小写 `zh-cn` 回退英文 |
| `fail-02-unknown-code-fallback-en.png` | 失败路径：未知代码回退英文 |
| `fail-03-corrupted-storage-fallback-en.png` | 失败路径：异常存储回退英文 |
| `01-dashboard-en-default.png` | 默认初始英文态（桌面 1440×900） |
| `02-account-menu-en-open.png` | 英文账号菜单展开，切换器可见 |
| `03-dashboard-zh-after-switch-no-reload.png` | UI 选择简体中文后即时变中文（未刷新） |
| `04-dashboard-zh-after-reload.png` | 刷新后中文持久化态 |
| `04b-dashboard-zh-new-page.png` | 新标签页自动继承中文态 |
| `05-account-menu-zh-open.png` | 中文账号菜单展开态 |
| `06-dashboard-en-after-reload.png` | 切回英文并刷新后恢复态 |
| `07-dashboard-zh-mobile.png` | 移动端中文排版态（390×844） |
| `08-dashboard-en-mobile.png` | 移动端英文排版态（390×844） |

### 12.7 正式实例隔离与清理

- 正式实例 `http://127.0.0.1:3100/api/health` 动态复验全程保持 `ok`，`processStartedAt` 始终为 `2026-09-19T07:23:27.352Z`，生产数据库端口 `54329` 未受任何影响。
- 隔离实例临时创建的测试智能体已在验证结束后彻底删除（恢复为 0 智能体初始态）。
- 隔离实例监听端口 `3200`、`54339` 已由 hub 正常停止并全部释放。
- 临时 worktree `/tmp/tok213-batch3-verify` 及分支 `tok213-batch3-verify` 已完全删除，主工作树与 `feat/anc-zh-cn` 保持干净。

### 12.8 跳过统计与最终放行结论

- 跳过统计：所有测试、构建、Gate、失败路径与正常路径全量执行，**跳过数为 0**。
- 三选一结论：**第 3 批（Dashboard.tsx 汉化与多语言接线）放行通过。**
- 下一步处置：本结论仅放行第 3 批，不代表 TOK-213 全站汉化完成；工单保持 `in_progress`，改派给开发部负责人（`agent://e49c1f9a-94cb-44b8-a80b-93ad76a725f2`），由负责人继续派发第 4 批（下一目录/页面汉化）。
