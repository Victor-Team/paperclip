# TOK-213 第31—35批工具目录浏览器证据索引

采集日期：2026-09-25。执行席：开发部·前端实现席。候选分支：`feat/anc-zh-cn`。隔离实例：`http://127.0.0.1:3200`、内嵌 PostgreSQL 54339；服务健康响应显示提交 `2efb1d81b`，本次前端返工由 Vite 热更新加载。浏览器：本机 Google Chrome，经 Playwright 实际渲染；桌面 1366×768，窄屏 390×844。中文通过界面语言选择或持久化偏好启用，`html[lang]` 为 `zh-CN`。截图是本工作树隔离环境的数据状态，不是正式实例证据。

## 页面 × 状态 × 断点

| 页面及状态 | 桌面截图 | 窄屏截图 | 视觉核查 | 与设计稿一致 |
|---|---|---|---|---|
| 访问配置：空列表、起始模板 | [桌面](桌面-profiles.png) | [窄屏](窄屏-profiles.png) | 中文主文案清晰，无横向溢出；窄屏卡片纵向排布 | 未核实：未取得对应设计稿；按现行布局自查无错位 |
| 访问配置：检查权限抽屉 | [桌面](桌面-profiles-检查权限展开.png) | 未采集 | 标题、说明、选择器空态为中文 | 未核实：未取得设计稿 |
| 访问配置：只读模板进入向导 | [桌面](桌面-profiles-向导展开.png) | 未采集 | 三步标题、模板与表单标签为中文 | 未核实：未取得设计稿 |
| 网关：空列表 | [桌面](桌面-gateways.png) | [窄屏](窄屏-gateways.png) | 中文空态和按钮正常；无横向溢出 | 未核实：未取得设计稿；现行布局自查无错位 |
| 网关：新建对话框 | [桌面](桌面-gateways-新建展开.png) | 未采集 | 字段、缺少访问配置提示及取消操作为中文；未提交创建 | 未核实：未取得设计稿 |
| 粘贴配置：初始表单 | [桌面](桌面-paste-config.png) | [窄屏](窄屏-paste-config.png) | 中文说明与按钮可见；代码示例保持原值；无横向溢出 | 未核实：未取得设计稿；现行布局自查无错位 |
| 粘贴配置：帮助弹窗返工后 | [桌面](复验-桌面-paste-config-帮助展开.png) | [窄屏](复验-窄屏-paste-config-帮助展开.png) | 三条操作说明已译中文；窄屏弹窗可纵向滚动；可复制的原始英文提示词仍在文本框内 | 未核实：未取得设计稿；提示词是否也需翻译待裁定 |
| Smoke Lab：服务及矩阵 | [桌面](桌面-smoke-lab.png) | [修正前窄屏](窄屏-smoke-lab.png)、[修正后左侧](复验-窄屏-smoke-lab-矩阵局部左.png)、[修正后右侧](复验-窄屏-smoke-lab-矩阵局部右.png) | 原窄屏列头逐字竖排；返工后矩阵 806px，在 356px 容器内横向滚动，列头保持横排。服务标题仍是服务器返回的英文 | 未核实：未取得设计稿；横向滚动需独立视觉复核 |
| 语言入口与持久化 | [英文菜单](桌面-语言切换菜单.png)、[简中刷新后](复验-桌面-语言切换刷新保持.png) | 未采集 | 菜单选择从 `en` 切换到 `zh-CN`；刷新后选择器、`html[lang]` 和 `localStorage[paperclip.locale]` 均为 `zh-CN` | 未核实：未取得设计稿 |

原始浏览器采集结果见 `浏览器采集记录.json`；展开态与语言记录见 `展开与语言切换记录.json`；返工后的可见文字与滚动尺寸见 `修正后复验记录.json`。`桌面-paste-config-帮助展开.png` 为返工前漏译证据；带“复验”前缀的是返工后证据。语言切换的第一张刷新截图在页面数据加载完成前拍下，已剔除，以“复验-桌面-语言切换刷新保持.png”为准。

## 仍需处理或复核

1. 第31—35批的真实浏览器证据已补齐所列入口，但**不能据此认定整个工具目录或全站汉化完成**：当前隔离数据中访问配置和网关为空，未覆盖有数据列表、校验错误、运行记录及所有展开状态。
2. 桌面应用次级侧栏仍有 `Browse`、`Review`、`DEVELOPER`、`Advanced setup for developers.`；这些文案不在本席独占的 `pages/**` 源文件内，需按并行文件边界由对应席处理。`Smoke Lab` 为现用产品名；其服务卡 `Fake OAuth 2.0 provider`、`HTTP MCP fixture` 等由服务端返回，需在第36批做已知服务标识到译文的前端映射或说明豁免。可复制提示词属于发送给智能体的原始请求内容，仍为英文；是否把显示与复制内容一起本地化需明确口径。
3. 窄屏 Smoke Lab 矩阵已能横向阅读，但缺设计稿对照和独立席位复核；不能由本席自验放行。
4. 图标资源 `/api/assets/2b9bc29c-97ce-4ba6-b2eb-8619cc453517/content` 在隔离克隆中返回 404；界面使用文字回退，不影响本次文字截图。没有把它写成资源不存在。
5. 默认隔离入口 `pnpm paperclipai run` 仍因隔离 home 缺 `cli/install.json` 在 doctor 阶段退出。浏览器取证使用仓库文档中的 `pnpm dev:once`，并显式指向既有 `PAPERCLIP_RUNNER_BINARY`；本次证明了隔离浏览器界面，不证明默认入口已修复。
