# TOK-213 第45批 Telegram 真实路由补证索引

取证日期：2026-09-25。候选分支 `feat/anc-zh-cn`，取证前 HEAD `6efc0ba0a`。隔离实例为 `127.0.0.1:3200`／内嵌 PostgreSQL 54339，使用 `pnpm dev:once` 启动；取证后已关闭。未访问正式实例 3100／54329。

为进入真实 Telegram 建连向导第 2 步，本轮在隔离数据库为“开发部·前端实现席”创建一条**未配置**的草稿端点 `ea1d2b09-042b-4f24-932c-0e3ce859c5fa`。只触发本地创建，不输入机器人令牌、不开启外部连接。之后用 `resume` 参数加载同一端点的实际路由。草稿留在隔离数据库中，供后续复验定位；不代表生产配置。

| 状态 | 视口 | 文件 | 实测 |
|---|---:|---|---|
| Telegram 第 2 步，简中 | 1366×768 | `desktop-zh-step2.png` | `html[lang]=zh-CN`；指令中文；`/newbot`、`bot`、`/task@bot_username <request>` 均保持原文；无横向溢出 |
| 同上 | 390×844 | `narrow-zh-step2.png` | 三项命令保持原文，段落自然换行；无横向溢出或目视重叠 |
| Telegram 第 2 步，英文对照 | 1366×768 | `desktop-en-step2.png` | `html[lang]=en`，说明英文，命令原文一致；无横向溢出 |

三次路由响应 200、页面脚本异常 0。无语言参数的新浏览器会话额外验证 `html[lang]=en` 和英文说明，未单独截图。桌面两次观察到侧栏公司标识资源 `/api/assets/2b9bc29c-97ce-4ba6-b2eb-8619cc453517/content` 返回 404，已在 `browser-result.json` 单列；不能把整页称为零网络异常。页面未与设计稿对照。

本次只覆盖 Telegram 第 2 步指令及布局。未配置机器人、未进入连接成功／失败态；未验证邮件收件箱动态态、语言切换入口刷新持久化或全站布局。截图不能替代独立席位复核。
