# TOK-213 共享建连向导页脚中文补证

- 候选提交：后端实现席 `1bffe6da539074248c1d3651d730e3be55355fd9`，基于第39批 `c225aaa7c`。本席未修改 `ui/src/components/**` 源码。
- 取证日期：2026-09-25。隔离 worktree 的 `pnpm dev:once` 在 127.0.0.1:3200／embedded PostgreSQL 54339 启动，runner 指向已有二进制。访问真实路由 `/TOK/apps/chat/connect?provider=github&purpose=chat` 的第1步，未创建端点。取证后隔离服务已停止。

| 截图 | 断点 | 页脚按钮 | 排版与异常 | 与设计稿一致否 |
|---|---:|---|---|---|
| `desktop-zh.png` | 1366×768 | `保存并退出` 可见，唯一匹配按钮 | 无横向溢出、按钮重叠或撑破；侧栏公司标识资源 404，显示破图 | 未核实，无对应设计稿 |
| `narrow-zh.png` | 390×844 | `保存并退出` 可见，唯一匹配按钮 | 无横向溢出、按钮重叠或撑破；本次未记录失败请求 | 未核实，无对应设计稿 |
| `desktop-en.png` | 1366×768 | `Save & exit` 可见，唯一匹配按钮 | 无横向溢出；同一侧栏公司标识资源 404 | 未核实，无对应设计稿 |

`browser-result.json` 记录每次 HTTP 页面状态、按钮匹配数、横向溢出、页面异常及失败响应。三次页面返回 200，页面异常均为 0；桌面两次失败响应均为 `/api/assets/2b9bc29c-97ce-4ba6-b2eb-8619cc453517/content` 的 404，属于页面公司标识资源，不能归为无异常整页验收。中文切换通过预置 `paperclip.locale`，未验证切换入口操作或刷新持久化。

本次只补第38批所报的共享页脚按钮，不代替 GitHub 向导步骤2—8、Slack 向导、有数据与失败态、设计稿对照或独立席位复核。默认入口 `pnpm paperclipai run` 的隔离 doctor 缺 `cli/install.json` 问题仍按原单记录，本次取证不证明默认入口修复。
