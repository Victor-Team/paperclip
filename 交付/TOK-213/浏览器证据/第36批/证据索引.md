# TOK-213 第36批浏览器证据索引

采集日期：2026-09-25。执行席：开发部·前端实现席。候选分支：`feat/anc-zh-cn`，代码提交 `37b99bb12`。隔离实例为 `127.0.0.1:3200` 和内嵌 PostgreSQL `54339`，使用工作树既有配置与 `pnpm dev:once` 启动，取证后已关闭。浏览器为本机 Chrome，由 Playwright 驱动；截图尺寸为 1366×768、390×844。`?lng=zh-CN` 使页面以简中渲染。

| 页面与状态 | 桌面 | 窄屏 | 观察结果 | 与设计稿一致 |
|---|---|---|---|---|
| Smoke Lab 测试服务卡：模拟 OAuth 已运行，HTTP MCP 已停止 | [截图](桌面-smoke-lab-服务卡.png) | [截图](窄屏-smoke-lab-服务卡.png) | 两个服务名及固定 OAuth 说明显示中文；原英文固定说明未出现在页面；两个断点的 `documentElement.scrollWidth` 等于 `clientWidth`，无页面横向溢出。窄屏矩阵沿用既有内部横向滚动。 | 未核实：未取得对应设计稿；自查文字未重叠、按钮未撑破、卡片未截断 |
| 访问配置向导：默认配置替换、智能体已有配置、分配失败 | 未采集 | 未采集 | 语言切换后的 React 定向测试覆盖替换说明和已有配置摘要；失败消息经语言包和类型检查。隔离数据不足以覆盖这些真实浏览器状态。 | 未核实 |
| 旧版访问配置：允许列表来源数量 | 未采集 | 未采集 | i18next 对英／简中计数插值实测正确；隔离数据中没有可供截图的列表记录。 | 未核实 |

原始 DOM 判定、断点尺寸与网络异常见 [浏览器采集记录](浏览器采集记录.json)。桌面有一条隔离克隆图标资源请求返回 404，窄屏无该请求；这不证明资源在正式实例不存在，也不影响本批服务卡文字判定。共用侧栏仍有英文，属于并行席源码范围。服务端返回的运行记录与错误详情保持原文，尚未逐项复核。第36批截图不能代替整个工具目录或全站的整机放行与独立复核。
