# TOK-213 N1 搜索结果状态图标证据

- 受测源码提交：`587e6e6f217c1fd6b198552cc205329fcf7bf0ae`。
- 启动：隔离实例 `pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc`，仅监听 127.0.0.1:3200，隔离数据库端口 54339。`/api/health` 回报 `2026.916.0+466.git.587e6e6f2`。
- 路由：真实 `/ANC/search?lng={en|zh-CN}&q=test`。浏览器只拦截本页搜索 GET 响应，把已有测试议题的 `status` 设为 `in_review`、`externalConversationState` 设为 `waiting`；未改数据库，也未模拟整机真实外部会话流程。
- 原始断言见 `browser-result.json`，可复跑脚本见 `browser-proof.cjs`。四组均为 HTTP 200、页面异常 0、横向溢出 0；图标保持空闲圆形，`aria-label` 与 SVG `title` 跟随语言显示 `Idle`／`空闲`，4/4 通过。
- 截图里的英文议题标题与描述是隔离测试库的原始用户内容，按原文呈现；`status:todo` 等示例是搜索语法字面量。

| 语言 | 断点 | 截图 | 标签与设计检查 |
|---|---:|---|---|
| 英文 | 1366×768 | `desktop-en.png` | `Idle`；无截断、重叠、按钮撑破或换行错位 |
| 简中 | 1366×768 | `desktop-zh.png` | `空闲`；无截断、重叠、按钮撑破或换行错位 |
| 英文 | 390×844 | `narrow-en.png` | `Idle`；无截断、重叠、按钮撑破或换行错位 |
| 简中 | 390×844 | `narrow-zh.png` | `空闲`；无截断、重叠、按钮撑破或换行错位 |

辅助标签不直接显示在截图中，须对照 `browser-result.json` 的 `label`、`title` 和 `glyphClass` 字段。此包证明受控搜索响应下的真实界面渲染，不替代整机放行席的默认入口及真实外部会话复验。
