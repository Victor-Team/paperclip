# TOK-213 运行基线返工：机密页浏览器证据

- 源码候选：`52747de38eb47a15cc41630130d2875c6400ae20`；隔离工作树 `feat/anc-zh-cn`。
- 启动入口：`pnpm paperclipai run --data-dir /home/yuanjian/.paperclip-anc --instance anc`；doctor 通过；仅访问隔离实例 `127.0.0.1:3200`，数据库端口 `54339`。取证后已停止，两端口均无监听。
- 实际路由：`/ANC/company/settings/secrets?lng=zh-CN`。隔离数据中的公司前缀是 `ANC`；`/TOK/...` 负控显示公司未找到，未用其冒充通过证据。
- `browser-result.json` 记录每张截图的路由、视口、文本摘录、页面错误及横向溢出。

| 页面／状态 | 断点 | 截图 | 观察结果 | 与设计稿一致 |
| --- | --- | --- | --- | --- |
| 机密默认态 | 1366×768 | `1366-default.png` | 视图按钮“文件夹／平铺”均为中文；无重叠或横向溢出 | 未核实：本单无对应设计稿 |
| 我的机密空态 | 1366×768 | `1366-my-secrets.png` | 说明和空状态为中文；无重叠或横向溢出 | 未核实：本单无对应设计稿 |
| 机密默认态 | 390×844 | `390-default.png` | 视图按钮“文件夹／平铺”均为中文；窄屏无撑破、截断或横向溢出 | 未核实：本单无对应设计稿 |
| 我的机密空态 | 390×844 | `390-my-secrets.png` | 说明和空状态为中文；窄屏换行完整，横向溢出 0 | 未核实：本单无对应设计稿 |

四项浏览器 `pageerror` 和非 2xx 响应均为 0。隔离库无用户机密定义，无法从该数据进入缺失机密横幅和设置值弹窗；对应译文由语言包核对及定向组件测试验证，本组截图不证明这两种非空数据状态。真实机密值从未输入或展示。

**发现并移交：**390 宽度的公司机密页下拉页签把 `proposals` 原样显示，源头是共享组件 `ui/src/components/PageTabBar.tsx` 对 ReactNode 标签回退到 `item.value`；默认页面还暴露 `ui/src/components/ui/command.tsx` 的英文默认标题与描述。本次席位边界只授权 `agent-config-primitives.tsx` 这一组件冲突文件，不改上述两个共享组件。请开发部经理在原单安排对应文件的实施与复核，不能把这两处当作已汉化。
