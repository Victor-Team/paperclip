# TOK-213 R1–R4 搜索界面返工报告

## 候选与范围

- 上轮源码候选：`6515db263cc67dc5100f73c5696debd7397377a5`；整机证据提交后的返工起点：`ad1da7c193486f87a4c14b335b27b5974105aa48`，可作为本轮回滚点。
- 本轮实现提交：`57528c1d0024be5657b5ee5404143cf599aa65ee`。最终证据提交和分支 HEAD 以交付评论为准；证据提交不再改实现源码。
- 运行基线：`/home/yuanjian/.paperclip/cli/current` 指向 `installs/git/2cce83439d07`；`git merge-base --is-ancestor 2cce83439d079db84e3f2dc8964d0908d7fd4b65 <候选 HEAD>` 返回 0。
- 仅改隔离分支 `feat/anc-zh-cn`；未 push、改 master、装机、部署或操作正式实例。默认语言仍是英文。

## 按整机退回项核对

| 项 | 处理与受影响文件 |
|---|---|
| R1 排序英文 | `ui/src/lib/search-filters.ts` 将排序显示名改由 locale 查键；`Search.tsx`、`SearchSortMenu.tsx`、`SearchFilterSheet.tsx` 均读取同一函数。桌面菜单与窄屏抽屉均显示「相关性／最近更新／最新创建／优先级」，英文仍显示既有名称。 |
| R2 来源英文 | `SearchResultRow.tsx` 按已知结果字段映射 Comment、Document、Artifact、Description、Agent、Project 等来源；真实结果见「描述」，其余类型以定向测试核对。未知服务端来源保留原始值，作为无法预译的数据。 |
| R3 筛选值英文 | `search-filters.ts` 与 `SearchFilterBar.tsx` 将状态、优先级、更新时间、chip 及空结果恢复建议共用 locale 名称；`StatusIcon.tsx` 增可选只读显示名覆盖，使搜索状态图标的可见文本及辅助文本同步为中文，不改变原有编辑控件默认行为。 |
| R4 相对时间英文 | `SearchResultRow.tsx` 为简中使用 `Intl.RelativeTimeFormat`，英文保留原有 `m/h/d/w/mo/y` 简写；分钟、天、月等边界由定向测试覆盖。 |

`en.json` 与 `zh-CN.json` 新增 42 个分层键；其余 38 份 locale 由 `node scripts/generate-i18n-locales.mjs` 重算，未逐份手工编辑。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 份 locale × 14,699 键完全对齐。
- `pnpm check:token-gates`：四项 CLEAN。
- UI 类型检查与 `PATH=/home/yuanjian/.cargo/bin:$PATH pnpm -r typecheck`：通过。
- 定向 Vitest：6 文件、62/62 通过。覆盖搜索筛选、结果来源和相对时间，以及既有搜索页与语言键校验。
- `git diff --check` 与暂存差异格式检查：通过。
- 浏览器证据：[`浏览器证据/搜索结果汉化-R1-R4/证据索引.md`](浏览器证据/搜索结果汉化-R1-R4/证据索引.md)。隔离 3200 路由的桌面／390×844 简中与英文四组取证均通过，错误 0、横向溢出 0。隔离测试数据的标题和描述保留英文；这不是界面漏译。

本席自验和隔离路由截图不替代独立代码复核与整机默认入口放行。真实公司导入执行仍未验证，本轮未触及该流程。
