# TOK-213 四动作默认入口整机取证

- 实现候选：`d7f452b8fa86e8daa5c1cd166a4e2f1fdbcffe88`；取证前 HEAD `2960b07f0d5d86c5bb86f1d0f607a05142ad1996`，后者仅增加此前的放行记录和截图。
- 服务启动：隔离工作树内，以干净进程环境执行无额外参数的 `pnpm paperclipai run`；doctor 为 12/12，通过后 `http://127.0.0.1:3200/api/health` 返回版本 `2026.916.0+434.git.2960b07f0`。
- 浏览器取证：复用仓库已跟踪且未改动的 `交付/TOK-213/浏览器证据/整机返工-路由与四动作/browser-proof.mjs`，SHA-256 `1db6026e19d92747c86ce6999c2cbd7382f7c188609f84aee2c81c99c2219d7a`；脚本从本次运行的专属临时工作目录执行，结果复制于此。
- **原始 JSON 的 `entry` 字段被脚本写死为 `pnpm dev:once`，与本次实际启动方式不符。** 保留原始值以便审计；本次真实入口以以上命令、doctor 和 health 记录为准。
- 浏览器访问真实 `/TOK/company/import?lng=zh-CN`；只拦截成功的 `GET /api/companies/import/jobs/browser-proof`，响应形状为 `{job:{id,status:'succeeded',importResult}}`。四行动作为 `created / renamed / replaced / skipped`。桌面与窄屏截图显示「已创建／已重命名／已替换／已跳过」及重命名连接词「为」。
- 两个视口的导入 POST 均为 0，横向溢出均为 0，目标页面错误均为空。**真实导入执行未验证。**
- 截图：`desktop-import-four-actions.png`（1366×768）；`narrow-import-four-actions.png`（390×844）。
