# TOK-194 复核记录 · 做单 TOK-193

- 复核对象：TOK-193（`4b0aa56d-ccab-4a55-94a6-da0eeb64fd24`），做席＝开发部·软件架构席，状态 `done`
- 复核方式：只读核对，不改任何文件
- 结论：**放行（通过）**，附一处待办（见「待办」节）

## 项 1：`交付/TOK-185/接口证据.md` §期望3

| 判据 | 结果 | 证据 |
|---|---|---|
| 原第174-185行一字未动 | 通过（非穷尽，无字节级基线——`交付/` 未被 git 跟踪，无法做逐字节 diff；用内容一致性判定：原文仍保留「因此这是**配置层的过期钉选**」「`gemini-3.5-flash` 这个串**全仓库不存在**」等原判定原话，未见改写痕迹，且做席自述已用 diff 校验改前副本） | 文件 170-185 行 |
| 追加更正块含：日志绝对路径＋第1行原文 | 通过 | 187-193行引用路径 `/home/yuanjian/.paperclip/instances/default/data/run-logs/6f94c8ef-aced-48f0-8ec3-e5a14fe0aa9e/1429075e-021f-4b81-a52b-8e7e8078c1a5/4010d65f-8f35-4dc1-9a51-b15c1d6b3cfd.ndjson`；本席 `sed -n '1p'` 亲自复跑，输出与引用逐字一致：`configured model changed from "gemini-3.8-flash-high" to "gemini-3.5-flash"; effective run configuration changed: adapter config, model profile.` |
| 含 CEO 取证文件路径 | 通过 | 197行引用 `/home/yuanjian/paperclip-run/词元共振/部门档案/00-战略部/交付/TOK-190/路由裁定-前提核验.md`；本席 `ls -la` 复核该文件存在（7809 字节，2026-09-16 08:44） |
| 三态措辞严格：`adapterConfig.model` 无该串＝不存在（穷尽）；注入源头＝按查证结果标注，不许把「查不到」写成「不存在」 | 通过 | 三态更正表（209-213行）：①TOK-179 执行席钉选＝**不存在（穷尽）**；②原文「全仓库不存在」被降级标注为「原文这句**级别用错**：当时只能算未找到（非穷尽）」；③注入源头本轮**已找到（实查，path:line）**——`/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local/dist/index.js:16-25`，本席 `grep` 复跑确认 `models[.. ] "gemini-3.5-flash"`、`modelProfiles[0].adapterConfig.model === "gemini-3.5-flash"` 均在该文件命中；服务端接口 `GET /api/companies/{id}/adapters/antigravity_local/model-profiles` 复跑返回同一条，`GET .../models` 复跑确认列表无该值。「找到注入点」与「找到？还有没有其他注入路径」被分别标注（后者交付评论里列为「未找到（非穷尽）」），未见把「未查」写成「不存在」的违规 |

**结论：项1 全部判据满足，通过。**

## 项 2：TOK-189（`1fca0264-85b9-4911-89be-6b38d75b1ec0`）

`GET` 读回当前 title/description（`updatedAt: 2026-09-17T00:53:53.928Z`，即 PATCH 已生效那版）：

| 判据 | 结果 | 证据（读回原文片段） |
|---|---|---|
| ①「根因」段末旧结论已改 | 通过 | 「🔴 结论（替换掉旧结论「这是配置层的过期钉选，不是代码里的硬编码」——那句是推断且与日志矛盾，已作废）：**这是派发期有效配置合成的问题：存储配置全程合法，被换掉的是合成出来的有效模型。**」 |
| ②「要做什么」§1-2 校验对象已改 | 通过 | 「护栏校验的**不是** `agents.adapterConfig.model` 这个存储值，而是**有效配置合成完成之后、下发给适配器之前的那个最终模型值**——即 `heartbeat.ts:21057-21058` 的 `configuredModel = readConfiguredModelFromAdapterConfig(runtimeConfig)`」 |
| ③「未决项」🔴 段假事实已核销 | 通过 | 「🔴 旧未决项「TOK-179 那个活着的 agent 上的过期 `adapterConfig.model` 钉选」——**已核销，无作用对象**。依据：TOK-190 裁定……与 TOK-191／TOK-193 取证。」并替换为新未决项（第三方适配器 `cheap` 档位）；产物段第4处同源表述也已同步改，未见「改前三处、留第4处自相矛盾」的情形 |
| ④ 标题已改 | 通过 | 现标题「期望3：派发期**有效模型**护栏（**校验下发适配器的最终模型值，非存储钉选** + 空列表 fail-open）」，不再含「过期钉选回落」 |
| 新判据写明校验对象＝合成之后/下发之前的最终模型值 | 通过 | 同②引用句 |
| 🔴 类形验收点，能把「存储合法、派发/恢复路径换成非法值」转红，含变异转红要求 | 通过 | 「> 对于**任何适配器类型、任何能覆写有效配置 `model` 的合成步骤**：当存储配置合法……而派发／恢复唤醒路径把合成后的有效模型换成了不在该列表里的值时，护栏必须在下发适配器之前拦住该值并按既定策略处置，且留下可观测痕迹。**删掉护栏调用后，该测试必须转红。**」；判据写法要求段另加「不许把断言写成 `model === "gemini-3.5-flash"` 时拦截」这种实例形提醒，并要求逐类型枚举＋双向计数（列出①模型档位通道②`Object.assign`写点③connector skill delivery） |
| 「列表为空 fail-open」分支及独立测试要求仍保留 | 通过 | 「🔴 **列表为空必须 fail-open**（原样使用，不回落）……验收点要求：交付里写明为何 fail-open；空列表分支**必须有独立测试**；这一条会被复核席单独审。」原样保留，未被删除 |

**结论：项2 列出的全部判据满足，通过。**

### 待办（不影响本次放行结论，但需记录并转交）

TOK-193 做席在交付后又发现顾问复核指出的 4 处措辞精度问题（「活模型列表已不含该值」应降级为「不在适配器接口实测返回的可选列表里」；需补一句提醒执行者开工前先确认列表来源走分支①还是②；验收点④措辞同步；「TOK-179 那条 run 也走 cheap 档」旁补一句 contextSnapshot 未找到的三态标注），起草了修正稿贴在 TOK-193 评论 `140287f8-6d5c-4d8d-9716-567aca8bfc99` 里，但 `PATCH /api/issues/1fca0264-...` 两次均返回 `403 cross_issue_influence_run_context_required`，按规则未再重试，已点名产品部负责人代写但**截至本次复核仍未落地**（TOK-189 `updatedAt` 仍是首次 PATCH 的 `00:53:53`，内容仍是精修前措辞）。

**本席判断：这 4 处属措辞精度问题，不构成本单（TOK-194）任何一条退回判据**——不是「把查不到写成不存在」，也不影响校验对象、类形判据、fail-open 分支这三条实质判据；且线上版本已要求 TOK-189 执行者「写明护栏实际用了 `listAdapterModels` 哪条分支的列表」，执行者动手时天然会撞到①/②的问题并自行查证。

⚠️ 需指出的事实（供上级掌握，不做处置）：`交付/TOK-185/接口证据.md` 的更正块**已经是精修后的措辞**（含「①／②未查（非穷尽）」「contextSnapshot 未找到（非穷尽）」），而线上 TOK-189 描述**还是精修前的措辞**——两份产物在这一处暂不一致，直到 TOK-193 评论里那份代写请求被产品部负责人落地为止。**本席不代为处置，只记录并在 TOK-191 放行评论里提醒。**

## 项 3：源头结论三态标注

通过。已找到（实查，path:line）：`/home/yuanjian/.paperclip/external-adapters/node_modules/@weslleycapelari/adapter-antigravity-local/dist/index.js:16-25`（本席复跑核实）；「除该档位外是否还有其他注入路径」标注为「未找到（非穷尽）」并点名未查面（内嵌 Postgres 运行期表；agent 令牌读 `runtimeConfig` 被脱敏为 `{}`；其余第三方适配器未逐个枚举）。未见把「未查」写成「不存在」的违规。

## 项 4：边界（未碰 agent 配置 / 未 push / 未碰本单以外文件）

- `git -C /home/yuanjian/dev/ANC status --porcelain`：与本次复核会话开始时的系统快照完全一致（`cli/**`、`ui/src/i18n/**`、`ui/src/pages/Search.tsx`、`server/src/services/recovery/*` 共 47 个既有 `M` 文件 + `?? 交付/`），无新增改动文件、无新增未跟踪文件（`交付/TOK-194/` 除外，为本席复核记录本身）。
- `server/src/services/recovery/{index,service,successful-run-handoff,successful-run-handoff.test}.ts` 的 `M` 在会话开始的系统快照里已存在（并非本次复核期间新出现），做席在 TOK-193 交付评论与 TOK-189 描述里都明确自称「不碰」该文件（归 TOK-188）；这几个文件 mtime 在本席复核过程中仍在变化（最近一次 `23:28 PDT ≈ 06:28 UTC`，接近本席复核时刻 `06:31 UTC`），与本单唤醒载荷提示的「共享工作区被另一并发 run 占用，预期出现并发改动」一致，**判断为工作区内其他并发活动（很可能是 TOK-188 侧），非 TOK-193 做席产物**，不计入退回判据。
- 未见任何 `agent update` / `permissions:update` / `config-revision:rollback` 调用痕迹（评论内自述 + 无对应产物）；未见 `git push` 痕迹（工作树无提交、`交付/` 未跟踪本就不需要 commit）。
- EXP-0108 越界追加：做席在评论中主动声明向 `~/eng-assets/lessons/经验登记表.md` 追加了第9条复发记录，本席 `grep` 复跑确认该行存在（2026-09-16，TOK-193 取证，cd-guard 拦截）。此为全局纪律要求的自身失误登记动作，不属业务产物越界，**不构成退回**。

**结论：项4通过。**

## 项 5：交付评论完整性

TOK-193 交付评论（`f6f2dfe5-6847-4c4e-a63b-84060538703f`）含：改动位置清单（文件+行号/TOK-189改前改后对照表）、一句话结论、三态标注表、本单实际运行次数「2次」（含中断的第1次说明）、自身失误登记行「已追加 EXP-0108 复发行（第9次）」。**通过。**

## 总结论

TOK-193 的两项产出（接口证据.md §期望3更正、TOK-189 判据重定）**均满足 TOK-194 列出的判据，予以放行**。唯一待办事项是 TOK-193 评论里那份卡在 403 权限的 TOK-189 二次精修代写请求，尚未落地，已在本记录与 TOK-191 评论中转交产品部负责人处理，不影响本次放行结论。
