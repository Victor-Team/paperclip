# ANC 第 1 步 · 产品研发部负责人审查

| 项 | 值 |
|---|---|
| 审查对象 | [TOK-155](/TOK/issues/TOK-155) 需求规格席交付 |
| 审查人 | 产品研发部负责人 |
| 日期 | 2026-09-16 |
| 结论 | **过审，不必返工。** 下一 Gate：CEO 建跨部门执行单（开发部·软件架构席） |

配套文件（未改）：

- `/home/yuanjian/dev/ANC/交付/第1步/任务书.md`
- `/home/yuanjian/dev/ANC/交付/第1步/验收单.md`

## 对照工单，规格齐了

任务书四项都有：取代码、装依赖/构建/启动顺序、端口与数据目录写死、失败怎么记。验收单十二条都能单独判通过/不通过；红线五条（AC-01～AC-05）任一条不过即整步不过。空系统里建测试公司，而不是「能看到现有公司和席位」。

## 本轮抽查（未改代码、未 checkout）

用 `git show origin/master:…` 与本机实测，不签出工作树。

| 任务书声明 | 抽查结果 |
|---|---|
| `embeddedPostgresPort` 默认 54329，无环境变量可覆盖 | `packages/db/src/runtime-config.ts:223` 属实；正式实例 `config.json` 也是 54329 |
| `onboard -y` 会立刻启动 | `cli/src/commands/onboard.ts:499,770` 属实 |
| 无 `-y` 会弹交互菜单 | 实际是 `:529` 的 `Choose setup path`（任务书写 `:527`，差两行，事实成立） |
| `-d X` 就是设 `PAPERCLIP_HOME=X` | `cli/src/config/data-dir.ts:26-29` 属实 |
| `PAPERCLIP_LISTEN_PORT` 优先于 `PORT` | `packages/adapter-utils/src/server-utils.ts:3114` 属实 |
| `pnpm install --frozen-lockfile` 是仓库 CI 用法 | `.github/workflows/e2e.yml:72`、`pr-trusted.yml:421` 属实 |
| `worktree:make --db-port` 存在 | `cli/src/commands/worktree.ts:4455` 属实 |
| 正式实例占 3100 / 54329，3200 / 54339 空闲 | 本轮 `ss -ltn` 属实 |
| `origin` = `https://github.com/Victor-Team/paperclip`，仅 `master`，基线 `bd51f157e` | 属实 |
| Node v26.6.0 / pnpm 11.23.0 | 属实 |
| 席位 API 读不到公司清单 | 本轮 `GET /api/companies` 仍是 HTTP 403 `Board access required` |

## 给执行与验收的备注（不是退回）

1. **U1 推荐候选 A。** 候选 B 会另建 checkout/分支，和「就在 `/home/yuanjian/dev/ANC` 干活」冲突。这是推荐，不是改规格。
2. **执行单必须落在 ANC 业务项目**（`projectId=2e3af214-90df-470d-ab2e-051660f56b7b`，工作区 `/home/yuanjian/dev/ANC`）。派到开发部部门档案目录，执行席会在错误目录里干活。
3. **本步整步上限 30 次运行。** 建议 CEO 直接指派软件架构席，少一跳转派。
4. 工作区里现有未跟踪项 `Agents.md`、`.claude/`，是席位环境注入，不是规格席改的。构建记录要写来源，避免 AC-05 冤判。
5. AC-04 辅判据（公司数量基线）席位取不到；主判据「正式平台不得出现 `ANC 测试公司`」仍可判。有 board 权限的一方若能在启动前记一版清单更好，记不到不挡执行。

## 下一 Gate

跨部门执行单由 CEO 建。产品验收席等执行单 identifier 回来之后再派，blockedBy 执行单。
