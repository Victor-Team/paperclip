# ANC 第 1 步 · 验收记录

> **版本**：v0.1
> **状态**：🔍 评审中
> **负责人**：产品研发部·产品验收席（agent `13436d07-f626-4f37-aa3b-e265441aa6c2`）
> **日期**：2026-09-16
> **最后更新**：2026-09-16 / 产品研发部·产品验收席
> **密级**：🔒 Internal
> **主文档引用**：[验收单.md](./验收单.md)（对照判定，v1.1 原文 ＋ TOK-161 裁定；v1.2 附录自第 2 步起生效，本轮忽略）
> **依赖规范**：[任务书.md](./任务书.md)、[构建记录.md](./构建记录.md)、TOK-161 裁定（构建记录「裁定落地记录」节）

本席按验收单 AC-01～AC-12 **独立判定**。不打分、不折中。本文件是 TOK-158 唯一主交付物。

---

## 1. 开工硬门

| # | 检查 | 结果 |
|---|---|---|
| 1 | `交付/第1步/构建记录.md` 存在且能读 | 是（2026-09-15 23:40，约 85757 字节） |
| 2 | 能回答：从哪取的代码 / 装依赖命令 / 构建命令 / 端口与数据目录 / 怎么验证起来了 / U1 选了哪条 | 是。取回 `origin/master`；`pnpm install --frozen-lockfile`；`pnpm build`；HTTP 3200 / PG 54339 / 数据目录 `~/.paperclip-anc/instances/anc/`；health + 建公司；**U1 选定候选 A**（先手工铺 `config.json`，`embeddedPostgresPort: 54339`） |
| 3 | TOK-157 状态确为 `done` | 是（id `68c4c9d9-7df3-4496-8088-99ae9b1c03ad`，本席 `GET` 实测 `status: done`） |

硬门通过，开始验收。本席**没有**自己补做构建、**没有**改 TOK-157、**没有**改任务书 / 验收单 / 负责人审查 / 构建记录。

---

## 2. 验收席自己重起的那一次

执行方进程在本席开工时已不在（3200 / 54339 空闲；3100 / 54329 正式实例仍在听）。凡「给定新实例已启动」均指下面这次。

**重起之前的正式平台公司清单基线**（无 Authorization 头，`GET http://127.0.0.1:3100/api/companies`）：

- 总数 **14** 家，与构建记录步骤 0.2 一致
- **不含**「ANC 测试公司」

**启动命令**（与构建记录「复现清单 ⑤」逐字一致；用 `env -i` 清空全部继承的 `PAPERCLIP_*`，只注入交接包三个变量）：

```sh
env -i HOME=/home/yuanjian USER=yuanjian SHELL=/bin/bash LANG=C.UTF-8 TERM=dumb \
  PATH="/home/linuxbrew/.linuxbrew/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" \
  PAPERCLIP_HOME=/home/yuanjian/.paperclip-anc \
  PAPERCLIP_INSTANCE_ID=anc \
  PORT=3200 \
  bash -c 'cd /home/yuanjian/dev/ANC && pnpm dev:once'
```

| 项 | 值 |
|---|---|
| 开始 | 2026-09-16T00:32:52-07:00 |
| health 就绪 | 2026-09-16T00:33:20-07:00（约 28 s） |
| 停机 | 2026-09-16T00:37:09-07:00；先确认 `/proc/<pid>/environ` 的 `PAPERCLIP_HOME=/home/yuanjian/.paperclip-anc`，再 `kill -TERM 1144817` |
| 停后端口 | 3200 / 54339 计数 **0**；3100 / 54329 计数 **3**；正式实例 `GET /api/health` → `{"status":"ok","version":"2026.831.1"}` |

入口：**文档化开发入口** `pnpm dev:once`（任务书 v1.1 §4c 二选一；U4 在构建记录里已从失败的 `pnpm paperclipai run` 切到此项）。本席**未**再跑默认生产入口 `pnpm paperclipai run`——构建记录 §4-8 已记其 doctor 退出码 1、3200/54339 不监听；TOK-161 裁定第 1 步不因此判不通过。本记录保留标注「默认入口未验证」。

---

## 3. AC-01～AC-12 判定表

| 条件 | 结论 | 证据定位 | 备注 |
|---|---|---|---|
| **AC-01** 正式 HTTP 端口没被抢 | **通过** | 本席重起后 `ss -ltn`：`127.0.0.1:3100` 与 `127.0.0.1:3200` 同时在听。3100 pid **378**，cmdline `…/.paperclip/cli/current/… run --instance default`，`PAPERCLIP_HOME=/home/yuanjian/.paperclip`。3200 pid **1144817**，cwd `/home/yuanjian/dev/ANC/server`，`PAPERCLIP_HOME=/home/yuanjian/.paperclip-anc`，`PAPERCLIP_INSTANCE_ID=anc`，`PORT=3200` | 3200 不是正式平台进程。标注：默认入口未验证 |
| **AC-02** 数据库端口没被抢，也没有连到正式库 | **通过** | **逐字读文件** `/home/yuanjian/.paperclip-anc/instances/anc/config.json` 第 10 行：`    "embeddedPostgresPort": 54339,`；解析值为整数 `54339`。`ss -ltn`：`127.0.0.1:54329`（postgres pid 1639，正式）与 `127.0.0.1:54339`（postgres pid 1144894，cmdline `-D /home/yuanjian/.paperclip-anc/instances/anc/db -p 54339`）同时在听 | 构建记录写明未用 `onboard -y`、新实例自始至终只用 54339；doctor 失败发生在启动之前，未监听 54329。标注：默认入口未验证 |
| **AC-03** 数据目录完全独立 | **通过** | `ls /home/yuanjian/.paperclip-anc/instances/anc/` 含 `config.json`、`db/`、`data/`、`logs/`、`secrets/`。验收单 v1.1 五字段机验脚本输出 **PASS**（取值均以 `/home/yuanjian/.paperclip-anc/instances/anc/` 为前缀）。3200 进程指向 `~/.paperclip/instances/default` 的 fd 计数 **0**。嵌入式 PG `-D` 落在 anc 目录 | 仓库根 `.paperclip/dev-server-status.json` 是 `pnpm dev:once` 的开发状态文件（已 gitignore），**不是** `~/.paperclip/`。`config.json` 全文 0 处 `paperclip/instances/default` |
| **AC-04** 正式平台数据没被污染 | **通过** | 重起前基线 14 家、无「ANC 测试公司」。建公司证据取完后正式平台仍 14 家、无「ANC 测试公司」。新实例 `GET http://127.0.0.1:3200/api/companies` 仅 1 家：`ANC 测试公司`（id `709b6d8c-3700-4d12-81af-c414a61e8aca`），**未出现**正式平台既有公司名 | 主判据满足。数量对照：**有基线，14→14，未缺**。新实例不是正式库 |
| **AC-05** 没有越界改动 | **通过** | `git -C /home/yuanjian/dev/ANC diff --stat` 空；`git log origin/master..HEAD` 空；`git log --oneline -3` 顶点仍为 `bd51f157e`。已跟踪文件零改动 | 未跟踪：`交付/`（本步产品包，预期内）；`Agents.md`、`.claude/skills/*` 为本席本次运行的席位环境注入（派发说明已定性，**不判执行方越界**）。构建记录执行当时 `git status` 只有 `?? 交付/` |
| **AC-06** 代码是取回来的 | **通过** | `git log -1 --format=%H` = `bd51f157e9aade37f2f4602eda238fe51405a0c2`（基线，非其后继）；`git remote -v` origin = `https://github.com/Victor-Team/paperclip`；`git branch -vv`：`* master … [origin/master]`。新实例 health 的 `commit` 同此 SHA | 「能看到现有公司和席位」未用作判据。新实例只有测试公司，符合「空白全新系统」 |
| **AC-07** 实际主分支名已写明 | **通过** | 构建记录步骤 0.1：「远端实际主分支名为 `master`，与预期一致」（`git ls-remote --heads origin` 只返回 `refs/heads/master`）；步骤 1 再写一次 | — |
| **AC-08** 构建产物真的产生了 | **通过** | 构建记录步骤 3 第 2 次：命令 `pnpm build`，起止 `22:33:47` / `22:35:21`，耗时 1m 34s，退出码 **0**，有输出摘录。本席只读核验 `交付/第1步/.build.log` 末行 `EXITCODE=0` / `END 22:35:21`；`packages/paperclip-runner/dist/bin/paperclip-runnerd` 仍在盘（36713616 字节，SHA-256 `18b012acd912e591119d498a9216a8698ab3378123374f78123cb274203df91b`） | 本席未重跑 `pnpm build`（那是 TOK-157 的活）。runnerd **运行接线未激活**属 AC-08 之外，按 TOK-161 不影响本条 |
| **AC-09** 首页能打开 | **通过** | `curl -s http://127.0.0.1:3200/api/health` 含 `"status":"ok"`（commit 与 HEAD 一致）。`GET http://127.0.0.1:3200/` HTTP 200。截图 `截图/验收-01-新实例首页-3200.png`：浏览器打开后落到 `/ANC/dashboard`，画面为未走完的引导浮层「Create your first agent」——**本席打开图像看过**，非报错页、非空白页；Playwright `pageerror` / `console.error` 为空数组 | 公司已存在后 `/` 会进仪表盘，浮层是执行方刻意未走完引导的遗留（与其截图 05 同类）。不以 DOM 文本代替画面。标注：默认入口未验证 |
| **AC-10** 能在新实例里新建测试公司 | **通过** | 本席按记录重起后，新实例已有 `ANC 测试公司`（与构建记录步骤 5-4 同一 id）。截图 `截图/验收-02-ANC测试公司-公司设置页.png`（本席打开图像看过）：面包屑 **`ANC 测试公司 > Settings`**，Organization name 输入框为 **`ANC 测试公司`** | 按 U6 数据保留，本席未再新建同名公司（名称已占用）。创建动作由 TOK-157 完成，本席在独立重起后目击其仍在新实例、且不在正式实例。标注：默认入口未验证 |
| **AC-11** 构建记录可被别人照着重跑 | **通过** | 本席未参与 TOK-157 执行，仅照「复现清单 ⑤」三变量 + `pnpm dev:once` 即把实例拉起（见 §2）。U1 写明**候选 A**；配置在第一次启动前写入 `embeddedPostgresPort: 54339`；环境清理用 `env -i`（比逐个 `unset` 更彻底），只导出 `PAPERCLIP_HOME` / `PAPERCLIP_INSTANCE_ID` / `PORT` | 缺步骤会起不来——本次起来了。默认入口照记录会失败，已在复现清单作事实陈述；按 TOK-161 **AC-11 通过**，标注「默认入口未验证 / 默认入口不通过」不得冲掉 |
| **AC-12** 耗时与失败都记了 | **通过** | 构建记录「本步失败与重试汇总」：取代码 / 装依赖 / 构建 / 启动四步均有起止与耗时。失败 6 次均有报错原文（`cargo: not found`、doctor `Managed install manifest`、观测错误、两种停法未停净、默认入口复测失败），不是复述 | 全程有失败，故未写「本步无失败」——按条文这是正确写法 |

红线条 AC-01～AC-05 **全部通过**，整步不被红线一票否决。

---

## 4. 三选一结论

**全部通过。**

依据：验收单 v1.1 AC-01～AC-12 原文 ＋ TOK-161 裁定（第 1 步「已启动」按任务书 v1.1 §4c 所选开发入口理解；不因 `pnpm paperclipai run` 未跑通而判不通过）。

必须保留、不得被本结论冲掉的标注：

1. **默认入口未验证**：`pnpm paperclipai run` 在本机被 doctor 拦住（构建记录 §4-8），本席本轮未再跑。第 2 步第 0 项前置不随本单通过而消失。
2. 构建记录《本步结论》那张开发路径 / 默认入口 **分列表**仍有效，本席不把它改回「十二项全过、默认入口也过」。
3. 第 1 步不授权改 `cli/src/install-store.ts` / `cli/src/checks/managed-install-check.ts`。

退回对象：无。

---

## 5. 本席未做的事（边界）

- 未触碰正式实例 3100 / 54329，未向 `~/.paperclip/` 写入
- 未改源码、文案、lockfile、`.gitignore`、LICENSE
- 未改 `任务书.md` / `验收单.md` / `负责人审查.md` / `构建记录.md`
- 未 `git checkout` 到别的分支、未 commit、未 `git push`
- 未引入新依赖（Playwright 用仓库既有 `playwright@1.62.1`，脚本写在本次 scratch，未进仓库）
- 未自己补做 `pnpm install` / `pnpm build`
- 未往第 2 步走

本席写入的仅有：本文件；`截图/验收-01-新实例首页-3200.png`；`截图/验收-02-ANC测试公司-公司设置页.png`。

---

## 6. 自身失误登记

自身失误登记：本次未发现

（硬门、基线、独立重起、逐条读 config.json、截图打开图像再写描述、停机前核对 `PAPERCLIP_HOME`，均按验收单与 EXP-0002 / EXP-0003 做了。）

---

## 变更日志（Changelog）

| 日期 | 修改人 | 修改内容 | 影响范围 | 关联文档 |
|---|---|---|---|---|
| 2026-09-16 | 产品研发部·产品验收席 | 初稿：对照 AC-01～AC-12 独立判定，结论「全部通过」 | 全文 | 验收单.md；构建记录.md；TOK-158 |
