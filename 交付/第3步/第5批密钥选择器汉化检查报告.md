# TOK-213 第 5 批密钥选择器汉化检查报告

## 范围

- 改动：`ui/src/components/environment-variables-editor/SecretPicker.tsx`。
- 覆盖目录浏览、返回上级目录、当前／最近使用／全部机密分组、搜索占位、空状态、缺失机密提示及非活跃状态徽标。
- 密钥名、路径、机密 ID 和服务端状态值是动态数据，保持原样；选择、禁用和目录导航逻辑未改。

## 翻译与语言文件

- `en.json` 新增 12 个英文事实源键；`zh-CN.json` 增加对应简体中文译文。
- 其余 38 个 locale 由 `scripts/generate-i18n-locales.mjs --copy-en-values` 从英文基准同步。
- `node scripts/check-i18n-locales.mjs`：通过，2,646 键、40 locale 键集一致。

## 验证

- `pnpm vitest run ui/src/components/environment-variables-editor/EnvironmentVariablesEditor.test.tsx --passWithNoTests`：29 条通过；包含中文目录分组、搜索占位、返回上级目录和无匹配空状态断言。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：未通过，仅有既有且未触及的 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺 `enableMcpAggregators`；本切片未新增相关类型错误。

## 未完成项

第 5 批仍在扫描十个组件目录中的模块级可见映射；全量完成前不宣称浏览器整机放行。
