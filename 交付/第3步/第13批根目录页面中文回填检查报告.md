# TOK-213 第 13 批：pages 根目录中文回填与计数文案修正

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`bad8c0ae0`。

## 范围与改动

- 仅修改 `ui/src/pages/*.tsx` 根目录下的 `CompanyEnvironments.tsx`、`CompanyImport.tsx`、`SystemNoticeUxLab.tsx`、`TeamCatalog.tsx`，以及 `ui/src/i18n/locales/zh-CN.json`；没有触及 `pages/apps/**`、其他席位目录、共享脚本或默认语言。
- 回填 124 个已接线键的简中值：邀请落地页 36、公司环境 28、公司导入 24、插件设置 36。遵守术语表：平台租户 `organization` 译“公司”，`agent` 译“智能体”，`workspace` 译“工作区”；Paperclip、SSH、Webhook 等专名保留。
- 修正根目录 JSX 中 9 处误留的字面 `t`：公司导入 4 处、公司环境 1 处、团队目录 2 处、系统通知体验页 2 处。清理公司导入、团队目录和时间线里把英文复数后缀 `s` 错译为“秒”的简中值；团队卡片计数缩写改为中文类别。
- 精确扫描根目录直接调用 `t("静态键")` 且简中值与英文值相同的键，由本批前 434 降至 302。这个口径不覆盖动态键、尚未接线的 JSX 和应保留英文的专名，不能视为全站残留清单。
- 40 个 locale 的键数仍均为 6,531；本批只改简中值与所属页面 JSX，未增加或删除键。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 键集一致，各 6,531 键。
- `pnpm vitest run ui/src/i18n/locale-validation.test.ts`：7/7 通过。
- `pnpm --dir ui exec tsc -b`：通过。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过；根目录 `rg -n '\}t\{' ui/src/pages/*.tsx`：无命中。

## 尚待完成

- `pages` 根目录直接静态键扫描仍有 302 个英文回退，其中含需按上下文翻译的界面文案，也含应保留的品牌、协议、代码标识；下一批逐页处理并列出最终豁免。
- 本批没有浏览器整机复验或两套断点截图。全量完成后按本单验收判据补齐可运行界面、语言切换、刷新持久化和版式证据，再交独立席复核。
