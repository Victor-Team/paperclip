# TOK-213 第 13 批：pages 根目录中文回填与残留复扫

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`bad8c0ae0`、`d2ae672f0`、待归档的箭头符号修正。

## 本批改动

- 仅改 `ui/src/pages/*.tsx` 根目录所属页面与 `ui/src/i18n/locales/zh-CN.json`；没有触及 `pages/apps/**`、其他席位目录、共享脚本或默认语言。
- 给 377 个已接线键回填简中值：邀请落地页 36、公司环境 28、公司导入 24、插件设置 36、邀请体验页 50、系统通知实验页 39、团队目录 39，以及其他根目录页面 125。术语表的公司、智能体、工作区用法保持一致；品牌、模型、协议和代码标识保留原文。
- 修正接线时误留的字面 `t` 共 11 处，涉及公司导入、公司环境、团队目录、系统通知实验页、时间线及待办页。公司导入、团队目录、时间线中英文复数后缀 `s` 被错译为“秒”的简中值已清理；团队卡片计数类别改为中文；公司导入的 HTML 箭头实体改为实际箭头符号。
- 40 个 locale 键数仍各为 6,531。本批只改简中值与所属页面 JSX，未增加或删除语言包键。

## 残留与边界

- 根目录 79 个非测试 TSX 文件中，直接 `t("静态键")` 的英文回退由本批前 434 降至 49。剩下 49 项为品牌与模型名、命令、API 路径、代码关键字、示例数据、权限错误码、无障碍属性名等原文标识；例如 `Codex`、`POST /api/agents/claim-api-key`、`origin/main`、`feedbackDataSharingPreference`、`aria-expanded`。这些按原样展示，不是待译自然语言。
- 对相同 79 个文件运行 `scripts/i18n-codemod.mjs` 只读 AST 扫描，仍检测到 **2,781 条可自动接线的可见文案**。集中在 `DesignGuide.tsx` 447、`Secrets.tsx` 334、`CompanySkills.tsx` 281、`CompanySkills.production.tsx` 275、`Pipelines.tsx` 221、`AgentDetail.tsx` 181、`AgentDetail.production.tsx` 166、`SkillStudio.tsx` 162、`PipelineSettings.tsx` 129、`ExecutionWorkspaceDetail.tsx` 95 等文件。`*.production.tsx` 是真实页面，下一批必须接线。上述 49 项和 2,781 条是两种不同口径，不能相加作全站总数。
- `CompanyImport.tsx` 原有非组件 JSX 箭头实体是 AST 扫描唯一人工候选，现已改为符号“→”，该文件复扫为 0。当前只是根目录快照，不构成 `ui/src` 全站残留清单。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 键集一致，各 6,531 键。
- `pnpm vitest run ui/src/i18n/locale-validation.test.ts`：7/7 通过。
- `pnpm --dir ui exec tsc -b`：通过。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过。

## 后续验收

- 下一批从上述根目录待接线文件推进，逐批补充中文值和语境豁免；不得把本批的“49 个静态键原文”误当成根目录全部完成。
- 本批未做浏览器整机复验和两套断点截图。全量完成后按本单验收判据补齐可运行界面、语言切换、刷新持久化及版式证据，再交独立席复核。
