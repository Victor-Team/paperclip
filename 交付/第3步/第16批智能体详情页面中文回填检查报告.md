# TOK-213 第 16 批：智能体详情页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`db0bbf896`。

## 本批改动

- 仅处理本席的 `ui/src/pages/AgentDetail.tsx`、`AgentDetail.production.tsx` 和 40 个语言包。两份页面分别使用自己的 `agentdetail.*` 与 `agentdetailproduction.*` 命名空间。
- 将两页共 **247 个原有英文回退值**回填为简体中文。覆盖智能体配置、运行、工作区、权限、日志等页面文案。补充 **139 个英／简中键**，覆盖此前 AST 扫描漏掉的自定义组件属性、工作区阶段与状态、动态确认框、面包屑、运行来源、设置分区与 toast。新增键的其余 38 个语言包按脚本填英文原文。
- 中文动态句子使用完整插值键，例如暂停内置智能体、删除文件、运行编号和清理多个议题会话。服务端错误、智能体名称、文件名、运行编号等动态数据保留原值；`TOOLS.md`、`stderr` 和 `# Agent instructions` 作为代码/文件示例保留原文。
- 40 个 locale 现各 **9,478 键**。相对第 13 批基线新增的键中，简中仍与英文相同者由上批的 1,705 降至 **1,458**；这不是全站剩余文案总数。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 9,478 键，键集与 `en.json` 一致。
- `pnpm --dir ui exec tsc -b`：通过。
- `pnpm vitest run ui/src/pages/AgentDetail.instructions.test.tsx ui/src/pages/AgentDetail.log-visibility.test.tsx ui/src/i18n/locale-validation.test.ts`：3 个文件、17/17 通过。
- `pnpm check:token-gates`：四项 CLEAN；`git diff --check`：通过。

## 剩余项与证据界限

- 本批没有浏览器整机复验或两套断点截图，因此尚未核验新布局中的截断、重叠、按钮宽度与换行。全站截图、切换语言后刷新持久化、独立抽样复核仍须按 TOK-213 验收判据完成。
- `AgentContextualSidebar` 位于 `ui/src/components/**`，由并行席处理；本批未改该组件。两页仍有后端返回的错误原文及文件/代码示例，须在最终残留清单中逐项说明。
