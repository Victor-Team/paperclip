# 第6批审批队列与连接审查提示汉化检查报告

日期：2026-09-24  
范围：`ui/src/pages/apps/ReviewQueueCard.tsx`、`app-detail/ReviewPanel.tsx`、`ReviewQueueCard.test.tsx`

## 本轮变更

- 审批队列卡及连接审查面板的标题、失败／空态、操作上下文、审批策略说明、允许／拒绝按钮、结果 toast 和本地保存失败回退均改为当前语言在渲染期解析。
- 工具操作名、应用名、相对时间、Markdown 预览与服务端返回的错误维持动态原值；审批、拒绝、信任规则、缓存失效及交互卡路径没有变更。
- 新增 20 个英／简中键；其余 38 个 locale 由既有同步脚本补齐英文后备值。40 个 locale 键集现为 2,862 键。

## 验证

- `pnpm vitest run ui/src/pages/apps/ReviewQueueCard.test.tsx ui/src/pages/apps/AppsReview.i18n.test.tsx --passWithNoTests`：2 个文件、4 条通过。新增用例断言简体中文“等待您的确认”、审批策略说明以及“允许一次／始终允许／拒绝”控制。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，共 2,862 键。
- `pnpm check:token-gates`：四项 Gate 均为 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仅重现未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`；本切片未新增类型错误。

## 已知边界

- `pages/apps` 的其他连接详情、配置和网关组件仍在第 6 批连续范围内；本报告不宣称目录或全站完成。
- 隔离最小种子将 `/apps/review` 解释为缺少的组织前缀，不能从该 URL 进入应用页；这是测试种子路由数据限制，不是本切片的浏览器渲染失败。本报告不作浏览器或整机放行声明。
