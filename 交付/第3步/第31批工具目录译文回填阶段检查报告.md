# TOK-213 第 31 批：工具目录译文回填阶段检查

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`43a3eb280`。

## 范围与结果

- 在 `ui/src/pages/tools/**` 范围内，回填 18 个页面命名空间中的 255 项英／简中相同自然语言值。涉及访问配置、工具权限、网关、审计、MCP 配置和 Smoke Lab。只保留 6 个技术或专有值：`vault://`、`send_email` / `slack.list_*` 示例、`p`、`Smoke Lab` 两处及 `ms`。
- 把工具数量、搜索无结果、应用分组和启用操作数量改成完整插值句子；修正审计操作与网关时间之间缺失的空格。新增 6 个英／简中键，40 个 locale 各 10,649 键且键集一致。新增测试核验语言切换后分组句子和无障碍标签立即更新。
- 本阶段尚未完成整个目录：只读 AST 扫描显示候选 0，但手工源码检查仍发现尚未接入 `t()` 的操作反馈、列表标签和部分可访问性文案，主要在 `ProfilesTab.tsx`、`GatewaysTab.tsx`、`AuditTab.tsx`、`ProfileDetail.tsx` 等文件。下一轮继续同一目录的源码接线，不将目录标为已完成。

## 验证

- `node scripts/generate-i18n-locales.mjs`、`node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,649 键且键集一致。
- `pnpm --dir ui exec vitest run src/pages/tools src/i18n/locale-validation.test.ts`：16 个文件，124/124 通过。首跑暴露审计/网关文本缺失空格和一项测试直接比较多行 `textContent`，已修正并复跑通过。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项和 `git diff --check`：通过。

## 后续验收

继续在 TOK-213 中处理 `pages/tools/**` 仍未接线的英文，再处理其他页面。浏览器整机、桌面和窄屏截图及独立席位复核均待全站收尾验收；本阶段不视作目录或全站放行。
