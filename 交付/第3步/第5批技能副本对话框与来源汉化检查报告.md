# 第5批技能副本对话框与来源汉化检查报告

日期：2026-09-24  
范围：`ui/src/components/skill-studio/ForkSkillDialog.tsx`、`SkillProvenance.tsx`、`ui/src/lib/skill-fork.ts`

## 本轮变更

- 将技能副本创建成功和失败提示、智能体使用数量、创建与切换动作标签接入渲染期 i18n。
- 为来源与智能体数量纯函数增加可选翻译函数，并由可见的技能来源组件传入当前语言。
- 技能名、智能体名、服务端返回的错误、品牌名、URL 和仓库定位信息保持动态原值；未改变副本创建、智能体切换或导航逻辑。

## 验证

- `pnpm vitest run ui/src/lib/skill-fork.test.ts ui/src/components/skill-studio/ForkSkillDialog.test.tsx --passWithNoTests`：2 个测试文件、25 条通过。新增断言确认简体中文下显示使用数量与“创建副本”。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，2,803 键。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仍只报既有、未触及的 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` fixture 缺少 `enableMcpAggregators`；本轮未新增类型错误。

## 未覆盖项

本报告只证明本切片的定向验证。第 5 批及全量 UI 尚未收尾，因此未进行浏览器整机放行，也不作整批或全站放行声明。
