# TOK-213 第 29 批：会议室聊天页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`644eaf693`。

## 范围与结果

- 本批源码仅 `ui/src/pages/BoardChat.tsx`，另更新同页测试与 40 个 locale JSON。没有修改 `pages/apps/**` 或并行席的组件、上下文、功能、适配器与插件目录。根目录其他页面仍在 TOK-213 范围内。
- `boardchat.general` 原有 13 个英／简中相同值中，12 个自然语言值回填中文、1 个无引用英文秒数后缀键删除；新增 22 个双语键，净增 21 键。40 个 locale 各 10,577 键且键集一致，本页命名空间英／简中相同值为 0。
- 会议室面包屑、欢迎语、四个建议按钮及其提问内容、发送标签、连接/思考/命令/读文件/搜索状态和错误回退按当前语言生成。欢迎语按有无使命选择完整句子，建议提问在简中模式下发送中文。公司名、CEO 名及使命内容来自用户数据，保持原值。
- 本页 AST 可见英文候选复扫为 0。相对第 13 批基线，全线新增键中英／简中相同值由 166 降至 153。服务端自由文本、聊天正文和并行席组件中的时间/反馈文案不在本页改动范围，最终整机验收仍需核对。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,577 键且键集一致。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/BoardChat.tsx --json <临时报告>`：只读扫描，可见英文候选 0。
- `pnpm --dir ui exec vitest run src/pages/BoardChat.test.tsx src/i18n/locale-validation.test.ts`：2 个文件，14/14 通过；新增断言覆盖切换简中后的欢迎语、建议按钮、建议提问和发送标签。首次新增测试将两个串联定时阶段一次推进，未等待第二阶段计时器，调整测试时钟后复跑通过。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项、`git diff --check`：通过。

## 页面 × 状态 × 断点证据

| 页面状态 | 桌面断点 | 窄屏断点 | 与设计一致 |
| --- | --- | --- | --- |
| 打字动画、欢迎语与建议按钮 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 发送中状态、错误与聊天记录 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 活动动态、语言切换与侧栏 | 待全量整机复验 | 待全量整机复验 | 待核对 |

本批没有做浏览器整机、双断点截图或独立席位复核，不能视作全站放行。
