# TOK-213 第41批聊天连接动态文案阶段检查报告

日期：2026-09-25。执行席：开发部·前端实现席。代码提交：`be18b0825730b6645f41c8c5a185c90c34f774af`。本批一轮，平台 token 用量待平台核算。

## 范围与改动

- 仅修改本席 `ui/src/pages/apps/chat/**` 中的 5 个生产文件、1 个定向测试及 `ui/src/i18n/locales/`。目录总共 13 个生产 TSX，本批处理同一聊天连接流程的动态余项，未凑 30 个文件；未修改并行席 `components/**` 或共享脚本。
- 新增 20 个英／简中键，40 个 locale 各 11,352 键；其余 38 个语言包由现有生成脚本生成。默认英文文案保持原值。
- `ChatEndpointSetup.tsx`：连接选择/聊天面包屑、Slack 七步导航、端点与私钥文件错误回退接入当前语言；已有的“选择智能体”“创建 Slack 应用”“验证 Slack 连接”“试用”复用现成键。语言切换后面包屑重新渲染。
- `ChatEndpointDetail.tsx`：访问权限更新失败提示接入当前语言；`ChatIdentityConfirm.tsx`：本地董事会身份名接入当前语言；`EmailEndpointSetup.tsx`：接收中和草稿、验证中、暂停、待处理、已撤销、归档状态按当前语言显示。
- `PhotonConnectStep.tsx`：重新连接说明与共享/专用线路项目标题改为带插值的完整句子，避免中文语序按英文碎片拼接。新增键的英／简中占位符一致。

## 验证

- `pnpm --dir ui exec vitest run src/pages/apps/chat/ChatEndpointSetup.routing.test.tsx src/pages/apps/chat/ChatIdentityConfirm.test.tsx`：11/11 通过；新增测试验证 GitHub 连接选择页语言切换后面包屑即时更新。
- `pnpm --dir ui exec tsc -b`、`node scripts/check-i18n-locales.mjs`、`pnpm check:token-gates`、`git diff --check` 均通过。Token Gate 四项 CLEAN。所查 5 个命名空间的 371 个英／简中键插值占位符一致。
- `pages/apps/chat/**` AST 复扫只检出 `ChatEndpointSetup.tsx` 的 `bot`，处于 `<code>bot</code>` Telegram 用户名示例，属于必须保留的代码标识，不是未译自然语言。
- 隔离真实路由 `/TOK/apps/chat/connect?provider=slack&purpose=choice`：桌面 1366×768 与窄屏 390×844 简中选择页、桌面英文对照均返回 200，页面异常 0、横向溢出 0；中文桌面七步导航及英文对照按预期显示。证据：`交付/TOK-213/浏览器证据/第41批/证据索引.md` 与同目录截图。

## 尚未完成与限制

- 窄屏步骤导航按现有响应式布局隐藏；截图仅验证窄屏面包屑和选择卡。邮件各状态、Photon 有数据/重新连接、身份确认、Slack 后续步骤与失败态尚无双断点浏览器证据。
- `chat-ui-contract.test.ts` 是旧的源码文本断言：本轮将其加入定向运行时 13 项中 12 项失败，断言仍要求汉化前的英文直接出现在 TSX 源码；本批没有据此宣称全套测试通过。需在本单后续阶段将该测试更新为针对译文/渲染行为的断言，并复跑。
- 侧栏公司标识资源在桌面返回 404，已在浏览器索引单列。设计稿一致性、独立席复核、全站残留英文清单与默认隔离入口仍未完成。`pnpm dev:once` 证据不替代 `pnpm paperclipai run` 默认入口验收。本批不作为聊天目录或全站放行。
