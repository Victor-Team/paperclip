# TOK-213 第31—35批浏览器证据与返工检查

日期：2026-09-25。执行席：开发部·前端实现席。

## 本轮结果

按开发部负责人裁定，在第36批前补第31—35批工具目录浏览器证据。隔离实例 3200/54339 上实际打开 `profiles`、`gateways`、`paste-config`、`smoke-lab` 四个入口，取得桌面 1366×768 与窄屏 390×844 截图，并检查访问配置向导、权限抽屉、网关新建对话框、粘贴配置帮助弹窗及语言选择。完整逐项索引：`交付/TOK-213/浏览器证据/第31-35批/证据索引.md`。

浏览器发现本席目录内两项问题，已在原批次范围返工：帮助弹窗三条直写英文说明接入 i18n；Smoke Lab 矩阵在窄屏改为横向滚动，列头不再逐字换行。新增 3 个英／简中键，其余 38 个 locale 由现有脚本同步。40 个 locale 各 10,995 键，键集一致，默认英文文案和复制提示词不变。

## 验证与限制

- 定向 Vitest：`McpConfigHelpDialog.test.tsx` 与 `SmokeLabTab.test.tsx` 共 16/16 通过；新增测试核对简中说明和复制提示词仍与原值一致。
- UI `tsc -b`、`check-i18n-locales`、Token Gate 四项、`git diff --check` 均通过。
- 真实浏览器：四入口桌面/窄屏基础状态有截图；帮助弹窗返工后桌面/窄屏有截图；矩阵修正后左/右滚动有截图。页面整体 `scrollWidth` 未超出视口；矩阵容器宽 356px、内容宽 806px、可滚动 450px。语言入口从英文选简中，刷新后选择器、`html[lang]` 与 `localStorage` 均保持简中。
- `pnpm paperclipai run` 的隔离 doctor 失败仍在：缺 `cli/install.json`。本次改用隔离 worktree 的 `pnpm dev:once` 加现有 runner 二进制取得截图；不把默认入口判为通过。
- 截图暴露的共用侧栏英文、服务端返回英文标签、可复制英文提示词，以及有数据列表/动态错误态、设计稿对照和独立复核，见证据索引。**本轮仅补浏览器证据与两处返工，不作为目录或全站放行。**
