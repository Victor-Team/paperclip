# 第6批连接高级配置与危险操作汉化检查报告

日期：2026-09-24  
范围：`ui/src/pages/apps/app-detail/AdvancedPanel.tsx`、`AppNotConnected.tsx`、`AdvancedPanel.render.test.tsx`

## 本轮变更

- 高级配置的重新连接、凭据验证、OAuth 登录、连接详情、暂停、撤销身份、移除应用及危险操作确认文案均改为当前语言在渲染期解析。
- 地址兜底帮助函数接受可选翻译函数，`AppNotConnected.tsx` 以当前语言传入该函数；未接入的调用方保留英文默认值，避免改变非 UI 调用的既有行为。
- Vercel Connect 品牌、应用名、身份提供方名称、连接显示名、地址、服务端健康消息和凭据字段标签保持动态原值；连接、OAuth、健康检查、替换凭据、暂停、撤销和移除逻辑没有变更。
- 新增 49 个英／简中键（含服务数量复数键）；其余 38 个 locale 由既有同步脚本补齐英文后备值。40 个 locale 键集现为 2,917 键。

## 验证

- `pnpm vitest run ui/src/pages/apps/app-detail/AdvancedPanel.render.test.tsx --passWithNoTests`：1 个文件、6 条通过。新增用例断言简体中文“危险区域”、移除应用标题及移除凭据与智能体访问说明。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，共 2,917 键。
- `pnpm check:token-gates`：四项 Gate 均为 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仅重现未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`；本切片未新增类型错误。

## 已知边界

- `pages/apps` 的其他连接详情和配置组件仍在第 6 批连续范围内；本报告不宣称目录或全站完成。
- 本轮未进行应用页浏览器验证：隔离最小种子的应用路由受组织前缀数据限制。本报告不作浏览器或整机放行声明。
