# TOK-213 第 6 批 · 操作测试面板汉化检查报告

## 范围

- 分支：`feat/anc-zh-cn`
- 本切片：`ui/src/pages/apps/app-detail/TestPanel.tsx`
- 工具改进：`scripts/i18n-codemod.mjs` 支持单个 TSX 文件输入。
- 不改：连接、审批、运行、轮询、缓存、路由、动态工具／智能体／应用名称和服务端原始数据。

## 接线结果

- AST codemod 在改写前检出 93 个可见文案候选，全部自动接入 `t()`。
- 改写后重扫：0 个候选；`en.json` 与 `zh-CN.json` 录入 93 个 `testpanel.general.*` 键，其余 38 个 locale 由生成脚本按英文同步。
- 新增中文渲染测试，验证“测试操作”“允许”“请求确认”“关闭”在切换至 `zh-CN` 后显示；测试结束后恢复英文，避免影响已有英文断言。

## 验证

| 命令 | 结果 |
|---|---|
| `pnpm vitest run ui/src/pages/apps/app-detail/TestPanel.test.tsx` | 1 文件，18/18 通过 |
| `node scripts/check-i18n-locales.mjs` | 3,151 键，40 locale 一致 |
| `pnpm check:token-gates` | 四项 CLEAN |
| `git diff --check` | 通过 |
| `pnpm --filter @paperclipai/ui exec tsc --noEmit` | 仅既有 `InstanceExperimentalSettings.test.tsx:71` 缺 `enableMcpAggregators`；本切片未新增类型错误 |

## 状态

这是第 6 批 `pages/apps` 目录的一个大组件切片，不是目录、浏览器或整机放行结论。后续继续处理该目录剩余可见文案；隔离实例浏览器验证留给第 6 批目录收口时执行。
