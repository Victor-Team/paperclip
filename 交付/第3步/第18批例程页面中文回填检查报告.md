# TOK-213 第 18 批：例程页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`cb39d5337`。

## 本批改动

- 处理 `ui/src/pages/Routines.tsx` 与真实生产变体 `Routines.production.tsx`，以及对应测试和 40 个语言包。两页原有 48 个英文回退值已回填简中，两个命名空间的静态键现均有中文值。
- 对此前绕过 AST 扫描的可见文字补接 i18n：创建和移动结果 toast、文件夹与项目分组、排序与筛选标签、负责人/项目选择器、空状态、并发与补跑策略说明。例程数量和移动目标使用完整插值句子，不再拼英文复数尾巴。新增 **140 个英／简中键**，其余 38 个语言包由脚本填英文原文。
- 分组函数保留英文默认输出以兼容既有调用；页面渲染时传入当前语言的翻译函数。动态例程标题、文件夹名称、项目名称和后端错误仍按原值显示。术语依照已验收术语表：`routine`＝“例程”，`folder`＝“文件夹”。
- 40 个 locale 各 **9,706 键**。相对第 13 批基线新增键中的简中英文回退从 1,360 降到 **1,312**；其中仍混有应保留原文的产品/代码和其他页面待译文案。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 9,706 键，与 `en.json` 键集一致。
- `pnpm --dir ui exec tsc -b`：通过。
- `pnpm vitest run ui/src/pages/Routines.test.tsx ui/src/i18n/locale-validation.test.ts`：2 个文件、23/23 通过；新增简中分组标签验证。
- `pnpm check:token-gates`：四项 CLEAN；`git diff --check`：通过。

## 剩余证据

- 本批未运行隔离浏览器实例，未拍两套断点截图；例程弹窗、分组菜单和 toast 的排版与切换语言后状态，需在全量整机验收时核对。
- `RoutineList`、文件夹控件和运行变量弹窗位于 `ui/src/components/**`，由并行席处理；本批没有修改这些组件。
