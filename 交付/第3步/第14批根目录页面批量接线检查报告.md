# TOK-213 第 14 批：pages 根目录剩余可见文案批量接线

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`b3d837230`。

## 范围与结果

- 对根目录 79 个非测试 TSX 文件只读 AST 扫描；其中仅 28 个还有可自动接线候选，因此本批源码文件数为 28，少于通常的 30 个。`ui/src/pages/apps/**` 与其他席位目录均未改。
- `scripts/i18n-codemod.mjs` 对这 28 个文件的 **2,781 条**候选全部接线；清理其中 24 处工具造成的可见多余字面 `t`，删除 3 个对应的无效语言键，并把 `IssueDetail.tsx` 依赖英文标签字面量的类型约束改为稳定的 `kind`，保留测试标识。
- `en.json` 新增 2,778 个有效键，`zh-CN.json` 同步新增；另外 38 个 locale 用既有脚本生成。40 个 locale 各 **9,309 键**，键集相同。`&middot;` 实体转为可渲染的间隔点；没有改默认语言。
- 从既有英文原文完全相同且只有一个简中译法的键中，按明确排除规则复用 **716 条**简中值；排除包含 `organization`、推理档位、孤立词片、含拉丁字母的候选译文及多义译文。另 **2,062 个新键仍为英文回退**，其中有待按页面语境翻译的文案，也有品牌、模型、命令、代码与示例数据。此批只完成接线，不宣称这些页面已全部汉化。
- 再扫同一根目录 79 个文件：AST 可见英文候选 **0**。该口径不覆盖模块级映射、动态拼接、服务端回显以及根目录以外的页面，仍须后续残留清单和浏览器检查。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 9,309 键，键集一致。
- `pnpm --dir ui exec tsc -b`：通过。
- `pnpm vitest run` 定向 10 个测试文件：**258/258** 通过，覆盖语言包、智能体详情、机密、收件箱、搜索、任务详情、公司技能、插件设置、流水线、费用页面。
- `pnpm check:token-gates`：四项 CLEAN；`git diff --check`：通过。
- 本批未起隔离实例，没有浏览器整机复验或两套断点截图；全站验收仍待后续。

## 续做顺序

1. 优先处理真实生产页面及其非生产同名页的英文回退：`CompanySkills(.production).tsx` 共 367、`AgentDetail(.production).tsx` 共 259、`Secrets.tsx` 231、`Pipelines.tsx` 173、`PipelineSettings.tsx` 116。
2. 继续处理 `DesignGuide.tsx` 393 条与其余根目录页面。按语境保留技术标识，逐页列明不译原因。
3. 在本单完成浏览器截图、语言切换与刷新持久化验证，并由独立席按本单判据复核。
