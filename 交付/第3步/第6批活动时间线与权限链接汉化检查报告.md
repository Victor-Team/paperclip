# 第6批活动时间线与权限链接汉化检查报告

日期：2026-09-24

## 范围

本切片处理 `ui/src/pages/apps/app-detail/ActivityPanel.tsx` 的活动时间线固定可见文案：标题与空态、任务关联前缀、测试调用归属、工具调用和审批结果、连接生命周期、Google Sheets 允许列表变更及权限链接标签。

智能体、人员、应用与工具名称、任务标识及相对时间保持为动态输入；事件筛选、排序、跳转、状态颜色与权限逻辑未改。

## 验证

- `node 交付/第3步/sync-locale-keys.mjs` 后，`node scripts/check-i18n-locales.mjs` 通过：40 个 locale 与英语键集一致，共 3,011 键。
- `pnpm vitest run ui/src/pages/apps/app-detail/ActivityPanel.test.tsx ui/src/pages/apps/app-detail/ActivityPanel.render.test.tsx --passWithNoTests`：2 个文件、11 条通过。覆盖既有英文审计句式，以及简体中文“近期活动”和保留动态 `Read Values` 工具名。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json` 仍只报既有未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`；本切片未新增类型错误。

## 已知边界

- `pages/apps` 目录仍有其他组件待处理；本报告不声明目录完成。
- 最小种子环境无法解析应用路由所需的组织前缀，故本切片不将该页面作为浏览器或整机放行证据。
