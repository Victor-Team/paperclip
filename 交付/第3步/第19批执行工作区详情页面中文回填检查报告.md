# TOK-213 第 19 批：执行工作区详情页中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`b4cc67a64`。

## 改动与范围

- 仅修改 `ui/src/pages/ExecutionWorkspaceDetail.tsx` 和 40 个语言包。页面命名空间原有 65 个普通英文回退值已回填中文；另修正 1 条摘要说明译文。6 个保留原文的值是工单/分支示例、`origin/main` 和 Shell 命令示例，均属于可复制的代码或标识，不作为自然语言翻译。
- 新增 52 个英／简中键，补齐标签页、面包屑、例程运行反馈、服务控制反馈、空态、设置提示、表单校验、端口冲突警告、操作阶段及状态、复制反馈和插件缺失时的返回入口。其他 38 个语言包由生成脚本填英文原文。
- 中文界面的日期时间在此页按 `zh-CN` 格式显示；英文界面沿用原 `en-US` 格式。用户输入的工作区/服务名称、路径、命令、仓库地址、服务器错误及日志内容按原值显示。配置服务的默认名称和端口冲突提示已在渲染调用处翻译，纯函数默认英文返回值保持兼容。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages` 对本文件复扫的 AST 可见英文候选为 0。整个 `executionworkspacedetail.general` 命名空间剩余 6 个与英文相同的值，均为上述示例/命令；根目录其他页面和并行席目录不在本批完成范围。
- 40 个 locale 各 9,758 键，相对第 13 批基线新增键的简中英文回退从 1,312 降至 1,247，其中仍含其他页面待译文案及技术原文。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 的 9,758 个键一致。
- `pnpm --dir ui exec tsc -b`：通过。
- `pnpm --dir ui exec vitest run src/pages/ExecutionWorkspaceDetail.test.tsx src/pages/ExecutionWorkspaceDetail.service-ports.test.ts src/pages/ExecutionWorkspaceDetail.provision-status.test.ts src/i18n/locale-validation.test.ts`：4 个文件，28/28 通过。
- `pnpm check:token-gates`：四项 CLEAN；`git diff --check`：通过。

## 浏览器证据与残余

| 页面状态 | 桌面断点 | 窄屏断点 | 与现有设计一致性 |
| --- | --- | --- | --- |
| 任务/服务/例程标签和空态 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 配置表单、端口冲突和错误提示 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 运行记录与预置状态 | 待全量整机复验 | 待全量整机复验 | 待核对 |

本批未启动隔离浏览器实例、未拍两套断点截图；这些状态的截断、重叠、按钮宽度和换行仍待本单全量整机验收。此报告只证明本批源码与语言包门禁，不作为全站放行结论。`ui/src/components/**` 由并行席处理，本批未改动。
