# 第6批断开应用状态与重连提示汉化检查报告

日期：2026-09-24  
范围：`ui/src/pages/apps/AppNotConnected.tsx`、`AppNotConnected.test.tsx`

## 本轮变更

- 将断开应用页的组织选择、应用已删除、连接状态、重连引导与限制、审查空态、权限暂停和操作标签改为按当前语言在渲染期解析。
- 为连接计数添加复数键。应用名称、连接身份、主机地址和服务端返回的连接策略保持动态原值。
- 没有变更查询、连接、重连、授权、路由或访问控制逻辑。
- 新增 23 个英／简中语义键；其余 38 个 locale 由既有键同步脚本补齐英文后备值。40 个 locale 键集现为 2,832 键。

## 验证

- `pnpm vitest run ui/src/pages/apps/AppNotConnected.test.tsx --passWithNoTests`：1 个文件、17 条通过。新增用例验证简体中文“未连接”“需要关注”“重新连接”，以及动态应用名 GitHub 在“请添加可用的 GitHub 密钥以恢复访问权限。”中的插值。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，共 2,832 键。
- `pnpm check:token-gates`：四项 Gate 均为 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仅重现未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`；本切片未新增类型错误。

## 已知边界

- `app-tabs.ts` 的通用页签标签以及其他应用页组件不属于本轮断开状态切片，仍在第 6 批 `ui/src/pages/apps` 的连续范围内。
- 隔离最小种子将 `/apps/review` 解释为缺少的组织前缀，不能从该 URL 进入应用页；这是测试种子路由数据限制，不是本组件的浏览器渲染失败。本报告不作浏览器或整机放行声明。
