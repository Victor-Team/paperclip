# TOK-213 第 5 批新建智能体设置汉化检查报告

## 范围

- 文件：`ui/src/components/new-agent/NewAgentSetup.tsx`、`ui/src/lib/agent-setup-fields.ts`
- 本切片将环境解析、模型／仓库／凭据输入验证、创建与测试回退错误、未使用凭据清理提示、步骤标签、默认模型／本地环境、API 密钥占位与端点提示接入运行期 i18n。
- 五种适配器的登录提示改为语义键，并由设置页按当前语言渲染。
- 提供商品牌、凭据键名、URL 与服务端返回的原始错误保持动态数据原值；认证、测试与创建流程未改。

## 键与译文

- 新增 25 个 `newagentsetup.general.*` 英文键和简体中文译文。
- 运行 `node scripts/generate-i18n-locales.mjs --copy-en-values` 后，40 个 locale 的键集与 `en.json` 完全一致，共 2,747 键。

## 验证

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 定向组件测试 | `pnpm vitest run ui/src/components/new-agent/AgentProviderConnection.test.tsx --passWithNoTests` | 1 个文件、24 条通过 |
| locale 键集 | `node scripts/check-i18n-locales.mjs` | 2,747 键、40 locale 一致 |
| 设计令牌 | `pnpm check:token-gates` | 四项 CLEAN |
| 差异检查 | `git diff --check` | 通过 |
| UI 类型检查 | `pnpm exec tsc --noEmit -p ui/tsconfig.json` | 仅既有未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺 `enableMcpAggregators` |

Vitest 输出中有 jsdom 未实现 `window.scrollTo()` 的提示，但进程以成功状态结束，24 条断言全部通过。

## 后续

本文件仅记录第 5 批的可回滚切片；第 5 批仍需扫描其余组件目录的模块级可见文案。全量范围完成前不宣称浏览器整机放行。
