# TOK-213 第 5 批自动接线检查报告

## 范围

本阶段扫描并接线以下同级组件目录：`access`、`ai-connections`、`chat`、`environment-variables-editor`、`folders`、`issue-properties`、`new-agent`、`routine-sections`、`skill-studio` 与 `icons`。

- 扫描文件：41 个 TSX
- 自动接线：660 条
- 新增键：660 条
- locale：40 个，键集 2,422 条（自动接线阶段）

## 已验证

- `node scripts/check-i18n-locales.mjs` 通过。
- 15 个相关 Vitest 文件、151 条测试通过。
- `pnpm check:token-gates` 四项 Gate 均 CLEAN。
- `git diff --check` 通过。
- UI 类型检查只保留既有的 `InstanceExperimentalSettings.test.tsx:71` fixture 缺字段错误；本阶段没有新增类型错误。

## 模块级映射收尾（本轮）

- `chat/ExternallyConnectedTaskBanner.tsx` 的发布回执状态、文件传输阶段、批次统计、存储恢复和附件回退均改为保存 i18n 键，在组件渲染期取 `t()`。
- `chat/AgentChannelsPanel.tsx` 的提供方身份回退已接入 i18n；提供方品牌名保留原文。
- 新增 55 个键后，`node scripts/check-i18n-locales.mjs` 通过：40 个 locale、2,477 个键一致。
- `pnpm vitest run ui/src/components/chat/AgentChannelsPanel.test.tsx ui/src/components/chat/ExternallyConnectedTaskBanner.test.tsx`：1 个测试文件、36 条通过；Token Gate 与 `git diff --check` 通过。

## 未关闭项

自动扫描器只安全改写 React 组件渲染期的文本和指定属性。它有意不改模块级常量映射、运行状态枚举、数据属性和代码标识，避免在模块初始化期调用 `t()` 破坏语言切换。

本阶段的下一动作仍是在同一第 5 批中完成其余组件文件的模块级可见文案映射扫描；动态数据、路径、用户输入、协议枚举和品牌／代码标识将保留原值并逐项记录豁免理由。此报告不把第 5 批宣称为整体完成。
