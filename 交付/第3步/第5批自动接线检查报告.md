# TOK-213 第 5 批自动接线检查报告

## 范围

本阶段扫描并接线以下同级组件目录：`access`、`ai-connections`、`chat`、`environment-variables-editor`、`folders`、`issue-properties`、`new-agent`、`routine-sections`、`skill-studio` 与 `icons`。

- 扫描文件：41 个 TSX
- 自动接线：660 条
- 新增键：660 条
- locale：40 个，键集 2,422 条（自动接线阶段）

## 已验证

- `node scripts/check-i18n-locales.mjs` 通过。
- 15 个相关 Vitest 文件、151 条测试通过。
- `pnpm check:token-gates` 四项 Gate 均 CLEAN。
- `git diff --check` 通过。
- UI 类型检查只保留既有的 `InstanceExperimentalSettings.test.tsx:71` fixture 缺字段错误；本阶段没有新增类型错误。

## 模块级映射收尾（本轮）

- `chat/ExternallyConnectedTaskBanner.tsx` 的发布回执状态、文件传输阶段、批次统计、存储恢复和附件回退均改为保存 i18n 键，在组件渲染期取 `t()`。
- `chat/AgentChannelsPanel.tsx` 的提供方身份回退已接入 i18n；提供方品牌名保留原文。
- 新增 55 个键后，`node scripts/check-i18n-locales.mjs` 通过：40 个 locale、2,477 个键一致。
- `pnpm vitest run ui/src/components/chat/AgentChannelsPanel.test.tsx ui/src/components/chat/ExternallyConnectedTaskBanner.test.tsx`：1 个测试文件、36 条通过；Token Gate 与 `git diff --check` 通过。

## 例程策略映射收尾（本轮）

- 普通和生产版 `editable-sections` 的模块级策略选项已改为保存语义键，在各自 `DeliverySection` 的渲染期生成翻译后的卡片选项；Webhook 签名方式说明同步接入 i18n。
- 新增 44 个键后，40 个 locale 共有 2,521 个键且一致。生产变体原有的 “Company-wide” 与普通变体的 “Organization-wide” 差异保持不变。
- `pnpm vitest run ui/src/components/routine-sections --passWithNoTests`：1 个测试文件、2 条通过；locale 键集、Token Gate、`git diff --check` 通过。UI 类型检查仍只有既有 fixture 缺字段错误。

## 成员邀请映射收尾（本轮）

- `InvitesSection` 的角色选项、复制回退、创建／撤销回执、权限及加载错误、历史状态和受众回退均改为 i18n 键；访问权限的 API 枚举值未改。
- 新增 33 个键后，40 个 locale 共有 2,554 个键且一致。
- `pnpm vitest run ui/src/components/access/InvitesSection.test.tsx --passWithNoTests`：1 个测试文件、3 条通过；locale 键集、Token Gate、`git diff --check` 通过。UI 类型检查仍只有既有 fixture 缺字段错误。

## 关系控件与技能来源辅助文案收尾（本轮）

- `relation-controls` 的阻塞关系移除、操作、任务标题及展开／收起列表可访问标签，均已在渲染期使用 i18n；议题标识和标题仍是动态数据。
- `SkillProvenance` 的原始技能、项目工作树回退说明与派生来源标签已接入 i18n；技能名、项目名、来源定位信息及 SHA 保持原值。
- 新增 10 个键后，40 个 locale 共有 2,564 个键且一致。
- `pnpm vitest run ui/src/components/issue-properties ui/src/components/skill-studio --passWithNoTests`：5 个测试文件、30 条通过；Token Gate、`git diff --check` 通过。UI 类型检查仍只有既有 fixture 缺字段错误。

## 未关闭项

自动扫描器只安全改写 React 组件渲染期的文本和指定属性。它有意不改模块级常量映射、运行状态枚举、数据属性和代码标识，避免在模块初始化期调用 `t()` 破坏语言切换。

本阶段的下一动作仍是在同一第 5 批中完成其余组件文件的模块级可见文案映射扫描；动态数据、路径、用户输入、协议枚举和品牌／代码标识将保留原值并逐项记录豁免理由。此报告不把第 5 批宣称为整体完成。
