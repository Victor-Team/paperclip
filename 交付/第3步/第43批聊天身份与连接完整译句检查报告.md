# TOK-213 第43批聊天身份与连接完整译句检查报告

日期：2026-09-25。执行席：开发部·前端实现席。代码提交：`fc8e6343ef8a7339cc273140d6b6460528d73ee5`。本批 1 轮，平台 token 用量待平台核算。

## 范围与改动

- 仅改本席 `ui/src/pages/apps/chat/**` 的 2 个生产文件、1 个定向测试，以及共享语言包；不改 `components/**`、共享脚本或默认语言。聊天子目录只有 13 个生产 TSX，本批围绕同一身份与连接提示语境处理，少于 30 个文件。
- `ChatIdentityConfirm.tsx`：确认前的提供商身份警告、确认后的外部身份／公司权限说明，由英文片段拼接改为完整的带参数译句；中文使用术语表的“公司”“权限”。
- `ChatEndpointDetail.tsx`：成员申请指引改为完整 `<Trans>` 译句，保留“请求访问”的加粗；移除连接确认改为含智能体名和提供商名的完整译句。
- 清理 9 个仅供拼句使用的旧碎片键，新增 4 个完整键，净减 5 键；40 个 locale 各 11,347 键且与 `en.json` 一致。其余 38 个语言包由脚本同步英文值，本批未声称它们已有目标语言译文。

## 验证

- `pnpm --dir ui exec vitest run src/pages/apps/chat`：12 个文件、112/112 通过。新增中文渲染测试使用合成身份数据，实际断言确认前整句和确认后含姓名、公司名的整句。
- `pnpm --dir ui exec tsc -b`、`node scripts/check-i18n-locales.mjs`、`pnpm check:token-gates`、`git diff --check` 均通过；Token Gate 四项 CLEAN。四个新键的英／简中插值参数与标签一致。首次测试发现派生语言包保留了旧 `{{action}}`，已针对该键脚本同步 38 包并复测通过。

## 浏览器证据与未决项

本批新增句子分别位于一次性身份链接的确认前／确认后、已配置 Slack 端点的成员访问与移除连接对话框。隔离数据没有有效的一次性身份链接或已配置端点，本批**没有**这些状态的真实路由桌面 1366×768、窄屏 390×844 截图；新增中文测试仅证明合成数据下的组件渲染，不能代替浏览器整机证据。设计稿对照与独立席位复核也未完成。现有第41批连接选择页截图不覆盖这些句子；本批不作为聊天目录或全站放行。

后续仍需用隔离实例覆盖上述状态，检查换行、截断、重叠、按钮宽度、语言切换与刷新持久化；服务端自由文本和其他 `pages/**` 可见英文继续在原单处理。
