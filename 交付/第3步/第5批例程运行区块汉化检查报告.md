# TOK-213 第 5 批例程运行区块汉化检查报告

## 本切片范围

- `ui/src/components/routine-sections/operate-sections.tsx`
- `ui/src/lib/routine-run-display.ts`
- `ui/src/lib/routine-run-display.test.ts`
- `ui/src/i18n/locales/*.json`（40 个 locale）

这是第 5 批的模块级映射收尾切片，不宣称第 5 批整体完成。

## 已接入的可见文案

- 时间范围筛选、筛选条件标签、来源和运行状态均从模块级英文文案改为语义键，并只在组件渲染期调用 `t()`。
- 空状态、操作按钮、运行标题回退和活动分组回退接入 i18n；活动日期按当前 `i18n.language` 格式化。
- 例程运行的已知跳过原因及无失败详情时的回退文案由调用处注入翻译函数；原始失败详情、用户配置值、工单标题和未知服务端枚举保留原值。
- 新增 28 个英文键与简体中文译文；其余 38 个 locale 按英文基准同步，避免键集漂移。

## 验证

- `pnpm vitest run ui/src/lib/routine-run-display.test.ts --passWithNoTests`：1 个文件，12 条通过。
- `node scripts/check-i18n-locales.mjs`：40 个 locale、2,629 个键，与 `en.json` 完全一致。
- `pnpm check:token-gates`：四项 Gate 均 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仍只报既有未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺 `enableMcpAggregators`；本切片未新增类型错误。

## 豁免

| 项目 | 原因 |
| --- | --- |
| `failureReason` | 服务端返回的原始错误详情，不安全地猜译。 |
| 例程变量名和值 | 用户配置与运行数据，保持原样。 |
| 工单标题与未知 `source`/`status` | 动态服务端数据，已知枚举均已映射；未知值原样显示以防语义漂移。 |

## 后续

第 5 批仍需继续扫描其余组件目录的模块级可见映射；全量完成后再做最终浏览器整机复验和独立放行。
