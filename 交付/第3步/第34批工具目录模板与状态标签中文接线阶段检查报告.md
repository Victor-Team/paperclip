# TOK-213 第 34 批：工具目录模板与状态标签中文接线阶段检查

日期：2026-09-25。执行席：开发部·前端实现席。代码提交：`135d6c0d1a3a159c8d0b7a2f033e30b5e660707e`。

## 范围与结果

- 在第 33 批提交 `6020d5963` 的干净工作树上接续；本批修改 `pages/tools/**` 下 5 个生产源文件和 1 个定向测试文件。
- 访问配置的 5 个模板标题及说明、应用工具选择摘要改为按当前语言取值。新增简中切换测试，核对“排除 1 个工具”与“未选择工具”两种摘要；默认英文输出保持原样。
- 粘贴配置流程的安全登录、OAuth 配置、HTTP 草稿与 JSON 校验错误回退及风险提示接入翻译；工具页的风险、连接健康和策略结果徽标按当前语言显示。服务端返回的自由文本和应用名称保持原值。
- `en.json`、`zh-CN.json` 新增 51 个双语键，其余 38 个 locale 由现有生成脚本同步。40 个 locale 各 10,982 键且键集一致。没有改共享脚本或并行席的源文件。

## 验证

- `pnpm exec vitest run ui/src/pages/tools ui/src/i18n`：16 个文件、129/129 通过。
- `pnpm --filter @paperclipai/ui exec tsc -b --pretty false`：通过。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 键集一致，各 10,982 键。
- `pnpm check:token-gates`：四项 CLEAN；`git diff --check`：通过。

## 尚未完成

- `pages/tools/**` 仍有未接线英文。例如 `PasteConfigTab.tsx` 的模块级传输说明、`connection-dialogs.tsx` 的动态回退、`profiles/profile-model.ts` 的“Tools”名称回退与未用的能力标签等。以上是已确认示例，并非穷尽清单；下一批继续逐项核对。
- 本批未起隔离实例，也未做桌面／窄屏截图和独立席位复核；不构成工具目录或全站放行。最终整机验收仍需覆盖展开态、服务端自由文本和实际排版。
