# TOK-213 第 5 批产物状态徽标汉化检查报告

## 本切片范围

- `ui/src/components/issue-properties/IssuePropertiesArtifactsTab.tsx`
- `ui/src/components/issue-properties/IssuePropertiesMarkdownWorkProduct.test.tsx`
- `ui/src/i18n/locales/*.json`（40 个 locale）

## 已完成

- 将工单产物的 `active`、`draft`、`ready_for_review`、`approved`、`merged`、`changes_requested` 和 `failed` 徽标从模块级英文值改为 i18n 键。
- 在渲染期解析键，避免模块初始化时冻结当前语言；简体中文分别显示“进行中”“待审查”“已完成”“已请求修改”“失败”。
- 新增中文语言切换断言，证明 `ready_for_review` 在中文状态下显示“待审查”。
- 新增 5 个英／简中键；其余 38 个 locale 按英文基准同步。

## 验证

- `pnpm vitest run ui/src/components/issue-properties/IssuePropertiesMarkdownWorkProduct.test.tsx ui/src/components/issue-properties/IssuePropertiesDocumentAnnotations.test.tsx --passWithNoTests`：2 个文件、15 条通过。
- `node scripts/check-i18n-locales.mjs`：40 个 locale、2,634 个键完全一致。
- `pnpm check:token-gates`：四项 Gate 均 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仍只报既有 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺 `enableMcpAggregators`；本切片未新增类型错误。

## 后续

本切片是第 5 批的模块级映射收尾之一；仍需完成同批其余可见模块级文案，再进行全量浏览器整机复验。
