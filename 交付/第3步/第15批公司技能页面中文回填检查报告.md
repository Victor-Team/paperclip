# TOK-213 第 15 批：公司技能页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`967fd8a55`。

## 本批改动

- 仅处理本席的 `ui/src/pages/CompanySkills.tsx`、`CompanySkills.production.tsx` 和 40 个语言包；生产变体是实际可进入的页面，两份按同一语义翻译。
- 对两页合计 **357 个已接线键**回填简中。覆盖技能发现、安装、更新、派生、共享、版本比较、来源校验、文件与智能体关联，以及删除确认。`organization` 和 `company` 在租户语境均译“公司”，`agent` 译“智能体”，`skill` 译“技能”；`SKILL.md`、示例短标识和类别示例保留原文。
- AST 接线未覆盖自定义组件属性。已把两页的筛选器标签、空状态、复制反馈、添加到智能体入口、选择器说明等属性文案接入 i18n；新增 **30 个有效键**，其余复用已有键。40 个 locale 现各 **9,339 键**，键集一致。
- 相对第 13 批基线，新增键中与英文相同的简中回退从上批的 2,062 降至 **1,705**。这些剩余项含自然语言及应保留原文的示例/代码，不等于最终可豁免数量。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 9,339 键且键集一致。
- `pnpm --dir ui exec tsc -b`：通过。
- `pnpm vitest run ui/src/pages/CompanySkills.test.tsx ui/src/i18n/locale-validation.test.ts`：2 个文件、36/36 通过。
- `pnpm check:token-gates`：四项 CLEAN；`git diff --check`：通过。

## 剩余风险与后续

- AST 候选为 0 只证明所覆盖的 JSX 文本与标准属性已接线。自定义组件属性、模块级标签、动态 toast 和拼接句子仍可能是可见英文；例如技能页的 `sourceMeta` 管理标签、安装成功/失败 toast 尚未全部处理。
- 本批没有浏览器整机复验或两套断点截图。下一批继续查上述盲区及其他生产页；最终按本单要求补齐页面截图、语言切换/刷新持久化与独立复核。
