# TOK-213 第 27 批：审批详情页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`afd2ee10a`。

## 范围与结果

- 本批源码仅 `ui/src/pages/ApprovalDetail.tsx`，另更新 40 个 locale JSON。没有修改 `pages/apps/**` 或并行席的组件、上下文、功能、适配器与插件目录。根目录其他页面仍在 TOK-213 范围内。
- `approvaldetail.general` 原有 16 个英／简中相同值已全部回填中文；新增 17 个双语键，覆盖面包屑、操作失败回退、审批确认后的入口、删除确认、身份名称、预算停用说明和完整的评论计数句子。本页命名空间英／简中相同值为 0。40 个 locale 各 10,540 键且键集一致。
- 评论时间在简中界面使用中文日期格式。审批正文、工单标题和服务端错误属用户或服务端自由文本，保留原值。页面内的 `/costs` 是导航路径，保留英文。
- 本页 AST 可见英文候选复扫为 0。相对第 13 批基线，全线新增键中英／简中相同值由 222 降至 206；其他页面和技术标识仍待逐项处理。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,540 键且键集一致。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/ApprovalDetail.tsx --json <临时报告>`：只读扫描，候选 0。
- `pnpm --dir ui exec vitest run src/i18n/locale-validation.test.ts`：7/7 通过。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项、`git diff --check`：通过。本批未新增仅重复实现的页面单测。

## 页面 × 状态 × 断点证据

| 页面状态 | 桌面断点 | 窄屏断点 | 与设计一致 |
| --- | --- | --- | --- |
| 待审批、批准、拒绝与修改 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 预算停用与关联工单 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 评论、删除确认与语言切换 | 待全量整机复验 | 待全量整机复验 | 待核对 |

本批没有做浏览器整机、双断点截图或独立席位复核，不能视作全站放行。嵌套组件和服务端自由文本在最终整机验收中核对。
