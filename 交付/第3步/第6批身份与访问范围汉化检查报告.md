# 第 6 批 · 身份与访问范围汉化检查报告

> 工单：TOK-213
> 执行席：开发部·前端实现席
> 范围：`ui/src/pages/apps/app-detail/IdentitiesSection.tsx` 与 `ui/src/i18n/locales/*.json`

## 改动

- 将专用、个人与组织身份的回退说明接入 `t()`。
- 将 GitHub 仓库范围、仓库计数、身份访问范围单选项、成员选择计数与无障碍标签接入 `t()`。
- 将撤销身份的三种标题和四种影响说明接入 `t()`；提供商名称保留为插值值。
- 在 `en.json` 写入英文原文，在 `zh-CN.json` 写入中文译文；运行 `scripts/generate-i18n-locales.mjs` 同步其余语言包。

## 验证

| 项目 | 结果 |
|---|---|
| `node scripts/check-i18n-locales.mjs` | 通过：40 个语言包、4,178 个键与 `en.json` 一致 |
| `pnpm vitest run ui/src/pages/apps/AppDetail.test.tsx ui/src/i18n/locale-validation.test.ts` | 通过：2 个文件、66 项测试 |
| `pnpm check:token-gates` | 通过：四项 Gate 均为 CLEAN |
| 残留英文定向检索 | 通过：本次接线的可见英文没有保留在 `IdentitiesSection.tsx`；动态身份、帐户、仓库名和 URL 维持原值 |
| `git diff --check` | 通过 |
| `pnpm --filter @paperclipai/ui typecheck` | 未通过：既有 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 测试夹具缺少 `enableMcpAggregators`；本切片未新增诊断 |

## 边界与后续

- 未改默认语言、后端、依赖、部署或主工作树；所有改动均在 `feat/anc-zh-cn` 的隔离 worktree 内。
- 本报告覆盖身份与访问范围子面板，不代表全站汉化已完成。
