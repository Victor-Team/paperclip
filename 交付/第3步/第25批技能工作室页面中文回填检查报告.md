# TOK-213 第 25 批：技能工作室页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`25cd16be93d411cc894371ce76d09c57e9d1e8a2`。

## 范围与结果

- 本批源码仅 `ui/src/pages/SkillStudio.tsx`，另更新同页测试和 40 个 locale JSON。没有修改 `pages/apps/**` 或并行席的 `components/context/features/adapters/plugins`。根目录其他页面继续在 TOK-213 范围内。
- `skillstudio.general` 原有 121 个英／简中相同值，回填 107 个自然语言译文；余下 14 项为 `Slug`、`code-review`、路径、`v` 版本前缀及 `{{skillName}}` 等模板占位符，按标识符保留英文。新增 95 个英／简中键，覆盖创建、文件、输入、运行模板、版本、错误回退、确认框和辅助标签。40 个 locale 各 10,458 键，键集一致。
- 面包屑、可见空态、动态 toast、删除确认、可访问性标签和模板运行反馈现在按当前语言生成。此页相对时间在简中模式下用中文格式，英文模式维持原显示。用户填写的名称、文件路径、代码标识及服务端返回的自由文本保持原值。
- 本页 AST 可见英文候选复扫为 0，`skillstudio.general` 共 257 键，其中英／简中相同的仅上述 14 项。全线相对第 13 批基线，新增键中的英／简中相同值由 373 降至 266；这仍混合其他页面待译自然语言与需保留的技术标识，不能当作已完成统计。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,458 键且键集一致。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/SkillStudio.tsx --json <临时报告>`：只读扫描，候选 0。
- `pnpm --dir ui exec vitest run src/pages/SkillStudio.test.tsx src/i18n/locale-validation.test.ts`：2 个文件，21/21 通过，包含切换简中后创建页与面包屑更新的断言。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项、`git diff --check`：通过。首次类型检查定位到 JSX 属性语法及两个组件缺少 `t`，修正后复跑通过。

## 页面 × 状态 × 断点证据

| 页面状态 | 桌面断点 | 窄屏断点 | 与设计一致 |
| --- | --- | --- | --- |
| 创建页、技能切换、文件与输入 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 运行模板、测试历史与错误反馈 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 版本历史、确认框和语言切换 | 待全量整机复验 | 待全量整机复验 | 待核对 |

本批没有做浏览器整机、双断点截图或独立席位复核，不能视作全站放行。共享组件、服务端自由文本和根目录其他页面仍需在 TOK-213 中继续核对。
