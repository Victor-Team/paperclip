# TOK-213 第 5 批例程摘要卡与触发预览汉化检查报告

## 范围

- 改动：`ui/src/components/routine-sections/editable-sections.tsx` 与 `editable-sections.production.tsx`。
- 覆盖触发器／机密／上次运行摘要卡、触发类型选项和下次触发预览。
- 保留服务端运行状态、计数、时间、策略值和时间数据本身；已知状态仅在渲染期映射显示键，未知枚举保持原样。未改运行、排队或触发逻辑。

## 翻译与语言文件

- 新增 35 个英文事实源键与对应简体中文译文。
- 其余 38 个 locale 由 `scripts/generate-i18n-locales.mjs --copy-en-values` 从英文基准同步。
- `node scripts/check-i18n-locales.mjs`：通过，2,707 键、40 locale 键集一致。

## 验证

- `pnpm vitest run ui/src/components/routine-sections/editable-sections.test.tsx ui/src/lib/cron-fires.test.ts --passWithNoTests`：15 条通过。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：未通过，仅有既有且未触及的 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺 `enableMcpAggregators`；本切片未新增相关类型错误。

## 未完成项

第 5 批仍要扫描十个组件目录其他模块级可见映射；全量完成前不宣称浏览器整机放行。
