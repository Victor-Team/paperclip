# 第6批应用页签侧栏与连接提示汉化检查报告

日期：2026-09-24  
范围：`ui/src/pages/apps/app-tabs.ts`、`AppDetail.tsx`、`AppNotConnected.tsx`、`ui/src/components/AppConnectionSidebar.tsx`、`AppConnectionSidebar.production.tsx`

## 本轮变更

- 将应用页“权限／服务／审查”页签改为语义键，并保留纯 helper 的英文默认回退；React 调用方在渲染期传入当前语言翻译函数。
- 常规与 production 侧栏的返回入口、页签标签、审查徽标说明和无数据时的应用兜底名称均接入 i18n。
- 连接详情页的连接成功 toast 及应用面包屑按当前语言渲染；应用名称、连接计数及页签路由键继续作为动态或稳定输入原样保留。
- 新增 10 个英／简中键；其余 38 个 locale 由既有同步脚本补齐英文后备值。40 个 locale 键集现为 2,842 键。

## 验证

- `pnpm vitest run ui/src/components/AppConnectionSidebar.test.tsx ui/src/pages/apps/AppNotConnected.test.tsx ui/src/pages/apps/AppDetail.test.tsx --passWithNoTests`：3 个文件、86 条通过。新增侧栏用例断言简体中文“所有连接器”“审查”“权限”“需要审查”。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，共 2,842 键。
- `pnpm check:token-gates`：四项 Gate 均为 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仅重现未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`；本切片未新增类型错误。

## 已知边界

- `pages/apps` 的其他连接详情与配置组件仍在第 6 批连续范围内；本报告不宣称目录或全站完成。
- 隔离最小种子将 `/apps/review` 解释为缺少的组织前缀，不能从该 URL 进入应用页；这是测试种子路由数据限制，不是本切片的浏览器渲染失败。本报告不作浏览器或整机放行声明。
