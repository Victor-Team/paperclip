# TOK-213 第 23 批：流水线页面中文回填与运行时文案接线

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`8fdcdf4bfe19cc26255ed53c5916f717b2d043fd`。

## 范围与结果

- 本批只改 `ui/src/pages/Pipelines.tsx`、其定向测试和 40 个 locale JSON。源码文件数为 1：根目录此前已整体完成 AST 接线，现按页面语境回填；根目录其他页面仍在 TOK-213 本单范围内。没有修改 `pages/apps/**`、并行席目录或共享脚本。
- `pipelines.general` 原有 173 个英／简中相同值：回填其中 163 项自然语言文案；删除 6 个已经没有引用的英文复数后缀碎片；余下 4 项 `j`、`k`、`Enter`、`a` 是键盘快捷键，保持原样。
- 新增 159 个英／简中键，净增 153 个，40 个 locale 各为 10,248 键。其他 38 个语言包由仓库生成脚本补齐键集；原有非默认值保留。
- 流水线列表的排序、搜索空态、状态数量、相对时间和日期、创建失败提示均使用整句译文。看板中的阶段、重试预检、人工移动、子事项、自动化、产出、复核决定和 toast 也补接 i18n；输出分类与面包屑在语言切换后重新求值。英文单复数与中文计数分别由完整句子处理。
- `Pipelines.tsx` 的 AST 可见英文候选为 0。静态键引用检查中，除 i18next 单复数后缀键与运行时拼接的产出分类键外，未发现缺失。剩余可见英文正则命中为源码注释、键盘按键或写入事项历史的审计原因；服务端传回的自由文本错误仍按原文显示。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,248 键且键集一致。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/Pipelines.tsx --json <临时报告>`：只读扫描，候选 0。
- `pnpm --dir ui exec vitest run src/pages/Pipelines.test.tsx src/i18n/locale-validation.test.ts`：2 个文件，18/18 通过；新增语言切换后“无下游目标事项”标签断言。第一次验证发现生成语言包插值占位符未同步，修正后重跑通过。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项、`git diff --check`：通过。

## 页面 × 状态 × 断点证据

| 页面状态 | 桌面断点 | 窄屏断点 | 与设计一致 |
| --- | --- | --- | --- |
| 流水线列表、排序、搜索与空态 | 待最终整机复验 | 待最终整机复验 | 待核对 |
| 看板、事项详情、阶段移动与重试弹窗 | 待最终整机复验 | 待最终整机复验 | 待核对 |
| 复核队列、批量批准、产出与经验总结 | 待最终整机复验 | 待最终整机复验 | 待核对 |
| 语言切换、刷新后保留中文及相对时间 | 待最终整机复验 | 待最终整机复验 | 待核对 |

本批没有浏览器整机复验、双断点截图或独立席位复核，不能视作全站放行。下一步在同一张单继续 `PipelineSettings.tsx` 等根目录页面中文回填，最终按整机放行判据核对视觉状态和服务端错误。
