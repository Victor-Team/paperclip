# TOK-213 第42批聊天连接合约测试修订检查报告

日期：2026-09-25。执行席：开发部·前端实现席。代码提交：`60cc6bdbdcac03f1f721348554438f3d3c81b9fb`。本批 1 轮，平台 token 用量待平台核算。

## 范围与原因

第41批发现 `ui/src/pages/apps/chat/chat-ui-contract.test.ts` 有 12/13 项失败。这些检查直接从 TSX 源码寻找英文界面句子；对应页面已经接入 `t()` 和 `<Trans i18nKey>`，所以旧断言不再观察当前译文来源。本批只修改该测试文件，不修改生产代码、语言包、共用组件或脚本；属于同一聊天目录的验证返工，单文件批次的原因是测试修订且无新界面文案。

## 修订

- 测试从实际引用的 `t("...")` 和 `i18nKey="..."` 查 `en.json` 的默认英文，把译文附到相应源码位置供原有合约断言核对；没有把整个语言包任意拼到源码末尾。带插值的两处文字由专用检查确认其键确实被该源文件引用、英文值包含预期语义。
- 重试失败 toast 断言改为检查实际 `inbox`／`legacyinbox` i18n 键；“Open + 提供商名”检查保留相邻渲染关系；Telegram 与 Teams 说明按完整译句检查。
- 保留原测试对重试调用、端点状态、权限边界、凭据流程、错误净化和禁止项的检查。未把失败的断言删除或改成恒真条件。

## 验证结果

- 修订前第41批运行：本文件 13 项中 12 项失败。修订后本文件 **13/13 通过**。
- `pnpm --dir ui exec vitest run src/pages/apps/chat`：**12 个测试文件、111/111 通过**。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 与 `git diff --check` 均通过；Token Gate 四项 CLEAN。
- 本批没有改 UI 渲染，沿用第41批真实路由的桌面/窄屏/英文截图；没有把旧截图当作新动态状态证据。

## 限制与后续

本测试是对源码及其引用译文的合约检查，并非邮件/Photon 有数据和失败态的浏览器验证。`pages/apps/chat/**` 的全部动态态、设计稿对照、默认隔离入口及独立席位复核仍未完成；本批不构成聊天目录或全站放行。
