# 第6批应用审批与未验证服务器汉化检查报告

日期：2026-09-24  
范围：`ui/src/pages/apps/AppsReview.tsx`、`UnverifiedServerBadge.tsx`

## 本轮变更

- 审批页的连接器／审查面包屑、无组织空态、页面标题、说明和等待确认标题，均改为由当前语言在渲染期解析。
- 未验证服务器徽标改为渲染期 i18n；主机名继续按动态输入原样显示。
- 新增 6 个英／简中键，40 个 locale 键集同步为 2,809 键。没有变更审批、连接、主机名或路由行为。

## 验证

- `pnpm vitest run ui/src/pages/apps/AppsReview.i18n.test.tsx --passWithNoTests`：1 个文件、2 条通过，断言“审查”“等待您的确认”“未验证服务器”及源码的渲染期键调用。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，2,809 键。
- `pnpm check:token-gates` 与 `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仍仅有未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`。

## 已知边界

- `pnpm vitest run ui/src/pages/apps --passWithNoTests` 在本轮重现两项不属于本切片的既有失败：`AppsConnect.test.tsx` 的连接身份 aria 断言，以及 `chat-ui-contract.test.ts` 对已接入 i18n 的旧英文源码字面量断言；其余 30 个测试文件、432 条测试通过。本切片的专属测试通过。
- 隔离最小种子将 `/apps/review` 解释成缺少的组织前缀，不能从该 URL 进入应用页；这是测试种子路由数据限制，不是组件渲染失败。本轮不把它作为浏览器放行证据。
- `ui/src/pages/apps` 共 37 个非测试 TSX，仍在第 6 批连续处理；本报告只覆盖本轮两处低风险显示文案，未作目录或整机完成声明。
