# TOK-213 第 32 批：工具目录操作反馈中文接线阶段检查

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`80574ff44`。

## 范围与结果

- 在 `ui/src/pages/tools/**` 内接入访问配置详情、访问配置列表、旧版配置页及 MCP 网关页的主要操作反馈、错误提示、空状态、说明和动态句子。
- 访问配置摘要、指派计数、状态、权限来源和能力标签随语言切换更新；新增一项简中切换测试。详情页的新增工具数量、应用名称、断线提示、日期和移除确认采用完整插值句子。
- 新增 97 个英／简中双语键；新键没有英／简中相同值，插值占位符一致。40 个 locale 各 10,746 键且键集一致。默认语言仍为英文。
- 本批未改 `pages/apps/**`、`components/**`、`context/**`、`features/**`、`adapters/**` 或 `plugins/**`。

## 验证

- `pnpm --dir ui exec vitest run src/pages/tools src/i18n/locale-validation.test.ts`：16 个文件，125/125 通过。最后一处复数句子调用调整后，定向复跑 3 个文件、27/27 通过。
- `pnpm --dir ui exec tsc -b`、`node scripts/check-i18n-locales.mjs`、`pnpm check:token-gates` 四项及 `git diff --check`：通过。
- 尚未做浏览器桌面和窄屏截图，也未做独立席位复核。

## 后续验收

工具目录仍有源码直写英文，主要位于 `ProfilesTab.tsx` 的选择器与条目标签、`GatewaysTab.tsx` 的身份及工具数摘要、`AuditTab.tsx`、`SmokeLabTab.tsx`、`connection-dialogs.tsx`、`PasteConfigTab.tsx` 与访问配置向导。下一轮继续同一目录，完成后再逐目录推进。服务端自由文本及嵌套组件状态待最终整机核对。本报告不代表目录或全站放行。
