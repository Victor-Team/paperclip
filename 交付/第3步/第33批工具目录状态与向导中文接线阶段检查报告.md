# TOK-213 第 33 批：工具目录状态与向导中文接线阶段检查

日期：2026-09-25。执行席：开发部·前端实现席。代码提交：`782d0a66b97223ac0654e263dfc0598404610f3c`。

## 范围与结果

- 接续上次取消运行保留的 50 个文件差异，核验后在原分支继续；没有覆盖或撤销已完成的第 32 批。
- `ui/src/pages/tools/**` 共改 12 个界面源文件、3 个定向测试，覆盖审计结果与原因、网关身份与工具摘要、Smoke Lab 状态和反馈、工具页导航、访问配置向导步骤与规则、连接对话框反馈、时间与错误回退。
- `en.json`、`zh-CN.json` 新增 185 个双语键，无旧键删除或旧译文改写；其余 38 个 locale 由现有脚本生成。40 个 locale 各 10,931 键且键集一致。185 个新键中仅 `Smoke Lab` 专有名称英／简中相同。
- 默认语言仍为英文。未改并行席拥有的 `components/**`、`context/**`、`features/**`、`adapters/**`、`plugins/**` 和共享脚本。

## 验证

- `pnpm --dir ui exec vitest run src/pages/tools src/i18n`：16 个文件、128/128 通过；新增简中切换检查覆盖审计、Smoke Lab 和访问配置向导步骤。
- `pnpm --dir ui typecheck`、`node scripts/check-i18n-locales.mjs`、`pnpm check:token-gates`、`git diff --check`：通过。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/tools` 的 AST 可见字面量候选为 0；这**不代表目录没有英文**，因为该扫描器不覆盖模块级标签、函数返回句子和操作反馈。

## 尚未完成

- 目录内仍有直写英文：`profiles/profile-model.ts` 的模板名称、说明和选择摘要；`PasteConfigTab.tsx` 的传输说明、OAuth 回退、JSON 校验反馈和操作风险提示；`connection-dialogs.tsx` 的技术类型与其他动态文案；`shared.tsx` 的安全结果标签；`ProfilesTab.tsx` 的部分实体状态与条目标签。下批继续逐项接线、复扫。上述为已确认示例，不是穷尽清单。
- 本批没有执行隔离实例的桌面／窄屏浏览器检查或独立席位复核，不构成工具目录或全站放行。最终整机验收仍须核对嵌套组件、服务端自由文本和语言切换后的实际布局。
