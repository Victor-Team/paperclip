# TOK-213 第 26 批：实验性设置页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`720414ea9`。

## 范围与结果

- 本批源码仅 `ui/src/pages/InstanceExperimentalSettings.tsx`，另更新同页测试和 40 个 locale JSON。没有修改 `pages/apps/**` 或并行席的 `components/context/features/adapters/plugins`。根目录其他页面仍在 TOK-213 范围内。
- `instanceexperimentalsettings.general` 原有 46 个英／简中相同值中，44 项自然语言回填中文；`Paperclip Runner`、`Smoke Lab` 作为产品名称保留英文。新增 65 个英／简中键，覆盖 27 张功能卡的说明、脚注和开关无障碍标签，以及面包屑、错误回退和完整的工作树运行时间句子。40 个 locale 各 10,523 键，键集一致。
- 实验性功能告警、启用范围、工作树执行的故障关闭说明、开发者工具和旧版功能说明均按当前语言显示。工作树启用时间在简中模式下使用中文日期格式。`pnpm dev:once`、MCP、GitHub、Honeycomb 等命令、协议和服务名保持原文。
- 本页 AST 可见英文候选复扫为 0，命名空间仅上述 2 个产品名仍英／简中相同。相对第 13 批基线，新增键中英／简中相同值由 266 降至 222；此数仍包含其他页面待译自然语言和技术标识。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,523 键且键集一致。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/InstanceExperimentalSettings.tsx --json <临时报告>`：只读扫描，候选 0。
- `pnpm --dir ui exec vitest run src/pages/InstanceExperimentalSettings.test.tsx src/i18n/locale-validation.test.ts`：2 个文件，53/53 通过，包含简中切换后告警、说明、无障碍标签及工作树启用时间句子的断言。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项、`git diff --check`：通过。首次测试发现直接改旧英文键的时间占位符会与其他语言包不一致，已改用新键承载完整句子并复跑通过。

## 页面 × 状态 × 断点证据

| 页面状态 | 桌面断点 | 窄屏断点 | 与设计一致 |
| --- | --- | --- | --- |
| 功能卡、告警和开关 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 工作树运行开启、关闭和故障关闭 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 开发者工具、旧版功能与语言切换 | 待全量整机复验 | 待全量整机复验 | 待核对 |

本批没有做浏览器整机、双断点截图或独立席位复核，不能视作全站放行。服务端返回的自由文本错误仍需最终整机核对。
