# 第 7 批：根目录页面自动接线检查报告

## 范围

- 分支：`feat/anc-zh-cn`
- 目录：`ui/src/pages/*.tsx` 根目录
- 文件：49 个非测试、非 `*.production.tsx` 页面；以 `upstream/master` 相对分支合并基线的改动文件清单排除官方更新触及的文件。
- 明确未触及：`ui/src/components/**`、`context/**`、`features/**`、`adapters/**`、`plugins/**`，避免与 TOK-218 并发冲突。

## 接线结果

- AST 扫描检出 1,282 条可见英文候选；1,281 条在 React 组件内自动接入 `t()`，自动率 99.92%。
- 唯一未自动接线项是 `ui/src/pages/CompanyImport.tsx:188` 的 `&rarr;` 图标实体；它不是可翻译文字，保留为视觉箭头。
- `en.json` 与 `zh-CN.json` 新增 1,281 个键；其余 38 个语言包由 `scripts/generate-i18n-locales.mjs --copy-en-values` 同步。
- 本文件按原文逐条、逐文件查询已验收词表：578 条取得该文件的中文译文；对同一原文的跨文件精确查询又复用 19 条中文译文。

## 未完成的中文译文

`zh-CN.json` 仍有 684 个新增键暂以英文值回退：其中包含 CEO 已规定保留英文的品牌、产品名、模型/版本、协议/HTTP 字段、代码标识符、URL 与推理档位，也包含词表没有中文建议的新页面文案。它们**已经接入 i18n，但不构成本批中文完成声明**；后续须逐条按术语表补足并把真正应译项替换为中文。

## 验证

| 命令 | 结果 |
| --- | --- |
| `node scripts/check-i18n-locales.mjs` | 通过：40 个语言包与 `en.json` 键集一致，共 5,459 键。 |
| `pnpm vitest run ui/src/i18n/locale-validation.test.ts` | 通过：1 文件、7 用例。 |
| `pnpm check:token-gates` | 通过：四项 Gate 均 CLEAN。 |
| `git diff --check` | 通过。 |
| `pnpm --filter @paperclipai/ui typecheck` | 未通过，但仅复现既有 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 测试夹具缺 `enableMcpAggregators`；未出现本批文件诊断。 |

## 术语边界

保留英文的候选包括 `Auto`、`Low`、`Medium`、`High`、`X-High`、产品/品牌名称、版本、协议字段、环境变量、路径、URL、文件扩展名、代码标识符与服务端枚举。动态用户数据和服务端错误仍按运行时原值显示，未被写入翻译文件。
