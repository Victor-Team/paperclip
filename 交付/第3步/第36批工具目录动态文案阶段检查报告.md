# TOK-213 第36批：工具目录动态文案阶段检查

日期：2026-09-25。执行席：开发部·前端实现席。代码提交：`37b99bb12`。

## 范围与结果

- 在第31—35批补证后的干净分支接续；仅改本席 `ui/src/pages/tools/**` 的 3 个生产源文件、2 个定向测试文件，以及 40 个语言包。新增 13 个英／简中键，其他 38 个语言包由现有脚本生成；每个 locale 现有 11,008 键，键集一致，默认英文未改。
- 访问配置允许列表的明确指定／模式匹配／默认规则数量，向导的默认配置替换、智能体已有配置与无配置提示、未命名草稿及分配失败提示，改为完整的当前语言文案。保留智能体名、配置名和分配目标标识原值。
- Smoke Lab 仅按两个已知服务 ID 翻译服务名；仅当 OAuth 服务返回约定的固定说明时翻译该说明。运行时错误详情、未知服务与服务 URL 保留原值，避免把诊断信息误译成正常状态。未修改服务端逻辑或并行席源文件。
- 本批生产文件少于30个，是既有 `pages/tools/**` 目录的残余动态文案补线；不把它当成一个已完成的新目录批次。

## 验证

- `pnpm exec vitest run ui/src/pages/tools ui/src/i18n`：16个测试文件，133/133通过；新增语言切换断言覆盖访问配置分配上下文和 Smoke Lab 服务名、固定说明。
- `pnpm --filter @paperclipai/ui exec tsc -b --pretty false`、`node scripts/check-i18n-locales.mjs`、`pnpm check:token-gates` 四项及 `git diff --check` 通过。i18next 运行时插值核对：英文 ` · 1 explicit`／` · 2 via pattern`，简中 ` · 1 项明确指定`／` · 2 项由模式匹配`。
- 隔离服务以 `PAPERCLIP_RUNNER_BINARY=/home/yuanjian/dev/ANC/packages/paperclip-runner/dist/bin/paperclip-runnerd pnpm dev:once` 启动，3200/54339 真实监听。Chrome 实际渲染 Smoke Lab 的 1366×768 与 390×844 简中页面，两断点均显示中文服务名和固定说明，`html[lang]=zh-CN`，页面 `scrollWidth=clientWidth`。截图和异常索引在 `交付/TOK-213/浏览器证据/第36批/证据索引.md`。服务取证后已关闭。

## 尚未完成

- 访问配置向导的有数据、替换默认配置及失败态，旧版允许列表的有数据态，缺桌面和窄屏真实浏览器截图；对应设计稿未取得，独立席位复核未完成。第36批只证明已覆盖的代码和 Smoke Lab 服务卡状态，不作目录或全站放行。
- 共用侧栏的英文属于并行席源文件；Smoke Lab 运行步骤、动态错误与服务端自由文本还需在后续整机核对时区分固定系统文案和原始数据。隔离克隆的一条图标资源请求为404，本批没有改该资源。
- 默认隔离入口 `pnpm paperclipai run` 的 `cli/install.json` doctor 问题仍在；本次 `pnpm dev:once` 浏览器证据不构成默认入口通过。下一批继续本席独占的 `ui/src/pages/**` 剩余页面，汇总后交独立席复核。
