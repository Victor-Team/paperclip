# TOK-213 第 35 批：工具目录动态标签中文接线阶段检查

日期：2026-09-25。执行席：开发部·前端实现席。代码提交：`1800c2126`。

## 范围与结果

- 接续第 34 批的干净分支；本批修改 `ui/src/pages/tools/**` 的 5 个生产源文件和 2 个定向测试文件，新增 10 个英／简中键。其余 38 个语言包由既有生成脚本同步，40 个 locale 各 10,992 键且键集一致。
- 粘贴配置卡的传输位置与密钥数量改为完整句子译文；中文切换后立即重绘，英文默认文案与原样一致。保留配置文件字段名、密钥名和服务端警告原值。
- 旧版访问配置界面的状态、目标类型、选择器、允许／拒绝及默认操作标签按当前语言显示；无应用名称时工具分组的回退名也随语言切换更新。已有用户名称、标识符和应用名称不翻译。
- 工具页签的无匹配回退文案改为译文键。未修改共享脚本、并行席源文件或默认语言。

## 验证

- `pnpm exec vitest run ui/src/pages/tools ui/src/i18n`：16 个测试文件、131/131 通过；新增测试覆盖粘贴配置卡在英文和简中之间切换，以及无应用名称时的工具分组回退。
- `pnpm --filter @paperclipai/ui exec tsc -b --pretty false`：通过。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 键集与 `en.json` 一致，每个 10,992 键。
- `pnpm check:token-gates`：四项 CLEAN；`git diff --check`：通过。
- 按隔离工作树的默认入口运行 `pnpm paperclipai run`：doctor 11 项通过、1 项失败；失败项为隔离 home 的 `cli/install.json` 缺失。进程在启动服务器前退出，3200/54339 均未监听。本席没有运行 `paperclipai install`，也没有触碰正式实例。

## 尚未完成

- 工具目录的静态和动态可见文案尚未完成穷尽审计；`ProfilesTab.tsx` 的运行时来源摘要、`SmokeLabTab.tsx` 的服务状态及服务端自由文本仍须按“系统文案／用户或服务数据”逐项核对。
- 因上述 doctor 失败，本批未取得桌面与窄屏截图；展开态及独立席位复核也未做。视觉与全站汉化不能据此放行。修复隔离启动路径后须补浏览器证据；下一轮可继续同一目录的源码接线。
