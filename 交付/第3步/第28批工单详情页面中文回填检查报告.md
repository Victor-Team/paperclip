# TOK-213 第 28 批：工单详情页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`df3b81473`。

## 范围与结果

- 本批源码仅 `ui/src/pages/IssueDetail.tsx`，另更新 40 个 locale JSON。没有修改 `pages/apps/**` 或并行席的组件、上下文、功能、适配器与插件目录。根目录其他页面仍在 TOK-213 范围内。
- `issuedetail.general` 原有 40 个英／简中相同值中，36 个自然语言值回填中文；4 个已无引用的英文复数碎片键删除。新增 20 个双语键，净增 16 键。40 个 locale 各 10,556 键且键集一致，本页命名空间英／简中相同值为 0。
- 成本摘要中的输入、输出、缓存、运行次数与工单数改为完整句子键，避免中文界面出现 `in/out/cached/run/task` 拼接。面包屑、收件箱操作、无障碍标签、看守工单说明、预览与状态更新错误、计划决定文本、身份默认名称均按当前语言显示。`Markdown` 与 `g f` 为标准名称和快捷键，保留原文。
- 本页 AST 可见英文候选复扫为 0。相对第 13 批基线，全线新增键中英／简中相同值由 206 降至 166；其他页面及技术标识仍待逐项处理。AST 扫描不能覆盖服务端自由文本和所有运行时状态，不据此宣称本页或全站完整放行。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,556 键且键集一致。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/IssueDetail.tsx --json <临时报告>`：只读扫描，可见英文候选 0。
- `pnpm --dir ui exec vitest run src/pages/IssueDetail.test.tsx src/i18n/locale-validation.test.ts`：2 个文件，125/125 通过。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项、`git diff --check`：通过。

## 页面 × 状态 × 断点证据

| 页面状态 | 桌面断点 | 窄屏断点 | 与设计一致 |
| --- | --- | --- | --- |
| 工单信息、收件箱操作与侧边栏 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 成本摘要、子工单与看守提示 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 计划决定、预览错误与语言切换 | 待全量整机复验 | 待全量整机复验 | 待核对 |

本批没有做浏览器整机、双断点截图或独立席位复核，不能视作全站放行。嵌套组件、服务端自由文本和未展开的动态状态在最终整机验收中核对。
