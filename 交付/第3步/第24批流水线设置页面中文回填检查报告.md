# TOK-213 第 24 批：流水线设置页面中文回填

日期：2026-09-24。执行席：开发部·前端实现席。代码提交：`4f1d20622af22b22015e594dea440507ee47c35b`。

## 范围与结果

- 本批源码仅 `ui/src/pages/PipelineSettings.tsx`，另更新同页定向测试和 40 个 locale JSON。根目录在第 14 批已完成 AST 批量接线，现逐页按语境回填；根目录其余页面仍在 TOK-213 范围内。没有修改 `pages/apps/**` 或并行席的 `components/context/features/adapters/plugins`。
- 将 `pipelinesettings.general` 中 116 个旧英／简中相同值回填中文。新增 130 个双语键，删除 15 个无引用英文碎片键，净增 115；40 个 locale 各有 10,363 键，键集一致。本页命名空间 244 个键，英／简中相同值为 0。
- 阶段导航、阶段类型和工作区模式从模块常量改为键映射，在渲染时翻译；继承字段和自动化变量的标签随语言切换重新生成。补齐动态 toast、错误回退、空态、表单提示、选择器、面包屑、计数、时间、辅助说明和确认框。拆分摘要的中文按完整语序生成，默认 `piece` 在显示文案中使用「拆分项」，保留实际配置值。
- 本页 AST 可见英文候选复扫为 0；静态键集和插值校验通过。`PipelineSettings.test.ts` 新增语言从英文切换到简中后祖先标签更新的断言。
- 范围内的已知待核文案：活动事件主体由共享 `ui/src/lib/pipeline-item-detail.ts` 的 `formatPipelineItemEvent` 生成，当前仍可能输出英文；`StageHealthWarnings` 等并行席组件和服务端返回的自由文本错误也需最终整机核对。代码/路径、用户创建的阶段名和字段名按原值显示。本批不宣称此页面所有嵌套状态或全站已汉化。

## 验证

- `node scripts/check-i18n-locales.mjs`：40 个 locale 各 10,363 键且键集一致。
- `node scripts/i18n-codemod.mjs --directory ui/src/pages/PipelineSettings.tsx --json <临时报告>`：只读扫描，候选 0。
- `pnpm --dir ui exec vitest run src/pages/PipelineSettings.test.ts src/i18n/locale-validation.test.ts`：2 个文件，15/15 通过。
- `pnpm --dir ui exec tsc -b`、`pnpm check:token-gates` 四项、`git diff --check`：通过。类型检查首次发现活动时间值可能为 `Date`，修正并复跑通过。
- 以第 13 批提交 `37c00a65b` 为基线，当前新增键中英／简中相同值为 373；其中既有品牌、键盘快捷键、代码示例，也有其他页面待译自然语言，不能把该数直接当作未完成译文总量。

## 页面 × 状态 × 断点证据

| 页面状态 | 桌面断点 | 窄屏断点 | 与设计一致 |
| --- | --- | --- | --- |
| 阶段导航、类型、工作区模式与表单 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 拆分设置、变量辅助、警告与活动 | 待全量整机复验 | 待全量整机复验 | 待核对 |
| 删除/归档确认、保存反馈和语言切换 | 待全量整机复验 | 待全量整机复验 | 待核对 |

本批未做浏览器整机、双断点截图或独立席位复核，不能视作全站放行。下一批继续根目录剩余页面语境回填，最后按本单判据做整机视觉与状态核对。
