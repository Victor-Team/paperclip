# TOK-213 第 5 批例程编辑选择器与空状态汉化检查报告

## 范围

- 改动：`ui/src/components/routine-sections/editable-sections.tsx` 与 `editable-sections.production.tsx`。
- 覆盖负责人／项目选择器的空值、搜索和无结果提示，触发器／变量空状态，Webhook URL 复制标签，以及四项例程策略控件的无障碍标签。
- 负责人、项目、机密值、Webhook URL、例程状态和 API 数据保持动态原值；未改变选择、复制、触发或策略逻辑。

## 翻译与语言文件

- 两套真实界面变体各新增 13 个英文事实源键和简体中文译文。
- 其余 38 个 locale 由 `scripts/generate-i18n-locales.mjs --copy-en-values` 从英文基准同步。
- `node scripts/check-i18n-locales.mjs`：通过，2,672 键、40 locale 键集一致。

## 验证

- `pnpm vitest run ui/src/components/routine-sections/editable-sections.test.tsx --passWithNoTests`：3 条通过；新增中文触发器空状态断言。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：未通过，仅有既有且未触及的 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺 `enableMcpAggregators`；本切片未新增相关类型错误。

## 未完成项

第 5 批仍要扫描例程摘要卡、触发类型选项和其他组件目录中的模块级可见映射；全量完成前不宣称浏览器整机放行。
