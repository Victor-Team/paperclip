# 第5批十目录可见文案收口审计报告

日期：2026-09-24  
范围：`ui/src/components/access`、`ai-connections`、`chat`、`environment-variables-editor`、`folders`、`issue-properties`、`new-agent`、`routine-sections`、`skill-studio`、`icons`

## 审计方法与结论

- 使用仓库现有 `scripts/i18n-codemod.mjs` 的 TypeScript AST 扫描模式，检查 JSX 文本、组件内可见表达式及 `placeholder`、`title`、`aria-label`、`alt`、`label` 属性。
- 扫描 41 个非测试 TSX 文件，检测到的待接线可见英文文本为 **0**；自动接线率为 **100%（0 / 0）**。原始 JSON 审计结果保存在本运行暂存目录，未作为源码产物提交。
- 对 `chat/board-send-draft.ts` 的唯一英文异常作了调用路径核对：它只作为内部解析失败信号抛出，`ExternallyConnectedTaskBanner` 已在用户可见处渲染既有 `externallyconnectedtaskbanner.general.saveddeliveryidentitycouldnotberead` 键。因此未将内部异常字符串错误地当成用户显示文案改写。
- 提供商品牌、用户／服务端动态值、ID、URL、路径、原始错误、枚举传输值和内部控制标识均不在本次收口改写范围；此前各切片的渲染期映射继续生效。

## 验证

- `node scripts/i18n-codemod.mjs`（十目录扫描）：41 个 TSX，0 个候选可见文本。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，2,803 键。
- `pnpm check:token-gates`：四项 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仍只报既有、未触及的 `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` fixture 缺少 `enableMcpAggregators`；本审计未引入类型错误。
- 隔离 worktree 服务已在 `127.0.0.1:3200` 启动并返回健康状态。其最小种子工作区仍为 `workspace_company_not_configured`，因此本报告不把健康检查视为浏览器整机证据，也不作浏览器放行声明。

## 边界与后续

本报告证明第 5 批十个组件目录的 AST 可识别可见英文残留已收口，不替代真实浏览器流程验证。后续仍需在隔离实例完成公司 bootstrap 后，按语言切换、刷新持久化和中文排版的实际页面状态补齐浏览器证据；在此之前，TOK-213 保持 `in_progress`。
