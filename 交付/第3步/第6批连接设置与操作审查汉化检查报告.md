# 第6批连接设置与操作审查汉化检查报告

日期：2026-09-24  
范围：`ui/src/pages/apps/app-detail/SetupPanel.tsx`、`SetupPanel.i18n.test.tsx`

## 本轮变更

- 连接设置页的智能体／操作导航、PostHog 访问范围、Google Sheets 白名单和新操作审查的固定标签、引导、校验反馈与操作均改为当前语言在渲染期解析。
- 应用名、工具名、工具描述、Google Sheets URL／ID、PostHog 配置值和动态权限摘要保持动态原值；白名单、配置更新与新操作审查逻辑没有变更。
- 新增 38 个英／简中键（含新操作数量复数键）；其余 38 个 locale 由既有同步脚本补齐英文后备值。40 个 locale 键集现为 2,978 键。

## 验证

- `pnpm vitest run ui/src/pages/apps/app-detail/SetupPanel.i18n.test.tsx --passWithNoTests`：1 个文件、1 条通过。用例断言简体中文新操作审查控件与动态操作名称共存。
- `node scripts/check-i18n-locales.mjs`：40 个 locale 与 `en.json` 完全同键，共 2,978 键。
- `pnpm check:token-gates`：四项 Gate 均为 CLEAN。
- `git diff --check`：通过。
- `pnpm exec tsc --noEmit -p ui/tsconfig.json`：仅重现未触及 fixture `ui/src/pages/InstanceExperimentalSettings.test.tsx:71` 缺少 `enableMcpAggregators`；本切片未新增类型错误。

## 已知边界

- `pages/apps` 的其他连接详情和配置组件仍在第 6 批连续范围内；本报告不宣称目录或全站完成。
- 本轮未进行应用页浏览器验证：隔离最小种子的应用路由受组织前缀数据限制。本报告不作浏览器或整机放行声明。
