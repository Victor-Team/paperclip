# ANC 第 2 步 · 词表验收记录

> **版本**：v0.1.1
> **状态**：🔍 评审中
> **负责人**：产品研发部·产品验收席（agent `13436d07-f626-4f37-aa3b-e265441aa6c2`）
> **日期**：2026-09-16
> **最后更新**：2026-09-16 / 产品研发部·产品验收席
> **密级**：🔒 Internal
> **主文档引用**：[词表.md](./词表.md)（待核产物，本席只读、未改一行）
> **依赖规范**：TOK-166 正文四条判据；CEO 评论 `641f1733` 追加的第五项（只出数、不判过/不过）

本席按 TOK-166 四条判据**机械核对**。第五项翻译质量普查按 CEO 要求独立复算，**只出数、不下过/不过**。本文件是 TOK-166 唯一主交付物。本席没有改 `词表.md`，没有改任何源码。

---

## 0. 本心跳怎么读 CEO 评论

唤醒包到达时 TOK-166 仍 `blocked`，payload 写着依赖 TOK-164。现场 `GET` 显示 TOK-164 已于 `2026-09-16T10:46:35.706Z` 置 `done`（唤醒包生成于 10:45:42，阻塞信息过期）。本席把本单改 `in_progress`，按原四条验收，并把第五项并进同一份记录。

第五项改变的动作：多做一次独立复算与三类抽样；**不改变**四条的过/不过口径；**不**由本席决定词表能否进第 3 步。

---

## 1. 开工硬门

| # | 检查 | 结果 |
|---|---|---|
| 1 | `交付/第2步/词表.md` 存在且能读 | 是（10730 行，1469970 字节） |
| 2 | TOK-164 状态确为 `done` | 是（id `cb3e974c-8fc7-4a37-8c6d-905188345c78`，`completedAt` `2026-09-16T10:46:35.706Z`） |
| 3 | 本席未改词表 | 是。本席只往 `交付/第2步/词表验收记录.md` 写入 |

硬门通过，开始验收。

---

## 2. 四条判据（逐条结论）

### 判据 1 · 扫描范围 —— **通过**

词表 §一声明：

- 根目录 `ui/src`，全部 `.tsx` / `.ts`
- 排除 `*.test.*`、`fixtures/`、`storybook/`，以及 `server/` / `cli/` / `packages/`
- 实际扫描源文件 **1020**；含提取文案的源文件 **528**

本席独立计数（`ui/src` 下 `.ts`/`.tsx`）：

| 口径 | 文件数 |
|---|---|
| 全部 `.ts`/`.tsx` | 1623 |
| 去掉 `*.test.*` 与路径含 `fixtures/` | **1020** |
| 再去掉路径含 `storybook/` | **1020**（storybook 文件数 = 0） |

声明的 1020 与本席「判据范围」计数一致。`storybook/` 写了排除原因（文档与组件演示），且该排除没有真正缩小集合。含提取文案的文件数本席按 `出现位置` 去重得 **528**，与声明一致。全部 `出现位置` 均在 `ui/src/` 下，0 条落在测试或 `fixtures/`。

范围没有比判据更窄。通过。

### 判据 2 · 四字段齐全 + 抽样回查 `file:line` —— **通过**

解析口径（与 CEO 第五项对照用的口径相同，本席独立实现）：数据行 = 行首 `|` 且第三列同时含 `/` 和 `:`。

| 项 | 本席数 |
|---|---|
| 主表数据行 | **10675** |
| 列数 ≠ 4 | 0 |
| 英文 / 建议中文 / 出现位置 / 建议键名 任一空 | 0 |
| `出现位置` 不合 `path:line` | 0 |

全表机械回查（打开声明的文件、读声明的行，看该行是否含完整英文原文）：

| 结果 | 条数 |
|---|---|
| 该行含完整原文 | 10288 |
| 未整行命中 | 387 |

387 条未整行命中的拆分：折行/邻近行拼得上 360；前缀折行 7；HTML 实体（`&rsquo;` `&middot;` `&rsaquo;` `&times;` `&rarr;` 等）20。**没有**指到不存在的文件，也没有行号越界。未见「文件对、行号指到无关代码」。

下面 10 条是本席亲自打开文件看过的抽样（跨 adapters / pages / components / lib / features / hooks / context / plugins）：

| # | 英文原文 | 词表 `file:line` | 打开后该行 | 对得上 |
|---|---|---|---|---|
| 1 | Enable Chrome | `ui/src/adapters/claude-local/config-fields.tsx:232` | `label="Enable Chrome"` | 是 |
| 2 | Teams Developer Portal · Apps | `ui/src/pages/apps/chat/ChatEndpointSetup.tsx:1005` | `<strong>Teams Developer Portal · Apps</strong>: select{" "}` | 是 |
| 3 | Retry preview | `ui/src/components/TaskTreeControls.tsx:146` | `Retry preview` | 是 |
| 4 | Database provisioning failed | `ui/src/lib/workspace-access-state.ts:91` | `title: "Database provisioning failed",` | 是 |
| 5 | service/my-connector or scl_… | `ui/src/features/connections/ConnectionSetupFlow.tsx:3577` | `placeholder="service/my-connector or scl_…"` | 是 |
| 6 | Couldn't retry now | `ui/src/hooks/useRetryNowMutation.ts:68` | `title: "Couldn't retry now",` | 是 |
| 7 | Open chat connections | `ui/src/context/LiveUpdatesProvider.tsx:1095` | `action: { label: "Open chat connections", href: "/apps" },` | 是 |
| 8 | : failed to render | `ui/src/plugins/launchers.tsx:420` | `{this.props.launcher.pluginDisplayName}: failed to render` | 是 |
| 9 | Each user | `ui/src/pages/Secrets.tsx:2408` | `<UserSecretChip label="Each user" />` | 是 |
| 10 | In Teams Developer Portal, create an app, …（长段） | `ui/src/pages/apps/chat/ChatEndpointSetup.tsx:923` | `In Teams Developer Portal, create an app, add a bot with the same` | **是（折行首段）**。原文从 923 行起，924 行续 `Application ID, then apply the manifest settings shown below.` |

10 条都能对上。通过。

### 判据 3 · 已接入 3 条不重抽 —— **通过**

词表 §二单列 3 条，并写明已在 `zh-CN.json`。本席打开 `ui/src/i18n/locales/zh-CN.json` 与调用点：

| 英文原文 | zh-CN.json | 调用点 |
|---|---|---|
| Create your first organization | `app.noCompanies.title` = 创建您的第一家公司（第 4 行） | `ui/src/App.tsx:709` |
| Get started by creating an organization. | `app.noCompanies.description` = 通过创建公司开始。（第 5 行） | `ui/src/App.tsx:712` |
| New Organization | `app.noCompanies.newCompany` = 新公司（第 6 行） | `ui/src/App.tsx:732`（§二把调用点写成了 `en.json:6`，那是英文本地化定义，不是调用；**不影响**「已单列且未进主表」） |

主表 10675 条里这 3 条英文原文命中 **0**。通过。

### 判据 4 · 已跟踪文件改动 —— **通过（归因后）**

本席当场命令与原文：

```
$ git -C /home/yuanjian/dev/ANC status --porcelain
 M cli/src/__tests__/managed-install-check.test.ts
 M cli/src/checks/managed-install-check.ts
?? .claude/skills/agent-browser--1ffa5880cd/
?? .claude/skills/company-operating-rules--bda6de4669/
?? .claude/skills/design-critique--9e260a5f5d/
?? .claude/skills/github-pr-workflow--43d4ea0362/
?? .claude/skills/issue-triage--527450ed81/
?? .claude/skills/paperclip-capsules--fa1d1c93d9/
?? .claude/skills/qa-acceptance--243dd1d7bf/
?? .claude/skills/release-announcement--457569c95d/
?? .claude/skills/simplified-english--9876d8956d/
?? .claude/skills/task-planning--b6efa0a667/
?? .claude/skills/wireframe--9113fc45b9/
?? Agents.md
?? 交付/
```

```
$ git -C /home/yuanjian/dev/ANC diff --stat
 cli/src/__tests__/managed-install-check.test.ts | 17 +++++++++++++++++
 cli/src/checks/managed-install-check.ts         |  5 ++++-
 2 files changed, 21 insertions(+), 1 deletion(-)
```

`HEAD` 仍为 `bd51f157e9aade37f2f4602eda238fe51405a0c2`。`ui/` 无 diff。

逐条归因：

| porcelain | 归属 |
|---|---|
| `M cli/src/checks/managed-install-check.ts` | 第 0 项 TOK-163 授权改动面（开工方案 A1；`交付/第2步/第0项修复记录.md` 已记） |
| `M cli/src/__tests__/managed-install-check.test.ts` | 同上第 0 项测试，不是词表抽取 |
| `?? 交付/` | 本步产品包，判据允许 |
| `?? .claude/skills/*`、`?? Agents.md` | 席位环境注入，未跟踪；第 1 步验收记录 AC-05 已定性，不判执行方越界 |

字面读「除 `交付/` 外 porcelain 必须空」会把本条打成不通过。开工方案明确 A（第 0 项）与 C（词表）**并行**，porcelain 不可能在 A1 未提交时干净。本席不把并行第 0 项的合法 diff 算到 TOK-164 头上。词表抽取自己的已跟踪改动面是空的。通过。

若决策人坚持字面全局干净，可改判本条不过——那是把 A 的 diff 记到 C。本席不采用。

---

## 3. 建议键名风格（不构成不通过）

- 10675 条全部是 3 段点分（`a.b.c`），沿用了 `app.noCompanies.title` 那种分层。
- 26 条最后一段以数字开头（`agentconfigform.general.1Code`、`timeline.general.7Days`、`designguide.general.120001InfoAgentStarted` 等），与「段首小写字母」的常见 i18n 习惯不一致。
- 第一段大小写不统一：有 `adapters.claudeLocal.*`，也有 `devrestartbanner.general.*`、`issuedetail.general.*`。

意见：第 3 步接入前建议把数字开头的 26 条改成合法段名；第一段是否统一 camelCase 可随现有 `en.json` 风格走。不单独构成不通过。

---

## 4. 第五项 · 翻译质量普查（只出数，不判过/不过）

**不许改词表。本项不是过/不过。词表能否进第 3 步由 CEO 定。**

### 4.1 与 CEO 对照的机械量

本席独立按「行首 `|` 且第三列含 `/` 和 `:`」解析，**分母与三个机械指标与 CEO 完全相同**：

| 指标 | CEO | 本席 | 差 |
|---|---|---|---|
| 数据行 | 10675 | 10675 | 0 |
| 原文与译文完全相同 | 4545（42.6%） | 4545（42.6%） | 0 |
| 译文里一个汉字都没有 | 4545（42.6%） | 4545（42.6%） | 0 |
| 中英混杂（有汉字且残留 ≥3 字母英文单词） | 3762（35.2%） | 3762（35.2%） | 0 |

「完全相同」集合 = 「无汉字」集合。解析口径一致，数字对得上。

词表 §一自称 **10678** 条，指的是对照总表，**不是**「主表 10675 + §二已接入 3 条」。v0.1 把两个碰巧都是 3 的集合加在一起，归因是错的。本席 2026-09-16 11:07 独立复跑后，差集就是下面 3 行——英文原文含转义竖线 `\|`，按单字符 `|` 分列时第 3 列不再是 `path:line`，被本席/CEO 口径排除：

| 英文原文（截断） | 出现位置 |
|---|---|
| `pkill -f vite \|\| true` | `ui/src/pages/ExecutionWorkspaceDetail.tsx:1256` |
| `pkill -f vite \|\| true` | `ui/src/pages/ProjectWorkspaceDetail.tsx:630` |
| `The long-form run page with the \`Nice \| Raw\` toggle …` | `ui/src/pages/RunTranscriptUxLab.tsx:28` |

按 `' | '`（空格-竖线-空格）分列得 **10678**，与自称一致。第五项三个百分比用 10675 或 10678 作分母，都仍是 42.6%，结论不变。第 3 步读这张表请用 `' | '` 分列，否则这 3 条会错位或被丢掉。

### 4.2 三类缺陷（本席口径）

从「原文=译文」的 4545 里把专有名词择出。本席**收紧**口径：只认产品名 / 适配器名 / 模型 id / 代码占位符（`Claude Code`、`Codex`、`gemini --acp`、`workspace-123`）。**不**把 `Created`、`Buttons`、`Hint:`、`Live` 这种 Title Case 界面词算专有名词——那些该译。

| 类 | 条数 | 占 10675 | 算不算缺陷 |
|---|---|---|---|
| 1. 专有名词，保留英文是对的 | **114** | 1.1% | 不算缺陷 |
| 2. 真正漏译 | **4431** | 41.5% | 是 |
| 3. 半截替换 | **3723** | 34.9% | 是 |
| （其余：有汉字且残留英文只剩产品名，或已译完） | 2407 | 22.5% | 本项不评 |

验算：114 + 4431 = 4545（全部「未译」行）。3723 比 CEO 的「中英混杂 3762」少 39：那 39 条残留英文只是 `Paperclip` / `GitHub` / `API` 这类产品或缩写，本席不打成半截替换。

**对 CEO「42.6% 会虚高」的回答：** 按收紧口径，专有名词只占 1.1 个百分点。42.6% 几乎都是真漏译，不是被产品名撑起来的。若把 Title Case 短词也算专有名词，能择出约 1764 条、漏译降到约 2781——本席抽看后认为那会把 `Created` / `Buttons` 错当成保留英文，故不采用。

### 4.3 类 1 样本（专有名词 · 不算缺陷）· 10 条

| 英文原文 | 建议中文 | 出现位置 |
|---|---|---|
| ACPX (retired) | ACPX (retired) | `ui/src/adapters/adapter-display-registry.ts:64` |
| Claude Code | Claude Code | `ui/src/adapters/adapter-display-registry.ts:72` |
| Codex | Codex | `ui/src/adapters/adapter-display-registry.ts:78` |
| Paperclip Runner | Paperclip Runner | `ui/src/adapters/adapter-display-registry.ts:84` |
| Gemini CLI | Gemini CLI | `ui/src/adapters/adapter-display-registry.ts:90` |
| Grok Build | Grok Build | `ui/src/adapters/adapter-display-registry.ts:95` |
| Kimi Code | Kimi Code | `ui/src/adapters/adapter-display-registry.ts:100` |
| OpenCode | OpenCode | `ui/src/adapters/adapter-display-registry.ts:116` |
| Cursor Cloud | Cursor Cloud | `ui/src/adapters/adapter-display-registry.ts:131` |
| OpenCode 1.18.29 | OpenCode 1.18.29 | `ui/src/adapters/codex-local/config-fields.tsx:232` |

### 4.4 类 2 样本（真正漏译）· 10 条

| 英文原文 | 建议中文 | 出现位置 |
|---|---|---|
| Revoke | Revoke | `ui/src/components/access/InvitesSection.tsx:345` |
| Enable Chrome | Enable Chrome | `ui/src/adapters/claude-local/config-fields.tsx:232` |
| Process | Process | `ui/src/adapters/adapter-display-registry.ts:144` |
| Undo | Undo | `ui/src/components/environment-variables-editor/Row.tsx:545` |
| Advanced | Advanced | `ui/src/features/connections/ConnectionSetupFlow.tsx:3602` |
| Version | Version | `ui/src/pages/CompanySkills.production.tsx:2302` |
| Execution engine | Execution engine | `ui/src/adapters/claude-local/config-fields.tsx:99` |
| Human only | Human only | `ui/src/components/InteractionGovernancePanel.tsx:254` |
| Send to channel | Send to channel | `ui/src/components/chat/ExternallyConnectedTaskBanner.tsx:515` |
| Recorded | Recorded | `ui/src/pages/apps/gateways/panels/GatewayActivityPanel.tsx:20` |

### 4.5 类 3 样本（半截替换）· 10 条

| 英文原文 | 建议中文 | 出现位置 |
|---|---|---|
| Retired standalone ACPX adapter | Retired standalone ACPX 适配器 | `ui/src/adapters/adapter-display-registry.ts:65` |
| External gateway adapter | External 网关 适配器 | `ui/src/adapters/adapter-display-registry.ts:137` |
| : failed to render | : 失败 to render | `ui/src/plugins/launchers.tsx:420` |
| Search responsible... | 搜索responsible... | `ui/src/components/IssueChatThread.tsx:5649` |
| Edit profile | 编辑profile | `ui/src/components/SidebarAccountMenu.production.tsx:189` |
| Editing a copy | Editing a 复制 | `ui/src/components/skill-studio/ForkSkillDialog.tsx:114` |
| Loading policies… | 加载中 policies… | `ui/src/pages/AgentToolsTab.tsx:716` |
| Skills moved | 技能 moved | `ui/src/pages/CompanySkills.tsx:4890` |
| Starter tasks | Starter 任务 | `ui/src/pages/TeamCatalog.tsx:1833` |
| Issue a token before copying a snippet; the full | 工单 a 令牌 before copying a snippet; the full | `ui/src/pages/apps/gateways/ConnectClientDialog.tsx:254` |

半截替换里还能看到逐词误义：`Issue` → `工单`、`copy` → `复制`。这是机器替换痕迹，不是翻译。

### 4.6 本项不评、但看见的误译

`Open chat connections` → `未结对话连接`（`ui/src/context/LiveUpdatesProvider.tsx:1095`）。汉字齐、无残留英文，机械上不算半截替换，但 `Open` 被读成「未结」。第五项没设「错译」类，记一笔给 CEO，不展开。

---

## 5. 总结论

| 项 | 结论 |
|---|---|
| 判据 1 扫描范围 | **通过** |
| 判据 2 四字段 + 抽样回查 | **通过** |
| 判据 3 已接入 3 条不重抽 | **通过** |
| 判据 4 已跟踪文件 | **通过（归因后）** |
| 第五项翻译质量 | **只出数，不判过/不过**。真正漏译 4431（41.5%），半截替换 3723（34.9%），专有名词 114（1.1%） |
| 词表能否进第 3 步 | **本席不定。数字在 §4。** |

形式四条通过，本单置 `done`。译文要不要返工、进不进第 3 步，等 CEO。

本单实际运行次数：**1**（本次心跳。指派时因 TOK-164 未完成被 skipped，不计入执行）。

---

## 变更日志（Changelog）

| 日期 | 修改人 | 修改内容 | 影响范围 | 关联文档 |
|---|---|---|---|---|
| 2026-09-16 | 产品研发部·产品验收席 | 更正：10678≠10675+已接入3条；差集是 3 条含 `\|` 的主表行 | §4.1 | 词表.md |
| 2026-09-16 | 产品研发部·产品验收席 | 初稿：四条机械结论 + 第五项独立复算 | 全文 | 词表.md、TOK-166、CEO 评论 641f1733 |
