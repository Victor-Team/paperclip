# TOK-164 词表 · 开发部负责人独立机械核查

- 核查人：开发部负责人（`e49c1f9a-94cb-44b8-a80b-93ad76a725f2`）
- 核查时间：2026-09-16
- 运行：`46a5e872-5689-4a9c-9473-e78676e640d3`
- 被核对象：`/home/yuanjian/dev/ANC/交付/第2步/词表.md`（10730 行，1469970 字节）
- 性质：**本席自查，不是复核。** 复核由 TOK-166 产品验收席做。本文件只提供一手证据。

---

## 一、结论

**判据 1、3、4 通过；判据 2 实质不通过。** 建议 TOK-166 判不通过，退回开发部·前端实现席补译。

---

## 二、逐条核对

### 判据 1 · 扫描范围 —— 通过

实跑：

```
find ui/src -type f \( -name '*.tsx' -o -name '*.ts' \) \
  | grep -v '\.test\.' | grep -v '/fixtures/' | grep -v '/storybook/' | wc -l
→ 1020
```

与词表第一节自称的 1020 完全一致。含测试与夹具的全量为 1623，排除规则生效。范围已在词表开头写明。

### 判据 2 · 四字段齐全 —— 位置字段通过，**中文字段实质不通过**

**位置字段（通过）**：随机抽 40 条回查源文件，37 条整串精确命中；3 条 MISS 经逐条查看，均为多行 JSX 文案（行号指向文案起始行）或 HTML 实体已解码（`&quot;` → `"`、`&amp;` → `&`），实质可回查。结论：`file:line` 可靠。

**中文字段（不通过）**：

| 指标 | 条数 | 占比 |
|---|---|---|
| 总条目 | 10678 | — |
| 「建议中文」与英文原文**完全相同** | 4547 | 42.6% |
| 「建议中文」无任何多字节字符（纯 ASCII） | 4311 | 40.4% |
| 英文原文 ≥3 词且完全未译 | **1203** | 占 ≥3 词条目的 29.6% |
| 英文原文 ≥5 词且完全未译 | **462** | 占 ≥5 词条目的 23.4% |

4547 条里确有一批是专有名词（Codex、Cursor、Pi、Claude Code），不译是对的。但 **≥3 词 1203 条、≥5 词 462 条无法用专有名词解释**，是整句界面文案原样照抄。实例：

```
| No steps recorded for this run.       | No steps recorded for this run.       | ui/src/pages/tools/SmokeLabTab.tsx:446            |
| This vote is always saved locally.    | This vote is always saved locally.    | ui/src/components/IssueChatThread.tsx:2933        |
| Could not load more results           | Could not load more results           | ui/src/pages/secrets/ImportFromVaultDialog.tsx:555|
| Raw YAML is the source of truth ...   | ……与左列逐字相同……                     | ui/src/components/FrontmatterPanel.tsx:728        |
| Refunds, offsets, and credit returns  | ……与左列逐字相同……                     | ui/src/pages/Costs.tsx:140                        |
```

判据 2 原文：「每条四字段齐全，**缺任一字段该条不计**」。一个名为「建议中文」的字段里一个汉字都没有，按平实读法即该字段为空。所以这不是判据没覆盖的新问题，而是**判据 2 项下至少 1203 条不计**。

**附带问题：已译的部分大量是逐词词典替换，不是翻译。** 含汉字又含拉丁字母的条目 3858 条，抽样可见：

```
Name the pipeline and add a short description.  ==>  名称 the 流水线 and add a short 描述.
Search all agents…                              ==>  搜索all agents…
Removal failed                                  ==>  Removal失败
Keep one follow-up run queued while an active run is still working.
                                                ==>  Keep one follow-up run queued while an 活跃 run is still working.
```

这类条目字段非空，但接进界面就是病句，同样不可用。

### 判据 3 · 已接入 3 条不重复抽 —— 通过

在总表（第 52 行起）实 grep 三条原文，命中数均为 **0**。第二节单列归档存在。自证属实。

### 判据 4 · 不改已跟踪文件 —— 通过

```
$ git status --porcelain
 M cli/src/__tests__/managed-install-check.test.ts     ← 并行单 TOK-163 所有
 M cli/src/checks/managed-install-check.ts             ← 并行单 TOK-163 所有
?? .claude/skills/…  ?? Agents.md  ?? 交付/
```

`ui/src/i18n/locales/zh-CN.json` 与全部 `.tsx` 干净。产物只落 `交付/第2步/`。

---

## 三、判据之外记录（不构成退回理由）

- 以标点开头的残句 **89 条（0.8%）**，如 `. Disable one plugin or change its route.`、`, add the two RSC` —— JSX 文本节点被按片段切开。
- 疑似命令行/代码片段 **16 条**，如 `pkill -f vite || true`（`ui/src/pages/ExecutionWorkspaceDetail.tsx:1256`），不是界面文案。

两项占比都小，第 3 步接线时剔除即可，不单独退回。

---

## 四、为什么这条必须拦在第 3 步之前

本步交付物的唯一用途是喂给第 3 步「把词表接进界面」。若按字面判据放行，第 3 步照表填进 `zh-CN.json` 之后，**这 1203 条在界面上仍然是英文**，而那时错误已经落在已跟踪文件里，回滚成本远高于现在补译。

## 五、建议的最小返工范围

只补译，不重扫：范围、位置、键名三列全部保留不动，只重写「建议中文」一列中 `en == zh` 且英文 ≥3 词的 1203 条，外加抽查修正逐词替换产生的病句。文件仍只落 `交付/第2步/词表.md`，不碰任何已跟踪文件。

---

## 六、复现命令

```bash
F=/home/yuanjian/dev/ANC/交付/第2步/词表.md
tail -n +52 "$F" | LC_ALL=C awk -F' \\| ' '
NF>=4 { en=$1; sub(/^\| /,"",en); zh=$2;
  gsub(/^ +| +$/,"",en); gsub(/^ +| +$/,"",zh);
  n=split(en, w, / +/); total++;
  if (en==zh) same++;
  if (n>=3) { m++; if (en==zh) mSame++ }
  if (n>=5) { l++; if (en==zh) lSame++ }
}
END{ printf "总条目 %d ／ 完全未译 %d (%.1f%%) ／ >=3词未译 %d ／ >=5词未译 %d\n",
     total, same, same*100/total, mSame, lSame }'
```
