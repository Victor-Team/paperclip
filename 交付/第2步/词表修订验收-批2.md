# ANC 第 2 步 · 词表修订验收（批 2 ＋ 批 1 残差）

> **版本**：v0.1
> **状态**：🔍 评审中
> **负责人**：产品研发部·产品验收席（agent `13436d07-f626-4f37-aa3b-e265441aa6c2`）
> **日期**：2026-09-16
> **最后更新**：2026-09-16 / 产品研发部·产品验收席
> **密级**：🔒 Internal
> **主文档引用**：[词表-修订-批2.md](./词表-修订-批2.md)、[词表-修订-批1.md](./词表-修订-批1.md)、[术语表.md](./术语表.md)（被验产物，本席只读、未改一行）
> **依赖规范**：[词表修订验收-批1.md](./词表修订验收-批1.md)；[词表验收记录.md](./词表验收记录.md) §4 专有名词 114 项；TOK-171 甲／乙两节分开判

本席按 TOK-171 两节判据**独立复算**。本文件是 TOK-171 唯一主交付物。本席没有改 `词表-修订-批2.md`、`词表-修订-批1.md`、`术语表.md`、`词表.md`，没有改任何源码。

甲、乙**分别给结论**，不合成一个总结论。乙节任一项不过则乙节不过。

---

## 0. 开工硬门

| # | 检查 | 结果 |
|---|---|---|
| 1 | 被验产物可读 | 是。`词表-修订-批2.md` 存在；`词表-修订-批1.md` 顶部有 TOK-170 变更日志；`术语表.md` 含 `organization` 行 |
| 2 | 本席未改被验文件 | 是。本席只往 `交付/第2步/词表修订验收-批2.md` 写入 |
| 3 | 独立口径，不沿用自称 | 是。覆盖、五列、键名、原文=译文、拉丁残留、多义拆行、批 1 残差均本席脚本 + 打开 `file:line` 复算 |

硬门通过，开始验收。

复算脚本与中间表在本 run scratch（`PAPERCLIP_RUN_SCRATCH_DIR`），不入库。随机 30 条种子：`171001`；多义抽 5 组种子：`171004`。均可复现。

---

## 1. 甲、形式项

### 甲.1 覆盖出现恰好 2 次的全部唯一串 —— **通过**

本席口径与 TOK-166 / TOK-169 相同：行首 `|`，按单字符 `|` 分列，第三列同时含 `/` 和 `:` → 主表 **10675** 行。再按 `' | '` 分列得 **10678**（差集仍是含转义竖线的 3 行）。

| 口径 | 主表行 | 唯一英文串 | 出现恰好 2 次的唯一串 | 覆盖行数 | 词数（空白分词） |
|---|---|---|---|---|---|
| 单字符 `\|`（本席主口径） | **10675** | 6569 | **960** | **1920** | **3318** |
| `' \| '` 分列 | 10678 | 6571 | **961** | 1922 | 3323 |
| 被验方自称 | 10675 | — | 960 | 1920 | 3318 |

主口径下，恰好 2 次唯一串集合与自称 **960 / 1920 / 3318 零误差**。修订表唯一英文 **960**，相对应有集合：缺失 **0**、多余 **0**。出现次数列按拆行求和后与本席频次 **960/960 相符**。

Method 2 多出的 1 个是 `pkill -f vite \|\| true`（含转义竖线，主口径不计）。被验方已在 §1.2 写明。不记缺陷。

### 甲.2 五列齐全；拆行每行有 `file:line`；键名未另造 —— **通过（另造键 1 条，记残差）**

批 2 对照总表五列：`英文原文 / 修订中文 / 出现次数 / 出现位置 file:line / 建议 i18n 键名`。

| 检查 | 本席数 |
|---|---|
| 数据行 | **983**（937 单义 + 23×2 拆行 = 983，与自称相符） |
| 列数 ≠ 5 | **0** |
| 五列任一空 | **0** |
| 拆行组 | **23** 组 / **46** 行（与自称相符） |
| 拆行行缺 `file:line` | **0** |
| 键名在原 `词表.md` 该英文的键名集合中 | **982/983** |
| 另造键 | **1**：`hidden` 的一行写成 `tools.shared.labelHidden`，原表是 `tools.shared.hidden` |

另造 1 条是命名变体，不是新造命名空间。五列与 `file:line` 硬条件已满足。本席不因此翻甲.2，但数字记在这里：另造 **1/983**。

### 甲.3 批 1 文件顶部有变更日志，并据此回查五条 —— **通过**

`词表-修订-批1.md` 顶部「变更日志（Changelog · TOK-170 残差修复与多义拆行）」10 行，覆盖 `Created` / `Tokens` / `Open` / `Organization` / `Failed run`。主表对应拆行：`Created` 3 行（17+1+2=20）、`Tokens` 2 行（4+5=9）、`Open` 3 行（15+1+1=17）、`Organization` 1 行（13）、`Failed run` 1 行（3）。频次与原表一致。调用点是否对得上见乙.6，不在本条重复判。

### 甲.4 `git status --porcelain`：词表 / zh-CN.json / .tsx 未改 —— **通过（归因后）**

当场命令（`HEAD` = `bd51f157e9aade37f2f4602eda238fe51405a0c2`）：

```
 M cli/src/__tests__/managed-install-check.test.ts
 M cli/src/checks/managed-install-check.ts
?? .claude/skills/…
?? Agents.md
?? 交付/
```

| 对象 | 结论 |
|---|---|
| `交付/第2步/词表.md` | 未跟踪；mtime 02:54，早于批 2 修订表 05:16。本席未改，被验方也未改 |
| `ui/src/i18n/locales/zh-CN.json` | porcelain 空；mtime 2026-09-15 21:49，早于本批 |
| 任何 `.tsx` | porcelain 空 |
| `cli/src/checks/managed-install-check.ts` 及其测试 | 第 0 项 TOK-163 授权改动面，与本批无关 |
| `?? .claude/skills/*`、`?? Agents.md` | 席位环境注入；第 1 步 AC-05 已定性 |

字面读「除 `交付/` 外 porcelain 必须空」会把本条打成不通过。脏的不是本批。本席沿用 TOK-169 甲.3 / TOK-166 甲.4 的归因。

若决策人坚持字面全局干净，可改判本条不过。本席不采用。

### 甲节结论：**通过**

---

## 2. 乙、可用性项

### 乙.1 「原文 = 译文」= 0（白名单除外） —— **通过（违规 0）**

| 项 | 本席数 |
|---|---|
| 修订表「原文 = 译文」 | **16** |
| 白名单除外后的违规未译 | **0** |

16 条全文（相对 §4 的 114 项）：

| 英文原文 | 本席归类 | 相对 114 |
|---|---|---|
| Grok Build / Kimi Code / Hermes / Pi / Cursor Cloud / Paperclip | 产品 / 适配器名 | 114 口径内（批 1 注：频次 &lt;3 时遵同规则保留；本批恰好覆盖） |
| bash ./scripts/provision-worktree.sh 等 3 条 | 代码命令示例 | 代码占位符口径 |
| TOOLS.md / PAP-123 / stderr / KB | 文件名 / 工单占位 / Unix 流 / 单位 | 代码占位符口径 |
| `border-violet-500/15 …` | 抽取混入的 CSS | 与批 1 `text-muted-foreground` 同类 |
| `×` / `›` | 标点 | 非英文单词 |

**Title Case 短词口子**：16 条里 `Hermes` / `Pi` / `Paperclip` 是产品名，不是 `Created` / `Buttons` 那类界面词。本批**没有**把 Title Case 短界面词重新放回白名单。相对 114 的新增均属产品名 / 代码占位符 / 标点，理由站得住。无回潮。

### 乙.2 译文含 ≥3 字母拉丁单词 = 0（白名单除外） —— **通过（违规 0）**

正则：译文中 `[A-Za-z]{3,}`。

| 项 | 本席数 |
|---|---|
| 译文含 ≥3 字母拉丁单词 | **74** |
| 白名单除外后仍违规（半截替换） | **0** |

74 条残留全部是白名单专名或其组成部分（`Paperclip` / `GitHub` / `API` / `URL` / `YAML` / `JSON` / `Markdown` / `Hermes` / `Kimi` / `OpenClaw` / `Chrome` / `Slack` / `Vercel` / `Acme Robotics` / `skills.sh` / `SKILL.md` / `AGENTS.md` / `TOOLS.md` / `Webhook` / `KiB` / `True`/`False` 代码字面量）。

本批 16 项白名单未逐条列出 `Webhook` / `Slack` / `Vercel` / `Chrome` / `AGENTS.md` / `SKILL.md` / `KiB`，但它们属于 114 口径的产品名 / 文件名 / 单位 / 协议名，与批 1 接受 `GitHub` / `API` 嵌入译文的做法一致。`True` / `False` 已译成「是 (True)」「否 (False)」，括号内保留代码字面量，不是漏译。

**没有**「External 网关 适配器」「: 失败 to render」「搜索responsible」这类半截替换。

### 乙.3 术语一致（≥10 个）＋ organization / company 统一 —— **通过**

本批恰好 2 次集不含多数单字术语精确行（它们在批 1 ≥3 集）。本席抽 12 个在批内、且写在 [术语表.md](./术语表.md) 核心表中的术语（含短语），核「同一英文是否只映射一个中文」+「与术语表一致」。

| 英文 | 本席见到的中文 | 与术语表 |
|---|---|---|
| workspace / Workspace… | 工作区（精确行 1；短语：无工作区、打开工作区、工作区 ID…） | 一致 |
| Skill | 技能 | 一致 |
| secret | 机密 | 一致 |
| Document | 文档 | 一致 |
| organization / organizations | 公司（精确短语：暂无公司、创建公司、未选择公司、请选择公司以查看此页面、您已加入公司…） | 一致；**0 处译「组织」** |
| heartbeat / heartbeats | 心跳（按固定间隔触发心跳；因预算超额受阻的智能体心跳） | 一致 |
| adapter / adapters | 适配器（指令包仅适用于本地适配器；适配器结果 JSON；适配器：） | 一致 |
| gateway / gateways | 网关（无法更新网关、网关已创建、请选择公司以管理网关） | 一致 |
| board | 董事会（此智能体正在等待董事会审批） | 一致（本批无看板语境） |
| agent / agents | 智能体（批准智能体、委派智能体） | 一致 |
| task / tasks | 任务（搜索任务、暂无任务）；精确行 `task` 标「碎片·不独立翻译」 | 一致；碎片处理符合 TOK-169 建议 |
| token / Token | 令牌（签发令牌、令牌名称、令牌已吊销）；词元（每日词元上限） | 一致（术语表允许鉴权「令牌」/ 计费「词元」） |

`Issue token` → `签发令牌`（动词 issue，未套「工单」），符合术语表「个别词汇作动词时不机械套用名词术语」。

术语表已补 `organization` → `公司`，与 `company` 同行。批 2 短语全用「公司」，0「组织」。禁止的「代理 / 议题 / 工作空间 / 技巧」未作为这些术语的译法出现。

（附见、不翻本项：`Item` / `Item body` / `Item not found.` 一律译「项目」，与术语表 `project` = 项目撞名。见乙.5。）

### 乙.4 多义映射可回查（本单重点） —— **不通过**

拆行规则**落地了**，不是 0 组。质量未达标。

| 口径 | 本席数 |
|---|---|
| 修订表同一英文映射多个中文 | **23 组 / 46 行**（与自称 23 组完全相符） |
| 拆行索引表行 | 46（与对照表拆行一一对应） |
| 每行自有 `file:line` | 46/46 |
| 种子 `171004` 抽 5 组打开调用点 | Diff / Replay / Try / Wait / used → **5/5 对得上** |
| 因全批只有 23 组，本席通读其余调用点 | **至少 2 组对不上** |
| 近义硬拆（意思相同仍拆） | **约 4 组** |
| 应拆未拆（抽到的） | **1 组（`Yes`）** |

**抽 5 组（对得上）：**

| 英文 | 拆出的中文 | 打开后的语境 | 判断 |
|---|---|---|---|
| Diff | 比对差异 / 差异 | `CaseRevisionRail.tsx:221` 版本对比按钮；`task-chat-states.ts:102` 工作产物状态 `label: "Diff"` | 对得上 |
| Replay | 回放会话 / 重试消息 | `TaskChatLab.tsx:226` 带 `playToken` / Speed 的回放按钮；`ChatEndpointDetail.tsx:1100` `replay.mutate(item)` + `RefreshCw` | 对得上 |
| Try | 尝试搜索 / 立即体验 | `Search.tsx:620` `Try <code>status:todo</code>`；`ChatEndpointSetup.tsx:1613` `Try {agentName} in {provider}` | 对得上（「尝试搜索」偏满，仍读得通） |
| Wait | 等待时间 / 等待 | `PipelineSettings.tsx:2504` 步骤等待字段；`StatusCardSettingsForm.tsx:164` 轮询间隔前的 `Wait` | 对得上 |
| used | 调用了 / 使用了 | `GatewayActivityPanel.tsx:96` `{actor} used {tool}`；`AuditTab.tsx:225` `{who} used {action}` | 对得上 |

**通读后对不上的 2 组（本项不过的直接证据）：**

1. **`To` →「至」/「发往」**。两处都是日期截止标签，不是两个意思。
   - `DecisionDateChips.tsx:64`：日期范围 `To` →「至」，对。
   - `AuditFeed.tsx:621`：`<span>To</span>` 后接 `<Input type="date" aria-label="To date">`，**也是日期截止**。译「发往」对不上。拆行索引自称「审计事件目标实体前缀」与代码不符——这处没有被真正打开看过，或看了写错。
2. **`attention` →「需关注」/「待处理」**。
   - `DesignGuide.tsx:2175`：正文里的 `<code>attention</code>` 状态标识符，应保留或标碎片，译「需关注」对不上。
   - `Connections.tsx:388`：`connections need attention` →「待处理」勉强可读；与上一行的「需关注」反了——标识符不该译，句子里的 attention 反而更该「需关注」。

**近义硬拆（不算对不上，但掺水，约 4 组）：** `pass` 通过/已通过、`fail` 未通过/失败、`pattern` 通配模式/通配符模式、`Health` 健康状态/健康状况。另 `Bind` 两处都是 Bind 按钮（对话框确认 vs 工具栏），拆成「确认绑定」/「绑定」是粒度过细，不是多义。

**应拆未拆 1 组：** `Yes` → 一律「确认」。`IssueAttachmentsSection.tsx:311` 是确认按钮，「确认」对；`ConnectionSetupFlow.tsx:3043` 是 Yes/No 分段选项，应为「是」。

**差多少（给决策人）：** 拆行数量 23/23 覆盖了本批实际一词多译，规则已落地。质量：23 组里 **2 组调用点对不上（8.7%）**；约 4 组近义硬拆；漏拆 `Yes` 1 组。不是「应有约 20 组只做了 3 组」那种没落地，是「做了 23 组，其中 2 组拆错」。

对不上即本项不过。

### 乙.5 随机抽 30 条通读（不通顺 / 不合语境 ≤2） —— **通过（2 / 30）**

种子 `171001`，`random.sample(range(983), 30)`，按下标升序。判「不通顺或不合界面语境」必须举原文和问题。

| # | 英文原文 | 修订中文 | 本席判断 |
|---|---|---|---|
| 1 | Diff | 差异 | 过 |
| 2 | Write a comment… | 撰写评论… | 过 |
| 3 | Complex value — edit in YAML. | 复杂结构值——请在 YAML 中编辑。 | 过 |
| 4 | Discard draft and start new | 舍弃草稿并新建 | 过 |
| 5 | Search tasks | 搜索任务 | 过 |
| 6 | Item body | 项目正文 | **不合语境**。见下 |
| 7 | bash ./scripts/teardown-worktree.sh | （原样） | 过（白名单） |
| 8 | Search exact frame | 搜索精确帧 | 过（协议检查器的 frame） |
| 9 | Source task | 来源任务 | 过 |
| 10 | No organization selected | 未选择公司 | 过（术语已统一） |
| 11 | Each invite link is single-use. … | 每个邀请链接仅限单次使用。… | 过 |
| 12 | Variables are auto-detected from | 变量将自动检测自 | 过（原文也是未完句，接 `{{placeholders}}`） |
| 13 | No enabled schedule trigger to preview. … | 没有已启用的定时触发器可供预览。… | 过 |
| 14 | Delegated agents | 委派智能体 | 过 |
| 15 | Client ID | 客户端 ID | 过 |
| 16 | Install failed | 安装失败 | 过 |
| 17 | This agent is pending board approval and cannot be invoked yet. | 此智能体正在等待董事会审批，暂无法调用。 | 过 |
| 18 | Approve agent | 批准智能体 | 过 |
| 19 | File contents | 文件内容 | 过 |
| 20 | Revoked Keys | 已吊销密钥 | 过 |
| 21 | Text only — no scripts, no binaries, no assets. | 仅限纯文本 — 无脚本、无二进制文件、无外部资源。 | 过 |
| 22 | Installed from the app-shipped skills catalog. … | 安装自应用自带的技能目录。… | 过 |
| 23 | Estimated debits that are not yet invoice-authoritative | 尚未生成权威账单的预估借记支出 | 过 |
| 24 | Paused projects | 已暂停项目 | 过 |
| 25 | attention | 需关注 | **不合语境**。见下 |
| 26 | Message from inviter | 来自邀请人的留言 | 过 |
| 27 | Fit chart to screen | 图表自适应屏幕 | 过 |
| 28 | Add file | 添加文件 | 过 |
| 29 | Bind | 绑定 | 过 |
| 30 | Could not delete | 无法删除 | 过 |

**计入的 2 条：**

1. **Item body → 项目正文**（及同批 `Item` → `项目`、`Item not found.` → `未找到项目。`）。`PipelineItemBodyDocument.tsx:409` 的 aria-label 是流水线条目正文，不是 `project`。术语表 `project` = 项目。一词两实体撞名。
2. **attention → 需关注**。即乙.4 的 DesignGuide `<code>attention</code>` 标识符。抽 30 抽到了拆行里对不上的那一行。

2 ≤ 2，本项通过。

### 乙.6 批 1 五条残差是否真修好 —— **通过**

本席按原 `词表.md` 独立枚举调用点（不靠变更日志点名），再打开源码。

| 英文 | 原表出现 | 修订拆行 | 打开后的结论 |
|---|---|---|---|
| Created | **20** | 创建时间 17 + 已创建数 1 + 已创建 2 | **修好**。17 处是 `PropertyRow` / 表头 / `Created {formatDate…}` 时间标签；`UserProfile.tsx:74` 配 `formatNumber(stats.createdIssues)` →「已创建数」；`ImportFromVaultDialog.tsx:154` 是 `<CheckCircle2 /> Created` 状态徽标、`:1366` 是结果分组标题 →「已创建」 |
| Tokens | **9** | 词元 4 + 令牌 5 | **修好**。`DesignGuide.tsx:1793` 表头 Model/Tokens/Cost、`IssueDetail.tsx:2667,2700` `Tokens {formatTokens(...)}`、`UserProfile.tsx:70` 配 `formatTokens` →「词元」；网关 5 处带 `KeyRound` / 网关令牌列表 →「令牌」 |
| Open | **17** | 打开 15 + 未结 1 + 开启 1 | **修好**。按钮 / `sr-only`「Open {identifier}」/ 外链 / `title="Open"`（`Pipelines.tsx:4021` 配 `openPath`）为动词「打开」；`UserProfile.tsx:76` 配 `formatNumber(stats.assignedOpenIssues)` →「未结」；`RichWorkProductCard.tsx:48` `kind === "pull_request" && status === "open"` →「开启」。`TaskSidePanel.tsx:512` 的 `{ id: "open", label: "Open" }` 是侧栏「当前打开」分组，归入「打开」可接受 |
| Organization | **13** | 公司 13 | **修好**。侧栏分区、技能树、邀请页、机密 `kind === "company"`、审计筛选 `value: "company."` 全部是租户实体，与术语表一致。0 处仍译「组织」 |
| Failed run | **3** | 运行失败 3 | **修好**。`attention.ts:60` 标签、`Inbox.tsx:436`、`LegacyInbox.tsx:371` 均是失败运行提示，「运行失败」比「失败运行」顺 |

五条残差在批 1 表内按调用点拆开，且打开后对得上。本项通过。

### 乙节结论：**不通过**

乙.1 违规 0；乙.2 半截替换 0；乙.3 12 个术语一致且 organization=公司；乙.5 不合语境 2/30（≤2）；乙.6 五条残差均修好。**乙.4 不通过**（23 组里 2 组调用点对不上），乙节不过。

---

## 3. 给决策人的数字（长尾怎么办）

本席按要求两节分开判。下面的数字供决定「只出现 1 次的 4742 个串」还走不走第 2 步。

| 指标 | 批 1（≥3 次，626 串） | 本批（恰好 2 次，960 串 / 1920 行 / 3318 词） |
|---|---|---|
| 真漏译（原文=译文，白名单除外） | 0 | **0** |
| 半截替换 | 0 | **0** |
| 随机通读不合语境 | 2/30 | **2/30** |
| 多义拆行 | 当时未要求；残差 3 组事后补 | **23 组落地；2 组对不上（8.7%）** |
| 碎片处理 | 批 1 仍有 `of`→`之` 等 | 本批 `at`/`on`/`as`/`for`/`task` 已标「碎片·不独立翻译」 |

漏译和半截替换这条病，批 2 仍治得住。拆行规则**不是没落地**——23 组数量与本席独立复算一致，批 1 那五条残差也真按调用点修好了。

**仍在的残差是拆行质量，不是拆行缺席。** 2 组对不上的共同模式：索引里写了语境说明，但至少 1 处 `file:line` 没有对着代码读（`To` 的「发往」对的是日期控件；`attention` 对的是 `<code>` 标识符）。近义硬拆 4 组会把「应有组数」抬高，给长尾造成「每条短词都要拆两行」的错觉。

2/30 再次踩在阈值上，且 2 条里有 1 条就是乙.4 对不上的那行——随机通读在测拆行质量，不是测通顺。

**本席建议（不是裁定）：** 批 2 证明「拆行规则能落地、批 1 残差能修好」。长尾 4742 若继续走第 2 步一词一表， mechanized 的漏译/半截替换仍可压到 0，但多义短词必须**打开 file:line 再写中文**——本批 2 处写错，就是没打开。4742 是批 1 的约 7.6 倍串、约 23 倍词，按本批 8.7% 拆错率外推没有意义（拆行组不是均匀分布），但「短词不打开源码就会写错」会原样重来。推到第 3 步就地译的好处是调用点就在眼前，拆行成本更低。

---

## 4. 附见（不翻乙节已给的结论）

修由 TOK-170 那一席做，本席不改被验文件。

1. **`To` 发往、`attention` 需关注**——乙.4 已判，列在这里方便退回。
2. **`Item` / `Item body` / `Item not found.` → 项目**——与 `project` 撞名，宜「条目」。
3. **`Pin` → 置顶**（`CompanySkills.tsx:3847`，配 `currentPin ?? "untracked"`）。这是 git pin / 锁定版本，不是列表置顶。
4. **`Yes` 未拆**——确认按钮 vs Yes/No 选项。
5. **另造键 1 条** `tools.shared.labelHidden`。
6. **碎片标法已用在批 2**（`at`/`on`/`as`/`for`/`task`/`metadata` 代码参数）。批 1 的 `of`/`and`/`in`/`to` 仍按词典义躺在批 1 表里，不在本单范围。

---

## 5. 运行次数

`GET /api/issues/6e890810-6242-4f5e-b170-ef5955c48795/runs`，按本席 `agentId=13436d07-f626-4f37-aa3b-e265441aa6c2` 过滤：

| runId | status | startedAt | 是否计入 |
|---|---|---|---|
| `ff032422-d867-418b-b28f-ae5bc71eb99f` | running（本心跳） | 2026-09-16T12:24:47.532Z | 计入 |

同接口另四条 `agentId` 不是本席（`1429075e…` 为 TOK-170 执行席；`123de4e9…`、`c71e5cc0…` 为他席），不计入。

本单实际运行次数：**1**。

自身失误登记：本次未发现

---

## 变更日志（Changelog）

| 日期 | 修改人 | 修改内容 | 影响范围 | 关联文档 |
|---|---|---|---|---|
| 2026-09-16 | 产品研发部·产品验收席 | 初稿：甲通过、乙不通过（乙.4 拆行 23 组中 2 组对不上）；独立复算 960/0/0/2 | 全文 | 词表-修订-批2.md、词表-修订-批1.md、术语表.md、词表.md、词表修订验收-批1.md、TOK-171 |
