# ANC 第 2 步 · 词表修订复验（批 2 · 8 条封闭 ＋ 乙.4）

> **版本**：v0.1
> **状态**：🔍 评审中
> **负责人**：产品研发部·产品验收席（agent `13436d07-f626-4f37-aa3b-e265441aa6c2`）
> **日期**：2026-09-16
> **最后更新**：2026-09-16 / 产品研发部·产品验收席
> **密级**：🔒 Internal
> **主文档引用**：[词表-修订-批2.md](./词表-修订-批2.md)、[词表-修订-批1.md](./词表-修订-批1.md)、[词表修订验收-批2.md](./词表修订验收-批2.md)（被验产物只读，本席未改一行）
> **依赖规范**：[词表修订验收-批2.md](./词表修订验收-批2.md) TOK-171 原口径；本单范围封闭于 TOK-174 的 8 条

本文件是 TOK-175 唯一主交付物。本席没有改 `词表-修订-批2.md`、`词表-修订-批1.md`、`术语表.md`、`词表.md`，没有改任何源码。

本单**不重跑**乙.1 / 乙.2 / 乙.3 / 乙.5 / 乙.6（TOK-171 已判通过）。只验甲 8 条、重判乙.4、做丙回归。

---

## 0. 开工硬门

| # | 检查 | 结果 |
|---|---|---|
| 1 | 被验产物可读 | 是。批 2 对照总表本席数 **979** 行；批 1 主表 **631** 行；两份顶部均有 TOK-174 变更日志 |
| 2 | 本席未改被验文件 | 是。本席只往 `交付/第2步/词表修订复验-批2.md` 写入 |
| 3 | 独立口径，不沿用自称 | 是。8 条逐条打开 `file:line`；拆行组数本席脚本复算；抽 5 组换种子 |
| 4 | 依赖已解 | 是。本心跳开始时 [TOK-174](/TOK/issues/TOK-174) 仍 `in_progress`；读回时已 `done`（`completedAt=2026-09-16T12:53:03.653Z`），随后 checkout 复验 |

硬门通过。复算脚本在本 run scratch，不入库。乙.4 新种子：`175001`。排除上次抽过的 Diff / Replay / Try / Wait / used。

---

## 1. 甲、8 条逐条回查

### 1. `To` —— **通过**

两处都是日期截止，中文现已一致为「至」。

| 调用点 | 打开后 | 现译 | 判断 |
|---|---|---|---|
| `DecisionDateChips.tsx:64` | `<label>To</label>` 后接 `type="date"`，与 From 成对 | 至 | 对 |
| `AuditFeed.tsx:621` | `<span>To</span>` 后接 `<Input type="date" aria-label="To date">` | 至 | 对（原「发往」已改） |

总表 L195–L196；索引仍占 2 行，中文相同。TOK-174 允许「合并回一条，或给同一个中文」——走了后者。不算对不上。

### 2. `attention` —— **通过**

拆反已纠正。

| 调用点 | 打开后 | 现译 | 判断 |
|---|---|---|---|
| `DesignGuide.tsx:2175` | 正文 `fourth <code>attention</code> state` | 碎片·状态标识符 | 对（标识符，不独立译） |
| `Connections.tsx:388` | `{n} connections need attention` | 需关注 | 对（原「待处理」已改） |

总表 L815–L816。

### 3. `Yes` —— **通过**

已拆开，各自对得上。

| 调用点 | 打开后 | 现译 | 判断 |
|---|---|---|---|
| `IssueAttachmentsSection.tsx:311` | 删除确认弹层按钮，对位 `No` | 确认 | 对 |
| `ConnectionSetupFlow.tsx:3043` | `SegmentedOption label="Yes"`，对位 `No` | 是 | 对 |

总表 L231–L232。

### 4. `Item` / `Item body` / `Item not found.` —— **通过**

已从「项目」改为「条目」，与 `project` 不再撞名。

| 英文 | 现译 | 打开后 |
|---|---|---|
| Item | 条目 | `JsonSchemaForm.tsx:950` 数组项 `Item {index+1}`；`Pipelines.tsx:4338` 流水线条目卡标题 |
| Item body | 条目正文 | `PipelineItemBodyDocument.tsx:409` `aria-label="Item body"`；`PipelineSettings.tsx:1007` `label: "Item body"`（描述：Body text of the item being automated） |
| Item not found. | 未找到条目。 | `Pipelines.tsx:1936`、`:2731` 缺失条目空态 |

总表 L291 / L318 / L887。各 1 行、频次 2。

### 5. `Pin` —— **通过**

`CompanySkills.tsx:3847`（及 production `:3829`）为 `<span>Pin</span>` 配 `currentPin ?? "untracked"`，git pin / 锁定版本，不是列表置顶。现译「锁定版本」。总表 L717。

### 6. 另造键 `tools.shared.labelHidden` —— **通过**

`shared.tsx:104` 仍是 `case "hidden": return { key: "hidden", label: "hidden" }`。总表 L893 与索引 L34 建议键名均为 `tools.shared.hidden`。原 `词表.md` L10701 该英文的键就是 `tools.shared.hidden`。本席在批 2 全文未再见到 `tools.shared.labelHidden`。

### 7. 近义硬拆反向合并 —— **通过**

`pass` / `fail` / `pattern` / `Health` / `Bind` 主表各 **1** 行，每个英文只剩一条。索引表已移出。

| 英文 | 现译 | 打开后 | 同义？ |
|---|---|---|---|
| pass | 通过 | `SmokeLabTab.tsx:56` `aria-label="pass"`；`:334` 图例 `pass` | 是，测试通过 |
| fail | 失败 | 同文件 `:57` / `:335` | 是，测试失败 |
| pattern | 通配模式 | `ProfilesTab.tsx:555` `pattern {source.label}`；`:631` `Tools marked pattern` | 是，通配拉入 |
| Health | 健康状态 | `Secrets.tsx:3679` `Health {status}`；`OverviewPanel.tsx:80` `<StatCard label="Health">` | 是，服务健康 |
| Bind | 绑定 | `ProfilesTab.tsx:1214` 对话框确认；`:1293` 工具栏按钮 | 是，两处都是 Bind 按钮 |

### 8. 批 1 碎片 `of` / `and` / `in` / `to` —— **通过**

批 1 主表（及变更日志 §2）四行均为「碎片·不独立翻译，接界面时按整句处理」：`of` L99 频次 23、`in` L110 频次 18、`and` L160 频次 10、`to` L185 频次 9。

### 甲节 8 条：**8 / 8 通过**

---

## 2. 乙、重判乙.4（多义映射可回查）

按 TOK-171 原口径：独立统计拆行组数 → 换种子抽 5 组打开调用点 → 通读其余组。

### 2.1 本席独立统计

| 口径 | TOK-171 | 本次 |
|---|---|---|
| 对照总表数据行 | 983（937 单义 + 23×2） | **979**（941 单义 + 19×2） |
| 索引表组 / 行 | 23 / 46 | **19 / 38**（每组恰好 2 行） |
| 主表「同一英文、多个中文」 | 23 | **18**（`To` 两行中文都是「至」，不再构成多义） |
| 索引有、主表不算多义 | — | `To`（允许的「给同一个中文」） |
| 行数账 | — | 983 − 5（合并）＋ 1（`Yes` 拆开）＝ **979**，与文件一致 |

索引 19 组：Cap、Diff、Dismissed、Human only、Previous、Replay、Resolve、Select、Stage、Test、To、Try、Wait、Yes、attention、default、hidden、metadata、used。

### 2.2 抽 5 组（种子 `175001`，排除 Diff / Replay / Try / Wait / used）

`eligible` 14 组。`random.Random(175001).sample(eligible, 5)` 后再按英文排序：**Dismissed / Resolve / Yes / attention / hidden**。

| 英文 | 拆出的中文 | 打开后的语境 | 判断 |
|---|---|---|---|
| Dismissed | 已忽略提示 / 已收起项 | `WhatNeedsMe.tsx:435` 忽略后 toast `title: "Dismissed"`；`:659` `<Curtain label="Dismissed" count=…>` | 对得上 |
| Resolve | 标记为已解决 / 解决 | `DocumentAnnotationPanel.tsx:404` 标注完成按钮（对位 Reopen）；`ChatEndpointDetail.tsx:1109` 异常项处置按钮 | 对得上 |
| Yes | 确认 / 是 | 见甲.3 | 对得上 |
| attention | 需关注 / 碎片·状态标识符 | 见甲.2 | 对得上 |
| hidden | 已隐藏 / 隐藏 | `Pipelines.tsx:4152` `N nested items hidden`；`shared.tsx:104` 可见性枚举 `label: "hidden"` | 对得上 |

**5 / 5 对得上。**

### 2.3 通读其余 14 组

| 英文 | 中文 | 打开后 | 判断 |
|---|---|---|---|
| Cap | 请求上限 / 上限 | `InteractionGovernancePanel.tsx:252` 正文定义「Cap narrows every request」；`:271` 列表列头 | 对 |
| Diff | 比对差异 / 差异 | `CaseRevisionRail.tsx:221` 版本对比按钮；`task-chat-states.ts:102` `label: "Diff"` | 对 |
| Human only | 仅限人工 / 仅人工审批 | 同文件 `:254` 工具审批约束；`review-policy.ts:41` 策略选项 `label: "Human only"` | 对 |
| Previous | 上一张 / 上一页 | `ImageGalleryModal.tsx:133` `title="Previous"` 画廊；`Pipelines.tsx:5266` 分页按钮 | 对 |
| Replay | 回放会话 / 重试消息 | `TaskChatLab.tsx:226` 带 Speed 的回放；`ChatEndpointDetail.tsx:1100` `replay` + RefreshCw | 对 |
| Select | 选择组件 / 选择 | `DesignGuide.tsx:1020` `<Section title="Select">`；`ImportFromVaultDialog.tsx:788` 向导步骤 | 对 |
| Stage | 阶段配置 / 阶段 | `PipelineSettings.tsx:169` 设置分组 `label: "Stage"`；`Pipelines.tsx:2934` 流转字段 | 对 |
| Test | 运行测试 / 测试 | `PermissionsPanel.tsx:509` 触发测试按钮；`TestPanel.tsx:147` `<DialogTitle>Test {title}` | 对 |
| To | 至 / 至 | 见甲.1，两处日期截止 | 对（同义两行，见计数说明） |
| Try | 尝试搜索 / 立即体验 | `Search.tsx:620` `Try <code>status:todo`；`ChatEndpointSetup.tsx:1613` `Try {agent} in {provider}` | 对 |
| Wait | 等待时间 / 等待 | `PipelineSettings.tsx:2504` `FieldRow label="Wait"`（等到子件完成再流转）；`StatusCardSettingsForm.tsx:164` 轮询间隔前的 `Wait` | 对（说明见附见） |
| default | 默认凭据库 / 默认 | `ImportFromVaultDialog.tsx:926` 默认库徽标；`ProfilesTab.tsx:1272` `default {action}` | 对 |
| metadata | 元数据 / 碎片·代码参数 | `FrontmatterPanel.tsx:529` 表单 `<Label>metadata`；`SystemNoticeUxLab.tsx:368` `<code>metadata</code>` | 对 |
| used | 调用了 / 使用了 | `GatewayActivityPanel.tsx:96` `{actor} used {tool}`；`AuditTab.tsx:225` `{who} used {action}` | 对 |

通读 **14 / 14 对得上**。上次那 2 组错译已不在。

### 乙.4 结论：**通过**

拆行规则仍在，质量达到「打开调用点对得上」。组数从 23 降到索引 19 / 主表多义 18，账算得平。

### 乙节现在过不过：**通过**

TOK-171：乙.1 / 乙.2 / 乙.3 / 乙.5 / 乙.6 已通过，本次不复验。本次乙.4 通过，丙未见越界波及那五项。乙节收口为通过。

---

## 3. 丙、回归检查

判据：除这 8 条涉及的行外，两份修订表其余内容应与修复前一致。`交付/` 未跟踪，**不能**对修复前做 `git diff`。本席用结构账 ＋ TOK-171 已记录的残差数字交叉核。

| 检查 | 结果 |
|---|---|
| 批 2 行数 983 → 979 | 由「合并 5 组 −5、`Yes` 拆开 ＋1」精确解释。未见第三种行数变动 |
| 批 2 索引组 23 → 19 | 移出 pass/fail/pattern/Health/Bind，新增 Yes。其余 18 个英文组仍在 |
| TOK-171 已点名且不在 8 条内的拆行中文 | Diff / Replay / Try / Wait / used 中文与 TOK-171 抽 5 组表一致 |
| 批 1 TOK-170 五条残差 | Created 17+1+2、Tokens 4+5、Open 15+1+1、Organization 13、Failed run 3，译文与 TOK-171 乙.6 一致 |
| 批 1 主表行数 | **631** ＝ 626 唯一串 ＋ Created/Tokens/Open 拆行多出的 5 行，与修复前结构一致；变动只在 of/and/in/to 四行译文 |
| `git status --porcelain` | 无 `.tsx` / `zh-CN.json` / 已跟踪 `词表.md` 因本修复变脏。`cli/src/checks/managed-install-check.ts` 及其测试仍是第 0 项 TOK-163 授权面，与本批无关 |

**未见越界改动。** 不能声称「其余 970 行字节级未动」（无入库基线），结构账与已记录残差均未露出第三处改动。

---

## 4. 附见（不翻已给的结论）

1. **`To` 仍占索引 2 行**。中文已统一，主表不再算多义。允许，但长尾不必把「同一个中文」再拆两行。
2. **Wait 索引说明写成「等待时长」**。`PipelineSettings.tsx:2504` 实际是「等到子件完成再流转」的条件，不是时长输入。中文「等待时间」对 Wait 标签仍读得通，故未判对不上。
3. **Resolve 两处都是 Resolve 按钮**，粒度过细，接近当年点名的 Bind。不在本次封闭清单里，不扩范围。

---

## 5. 一句话结论

8 条 **8/8 通过**；乙.4 **通过**；乙节现在 **通过**；丙 **未见越界**。第 2 步词表修订按本单范围可以收口。

---

## 6. 运行次数

`GET /api/issues/d3d04b08-b7c0-4f3e-b80d-1b481cd7136a/runs`，按本席 `agentId=13436d07-f626-4f37-aa3b-e265441aa6c2` 过滤后读回：

| runId | status | startedAt | 是否计入 |
|---|---|---|---|
| `a0d5576e-4f17-4f85-8fdf-8b418cac0395` | running（本心跳） | 2026-09-16T12:47:34.406Z | 计入 |

同接口另外三条 `agentId` 不是本席（`1429075e…` 前端实现席、`123de4e9…` watchdog、`c71e5cc0…` 建单席），不计入。

本单实际运行次数：**1**。

自身失误登记：本次未发现

---

## 变更日志（Changelog）

| 日期 | 修改人 | 修改内容 | 影响范围 | 关联文档 |
|---|---|---|---|---|
| 2026-09-16 | 产品研发部·产品验收席 | 初稿：8 条全过、乙.4 通过、乙节通过、丙未见越界 | 全文 | 词表-修订-批2.md、词表-修订-批1.md、词表修订验收-批2.md、TOK-175 |
